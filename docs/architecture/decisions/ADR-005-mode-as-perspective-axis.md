---
title: Mode as a Generic `perspective` View Axis
status: Proposed (final, ready to implement — one-way door)
date: 2026-06-27
authors: architect (Opus)
migrated_from: adr_mode_as_view_axis
---

# ADR-005 — Mode as a Generic `perspective` View Axis

**Status:** Proposed — FINAL, ready to implement (a one-way-door reframe). The name `perspective` is confirmed (collision-free with live `node.view`, OLAP-correct). Detailed engine + full-stack plans exist (`.v3.md`, `.v3-PLAN.md`, `.v3-FULLSTACK.md`).

## Context

`mode` (year/range/both toggling) is currently a privileged, hardcoded concept threaded through the engine as `ctx.timeMode`, `ContextMapping.timeMode`, `BarDef.timeToggle`, `by-mode` DataSpec pivots, `KpiSpec.mode` (closed union), `modeOrder`, and a `mode-bar` node — a Law-1 violation (a privileged dimension) and a source of per-view duplication and special-case machinery (bar-visibility gates, `alwaysResolve`, effects). Lineage: OLAP perspectives / named-cube-query, NOT PowerBI bookmarks.

## Decision

- **Generalize `mode` into a generic `perspective` view axis.** `PerspectiveAxis{param, perspectives[]}` (perspectives[0] = default, one SSOT) + `PerspectiveDef{id, label, when?, scope:{timeBinding, metric?}}`. Active id lives in a NEW generic `ctx.perspectiveState: Record<string,string>` (Harel orthogonal-regions container), retiring `ctx.timeMode`.
- **Delete `by-mode` everywhere with NO shim** (Strangler expand-contract: relax mandatory `ContextMapping.timeMode` to optional + Postel-derive first, migrate pages, then delete). Rewire all mode-reading sites to `ctx.perspectiveState[param]`.
- **One primitive, composable to 2^N**: 7-door roadmap where each door is an optional field / Record-key / scope-key registration, fitness-locked shut. One perspective = zero machinery, structurally.
- **Permalink GENERATED from the PerspectiveAxis registry** (param names + default-elision) — url-permalink becomes an architecture property, not a convention.

## Rejected Alternatives

1. **Name the axis `view`** — REJECTED: collides with the live `node.view` (HOW/WHEN params); the v2 `view`-named docs were deleted. `perspective` is collision-free and OLAP-correct.
2. **Keep `mode` as a privileged closed concept (`timeMode`, `KpiSpec.mode` union, `by-mode` pivot) with a compatibility shim** — REJECTED: perpetuates the Law-1 violation and the per-view duplication machinery; the reframe ELIMINATES (not relocates) the bar-visibility gate, `alwaysResolve`, effects, and dup-span params.
3. **PowerBI-bookmarks-style snapshot-from-config** — REJECTED: cut to a render-option, not a config primitive; snapshots are a view *rendering* choice, not a stored axis.
4. **Multi-axis / `compare` / `navMode` as live axes now** — REJECTED (YAGNI): kept as deferred, shape-compatible peers (`scope.store/dims/blend/filters[]/facet`), not built until triggered.

## Consequences

- Positive: restores Harel orthogonality and Law-1 (no privileged dimension); one primitive yields composable multi-axis views; permalink becomes registry-derived.
- Negative / cost: a large expand-contract migration across ~8 engine sites + Constructor + 6 mode-reading sites + 3 pages; it is a one-way door once `by-mode` is deleted.
- Fitness functions: `FF-ONE-VIEW-NO-MACHINERY`, `FF-PERSPECTIVE-IS-PURE-FUNCTION`, `FF-NO-PER-VIEW-DUPLICATION`, `FF-VIEW-AXIS-GENERIC`, `FF-NO-BYMODE-REMNANT`, `FF-SNAPSHOT-VIEW-EQUIV`, `FF-SSR-WALKER-VIEW-AWARE`, `FF-PERMALINK-FROM-REGISTRY`.

---

## Detailed Record (preserved verbatim from architect memory)

> Migrated from `.claude/agent-memory/architect/`. Full engine + full-stack sequencing (P0–P6, P4.5, P5.2) follows.


# VISION — `mode` → generic `perspective` axis (perspective = f(state))

**Docs (FINAL):** `platform/work/VISION-mode-as-perspective-axis.v3.md` (analysis + config-tangle + minimal design + complete System-A/by-mode deletion set + resolved Qs + permalink innovation) · `VISION-mode-as-perspective-axis.v3-PLAN.md` (Strangler P0–Pfinal + FF suite + SSR opt + exact P0/P-opt specs) · `VISION-mode-as-perspective-axis.v3-FULLSTACK.md` (Constructor+API+end-to-end extension, see below) · `VISION-mode-as-perspective-axis.v3-FULLSTACK-BENCHMARK.md` (competitor table + authoring-UX lineage + deferred-door foresight). `VISION-mode-as-view-axis.md` = #1 audit/research record (historical). v2 docs DELETED (carried rejected `view` name).

## FULL-STACK EXTENSION (v3-FULLSTACK, 2026-06-27) — Constructor + API + end-to-end coherence
**ONE contract, real home = `packages/contracts`** (NOT only core): the arrow lets apps/api import contracts but NOT core/react. Contracts carries the JSON-Schema-validatable STRUCTURAL envelope (`when`/`scope` typed as JsonRecord at the wire); `packages/core` REFINES `when→VisibilityExpr`, `scope.timeBinding→TimeBindingSpec` for render. Identical to the EXISTING `SiteManifestContract`/`ManifestMode`⇄`ModeDef` opaque-blob-refined-by-consumer pattern (`packages/contracts/src/manifest.ts`).
**TWO api/contracts System-A surfaces the engine plan (§4) MISSED + must close:** (1) `SiteManifestContract.modes:ManifestMode[]` (manifest.ts:27,70) SERVED by GET /api/bootstrap from `site_config.modes` key (bootstrap/index.ts:94,269, DEFAULT_MODES=[]) — this is a SITE-SCOPED registry, distinct from PAGE-SCOPED modeOrder/PerspectiveAxis. Repurpose→`perspectiveKinds` (Postel-alias `modes` until runner migrates; `site_config.modes`→`perspective_kinds` data migration P6). (2) `page-config.schema.json:436 node_mode-bar__default` (4th schema site beyond the 3 modeOrder page-defs @169/253/337) — DELETE (axis is page-level not a node).
**THE site/page split (key coherence insight):** site-scoped = WHAT KINDS exist (id→label/icon, the registry, = modeRegistry content) → `perspectiveKinds` in manifest+registry singleton. Page-scoped = WHICH perspectives this page exposes + order + scope/when → `page.perspectiveAxis` (rides the page config blob, served verbatim in pages[id], NO new manifest field). Same registry↔instance split the whole platform uses (nodeRegistry↔children, param-schema-registry↔filterSchema). PerspectiveDef.id picks from registered kinds (Law 8 pick-don't-type).
**Constructor authoring = mostly REGISTRATION into existing seams, ONE new organism:** per-node `when:perspective-is` → existing recursive `VisibilityBuilder` (features/visibility/; register `perspective-*` leaf ops EXACTLY like live `mode-is/-in/-not` @visibilityFactory.ts:16,37-39 + VisibilityBuilder.tsx:35 OP_LABELS); `scope.metric` → existing semantic-layer R1 MetricDef picker; NEW = page-level "Perspectives" PANE (Power BI bookmark-pane IA + Grafana variables-list + Tableau-param + Looker-measure UNIFIED) replacing page-root `modeOrder` array field (pageSchemaSource.ts:62,75 "Modes" group). Pane reuses: `filterSchemaModel` map⇄node[] lossless-reorder adapter (for PerspectiveDef[] list), `YearsField`/$ctx cube-profile time-ref picker (timeBinding), MetricDef picker (metric), VisibilityBuilder (when), CanvasView+perspectiveState live-preview chip (G3 seam). Pane fields driven by NEW `perspectiveSchemaSource` (same SchemaSource port as pageSchemaSource/filterParamSchemaSource) → deferred scope.* keys auto-surface (OCP).
**ByModeEditor DELETE → need DECOMPOSES (NOT a replacement DataSpec editor):** "node only in perspective X" = `when:perspective-is(X)` in VisibilityBuilder; "node measures differently" = perspective `scope.metric` (common) or node `value.type` (one-off, LOW-2). Forces coverage.fitness.test.ts edit: drop 'by-mode' from DATASPEC_EDITORS:59 + discriminant-manifest.ts:36 (Exact<> assert moves both together).
**COVERAGE GATE has NO axis dimension (structural gap) — ADD 5th axis:** coverage.fitness.test.ts enumerates DataSpec/ParamDef/TransformOp/VisibilityOp only; a page-level PerspectiveAxis capability has ZERO authorability assertion. Add `PERSPECTIVE_SCOPE_KEYS` (timeBinding/metric surfaced, store/dims/blend/facet ALLOWLISTED roadmap-keyed) = visible shrinking list (Law 8).
**API validate-on-save = extend config-cube-contract.fitness.test.ts** (already walks every page asserting refs-exist+pinned, builds DSD per dataset, generic over dims Law 1) with perspective-contract: FF-PERSPECTIVE-REFS-EXIST (timeBinding.dim real cube dim, scope.metric registered MetricDef, when/filters[].ref real), FF-NO-ORPHAN-PERSPECTIVE, FF-NO-PER-VIEW-DUPLICATION (server-side LOW-2 dual-encode guard), FF-PERMALINK-FROM-REGISTRY (server), FF-DEFAULT-IS-FIRST. Permalink+snapshot = SERVED guarantee (Law 9 as artifact property): served pages[id].perspectiveAxis fully describes URL surface; permalinkParams fed by served config. `migratePageConfig` (bootstrap:238) = the modeOrder→perspectiveAxis legacy-desugar lazy seam.
**v3 RESIDUAL RESOLVED: scope.metric (registered MetricDef) CANONICAL** for full-stack reasons — authorable(pick, picker exists), validatable(ref resolves server-side, node-local opaque to api), permalink/snapshot-coherent(regenerable unit). node-local value.type = single-node OVERRIDE only (LOW-2, never dual). Narrowed open item = DATA-INVENTORY check (are all 3-page CAGR/share already registered MetricDefs in shipped R1? if not, P5 registers-first preferred or node-local for one-offs) — NOT an architecture decision.
**Full-stack STRANGLER map** extends P0-Pfinal w/ contracts+api+panel folded per phase, expand-contract now spans contracts→api (ManifestMode alias, site_config data migration) so NO surface ever un-typecheckable. P0 adds: NEW contracts perspective-axis.schema.json + TS mirror + ManifestMode→ManifestPerspectiveKind alias. P-final = the Perspectives pane (after P5, needs served axis+permalink). Panel net-new = ONLY P2 op-registration + P-final pane.
**Competitor verdict:** NO platform (PowerBI/Tableau/Looker/Superset/Grafana/Retool) unifies derived-not-captured + declarative-scope-one-object + non-coder-authorable + generated/served-permalink + server-validation + N=1-free + registry↔instance-split. Deferred doors (facet/scope.store/scope.blend/2nd-axis) = additive scope.* keys, CONSTANT not linear cost (generic Record container pre-empts per-axis privileged-field debt). FLAGGED caveat: 'all-perspectives' snapshot = cross-product over N axes → faceting door needs own pagination, must NOT inherit 'all-perspectives'.
**SYNTHESIS (v3-SYNTHESIS, 2026-06-27, the capstone-of-capstones, doc=`platform/work/VISION-mode-as-perspective-axis.v3-SYNTHESIS.md`):** corpus ground-truth-reverified (ScopeOverride.compare REAL+half-built @scopeOverride.ts:37 = live D-COMPARE anchor; perspectiveState green-field; renderNode.ts:229=ctx.mode.current). Design EXCEPTIONAL+READY — verdict "this is the plan; build it." FOUR P0 refinements, each a CUT or seam-shape (contract gets SMALLER, NOT a feature, YAGNI held): (1) CUT `snapshot` from PerspectiveAxis config → a `renderPage(...,{snapshot})` RENDER-OPTION (it serializes render-INTENT not artifact-property; removes field from type+schema+manifest+round-trip+coverage; full-stack doc's serve-in-config was a regression to undo). (2) `page.perspectives: Record<param,PerspectiveAxis>` NOT singular-`perspectiveAxis`+future-plural-`perspectiveAxes` — ONE container shape mirroring ctx.perspectiveState Record; multi-axis = a future KEY, zero rename; lock at P0. (3) RE-SHAPE `scope` from closed `{timeBinding,metric}` interface → a `Record` of REGISTRY-KEYED scope-keys (timeBinding+metric registered today); every deferred scope.* door = a registration NOT an interface-widen (true OCP); coverage-gate 5th axis reads the registry, retiring the hand-maintained allowlist; FF-SCOPE-KEYS-REGISTERED. This is the ONE closed interface the corpus left that doors would force open. (4) `facet` belongs on `PerspectiveAxis.render:'switch'|'facet'` (Grammar-of-Graphics: facet operates on the AXIS not one perspective's scope), NOT `scope.facet`. RESEARCH sweep (XState/CRDT/Malloy/Cube/SDUI-scale/Observable/GoG): corpus already stole the load-bearing concepts; yields exactly ONE new door D-GUARD (`PerspectiveDef.available?:VisibilityExpr` = statechart availability-guard, closes "perspective offered but renders empty", reuses evaluator) + the facet relocation + foresight-note (URL IS the Harel history-state, so "back to my view" needs nothing). REJECT XState actor-model/CRDT-runtime (imperative orchestration violates f(state) purity). 7-DOOR ROADMAP (D-GUARD/D-FACET/D-MULTIAXIS/D-COMPARE/D-STORE/D-BLEND/D-METRIC-VIEW) each PROVEN a pure additive change of EXACTLY 3 shapes: optional-field | Record-key | scope-key-registration — none touches interpreter/evaluator/active-id-slot/existing-config; each fitness-locked SHUT until a real 2nd caller. POWER claim made concrete: N doors = 2^N composable view-behaviours from ONE primitive, no pairwise integration code (Power BI bookmarks don't compose w/ params; Grafana vars carry no typed effect) = more power from a SMALLER core. Reframe stress-tested vs `scenario`/`lens` → `perspective` still best (OLAP-correct, collision-free). FLAG (guardian-of-canon): the half-built ScopeOverride.compare is a LIABILITY to contain (use it via D-COMPARE scope-key OR acknowledge as debt — don't leave ambiguous-middle); pre-built doors are worse than none. Build order: lock the 4 refinements at P0, then P0(+scope-key-registry skeleton, FF-SCOPE-KEYS-REGISTERED+FF-WHEN-IS-ESCAPE-ONLY stubs) ∥ P-opt(snapshot now a render-option), then P1-Pfinal unchanged. NO further paper — next artifact = P0 CODE.
**P4.5 — ENGINE-OWNS-THE-BINDING phase INSERTED (2026-06-27, engine-specialist escalation resolved, DECISION=A).** The P4 byte-identical blocker: the geostat config cannot collapse the two filter bars into one + drop `effects` and stay byte-identical until `scope.timeBinding` ACTUALLY owns the time binding — and it can't yet, for 3 ground-truth-verified reasons: (1) default-resolution gate is BAR-scoped (`useFilterState.ts:104-112` gates on `barShowWhen` only) — the two-bar split IS the parity mechanism: in `range` the hidden `year-bar` (`showWhen:{mode:{neq:range}}`, provisioning :1201) leaves `year` (`pick:last`=2025, :1184) unresolved → `ctx.dims.time` unset → dynamics timeseries renders FULL SPAN; collapsing one bar over-resolves → regression. (2) the window writes `${dim}From`/`${dim}To`=`timeFrom`/`timeTo` (`perspective-axis-parser.ts:155-158`) but geostat KPIs read `{$ctx:"fromYear"}`/`{$ctx:"toYear"}` (provisioning :142-148; `context`:1214 maps toYear→toYear) → declared window writes keys nobody reads. (3) no ctx-ref single-period pin — `scopeCtxByPerspective` (:143-149) pins only a literal 1-element number list, not a `{$ctx:"<param>"}` ref. **DECISION = (A): land an additive engine-binding phase (P4.5) BEFORE the config collapse** (Strangler-canonical: mechanism lands alongside live legacy bars → proven byte-identical → config switches+collapses P5 → P6 deletes dead System A; each step independently green). REJECTED (B) fold-into-P6-atomically (couples collapse+binding-transfer+System-A-delete = the big-bang Strangler avoids) and (C) re-scope (blocker is real+contained). **P4.5 = three additive, byte-identical, fitness-locked engine capabilities** (engine-only + ONE React filter touch; no config; inert until opted in): **(a) ctx-ref single-period PIN** — extend the PIN branch to resolve `timeBinding.pin:{$ctx:"<param>"}` via the SAME `resolveTimeBound` ref dispatcher (`time-dimension.ts:57`), write `dims[dim]`; unset/NaN writes nothing (all-years path, `isUnsetTime` SSOT). **(b) configurable window TARGET-KEYS** — add `targetKeys?:{from?,to?}` to the timeBinding (`perspective-axis.ts:50`), write `dims[targetKeys.from ?? \`${dim}From\`]` so geostat declares `{from:"fromYear",to:"toYear"}` and drives the EXISTING resolvers; absent = today's `${dim}From/To` byte-for-byte. **(c) perspective-aware default-resolution gate** (Protected Variations — move the seam bar-visibility→perspective-scope) — extend `defaultParams` (`useFilterState.ts:104-112`): a param OWNED by the active perspective's `timeBinding` resolves; a param owned ONLY by a non-active perspective (and no other live default) does NOT → collapsed single-bar `range` leaves `time` unset → FULL SPAN preserved STRUCTURALLY (parity fix without two bars). No-binding configs reduce to the legacy `barShowWhen` branch exactly. FFs: FF-BINDING-PIN-CTX-REF / FF-BINDING-TARGET-KEYS / FF-PERSPECTIVE-DEFAULT-GATE / FF-BINDING-ADDITIVE-IDENTITY + FF-SNAPSHOT-VIEW-EQUIV stays green both with legacy bars AND once config switches. SEQUENCE: P4→P4.5(engine-only, ∥ P3)→P5(collapse, now byte-identical)→P6(delete the `barShowWhen` default-gate branch — perspective-scope gate becomes the sole SSOT — only safe AFTER every page migrated). Parity preserved (asserted per-perspective): full-span range dynamics, CAGR KPIs (window targetKeys→fromYear/toYear), single-period KPIs ({$ctx:toYear}), regional map/sectors, accounts SNA bars, ka+en locales. *Files:* `core/config/perspective-axis-parser.ts`+`perspective-axis.ts`+`core/time-dimension.ts`, `react/src/filters/useFilterState.ts:104-112`. Plan doc updated: `.v3-PLAN.md` §0 (decision) + P4.5 + re-sequenced P5/P6 + FF table.

**P5.2 — MIGRATE the three surviving System-A surfaces phase INSERTED (2026-06-27, SECOND engine-specialist escalation resolved, DECISION=A).** P5 moved the time BINDING (live: geostat config has `perspectives` blocks authored in all 3 pages alongside legacy — `year` pins `{$ctx:"year"}`, `range` windows `[{$ctx:"fromYear"},{$ctx:"toYear"}]`+`targetKeys`, exactly P4.5 caps; verified `geostat.provisioning.json:1207`). But three System-A surfaces STILL LIVE w/ NO perspective replacement, so P6's "delete System A" would regress: (1) `mode-bar` node (`packages/plugins/nodes/mode-bar/default/ModeBarShell.tsx`, 3× config @:28/:1304/:2569) = live year/range TOGGLE reading `ctx.mode` via `useModeContext(timeModeKey,page.modeOrder)`→`modeRegistry`; NO perspective-toggle node exists. (2) `KpiSpec.mode:'year'|'range'|'both'` (`packages/core/src/data/kpi.ts:62`) = kpi-strip partition filtered at TWO sites w/ SAME predicate `s.mode==='both'||s.mode===ctx.timeMode`: `interpretKpis:221` (render) AND `extractKpiRequirements:309` (warm — must move TOGETHER or warm-set diverges = kpi-strip cold-throw); closed-union Law-1 violation reading ctx.timeMode. (3) `modeOrder` (3× config + nav-sort rank `navUtils:137-144` + `useModeContext` avail list `SiteRenderer:130-131`); navUtils ELSE already perspective-aware (getNavMode parses perspective-is/mode-is/{op:eq}), only `modeOrder?:string[]` sort key remains. These keep useModeContext/ModeContext/modeRegistry load-bearing. **DECISION=(A): insert P5.2 (migrate-then-delete, same Strangler as P4.5/P5) BEFORE P6.** REJECT (B) leave-surviving-legacy (user mandate: no surviving legacy/shims). **P5.2 = 3 additive, byte-identical, fitness-locked migrations:** **(1) NEW `perspective-bar` plugin node** (`packages/plugins/nodes/perspective-bar/default/`, mirrors mode-bar but: `param?` NOT `modes` — options DERIVE from `PerspectiveAxis.perspectives[]` killing the modes-list SSOT-dup; reads active id from `sectionCtx.perspectiveState[param]` via `activeIdForAxis` fallback perspectives[0]; label=authored `PerspectiveDef.label`; <2-hide carries; D-GUARD `available?` filters). Engine seam: keep `RenderContext.mode` triad field NAME (rename=P6 churn), SiteRenderer derives `available` from axis perspectives[] not modeRegistry, `current`=activeIdForAxis, `set`=filterSet(param,id)+URL (permalink seam; UX byte-identical: same buttons/order/active/click-URL). Config: mode-bar→perspective-bar ×3 drop modes, param:"mode". **(2) `KpiSpec.mode`→`when:perspective-is`** (canonical closure: show/hide IS visibility, mode IS privileged closed union). Approach=PER-ITEM `when?:VisibilityExpr` on KpiSpec (NOT strip-level partition key=would re-privilege strip), reuses evalVisibility+perspective-is, same core layer no arrow. Factor `kpiVisible(spec,ctx)` ONE fn both filter sites call (warm===render SSOT). mode→Postel fallback until P6. Config: `"mode":"year"`→`"when":{op:perspective-is,perspective:"year",param:"mode"}`, range likewise, `"both"`→drop entirely (no when=always). value.type UNTOUCHED (year↔range measurement diff stays node-local, RESIDUAL 2). **(3) `modeOrder`→`perspectives[]`-order** (capstone MEDIUM-2): perspectives[] order=SSOT for toggle order (covered by (1)) AND nav-sort; SiteRenderer:190 passes `axis.perspectives.map(p=>p.id)` to navUtils (no navUtils signature change — already takes ordered list, only SOURCE moves); delete modeOrder config ×3 (:1202/:2236/:3855) + schema (:169/253/337). FFs: FF-PERSPECTIVE-BAR-FROM-AXIS/FF-PERSPECTIVE-BAR-EQUIV/FF-KPI-WHEN-NOT-MODE/FF-NO-PRIVILEGED-KPI-UNION(P6 gate)/FF-NAV-ORDER-FROM-PERSPECTIVES + FF-SNAPSHOT-VIEW-EQUIV carried (green WITH legacy live AND post-switch). SEQ P5→P5.2(plugin+engine+config-switch for 3 surfaces, ∥ nothing-binding)→P6. **P6 now grep-clean** deletes ALL System A: mode-bar node, ModeContext/useModeContext/ModeProvider, modeRegistry alias, applyEffects mode-clear, KpiSpec.mode union+kpiVisible fallback, modeOrder, barShowWhen default-gate+alwaysResolve, ContextMapping.timeMode/rangeKey/timeToggle, ctx.timeMode+TimeMode, ScopeOverride.timeMode+compare+resolveCompareRows+renderNode:335-342+compareRows/Label, mode-is/-in/-not+{op:eq,param:mode} Postel+legacy modeOrder desugar in parsePerspectiveAxes. Grep-zero acceptance across packages/**+apps/api/provisioning/**. Parity: toggle still switches year/range (same buttons/order/active/URL), same KPIs show per perspective (render+warm both sites), nav order preserved, all 3 pages ka+en. *Files:* perspective-bar/** NEW · kpi.ts (when?+shared kpiVisible) · SiteRenderer.tsx:130-131,190 · navUtils.ts · geostat.provisioning.json · page-config.schema.json. Plan doc: `.v3-PLAN.md` §0b (decision) + P5.2 phase + re-sequenced P6 deletion-set + 5 new FFs. NOTE: mode-bar path in body §11 line is `packages/plugins/nodes/mode-bar/...` (NOT plain `plugins/` — the de-tenant added `packages/`); kpi.ts at `packages/core/src/data/kpi.ts`.

**Status:** VISION #3 — **FINAL, implementable, one-way-door. READY TO IMPLEMENT (P0 + P-opt parallel), with the 4 SYNTHESIS refinements locked at P0; P4.5 engine-binding phase resolves the P4 byte-identical blocker; P5.2 surviving-surfaces phase resolves the P6 sequencing blocker (both decision A).** Verdict = AGREE-WITH-REFRAME + BUILD-IT, ground-truth-verified 2026-06-27. Name confirmed `perspective`. Engine spine (P0-P4.5) + config perspectives-blocks VERIFIED BUILT in tree; remaining = P5 collapse, P5.2 surviving surfaces, P6 grep-clean delete.

> Body below is the v2-era analysis (still accurate). v3 supersedes naming (`view`→`perspective`), confirms DELETE by-mode (no shim), bakes in the capstone MUST-FIX (expand-contract order, perspectiveState SSOT + all 6 callsites, full modeOrder + by-mode + Constructor deletion sets, corrected react/src/engine paths, one-default SSOT, scope.metric-vs-value.type rule), the Harel orthogonal-regions invariant, and the permalink-from-registry innovation.

## VISION #2 — EMPIRICAL CORRECTIONS (verified, supersede #1 hypotheses)
1. `by-mode` DataSpec + ByModeResolver = **DEAD CODE** rel. to config: `grep "by-mode" geostat.provisioning.json` = **0 matches** (exists only in engine/tests/schema). The hypothesised hardest residue (per-mode data envelopes) DOES NOT EXIST. P3 = retire unused member (shim or delete), ZERO migration risk.
2. **Live DOM ALREADY LAZY** — `renderNode.ts:228-231` gates `view.visibleWhen` BEFORE resolveNodeRows/useNodeRows/useKpiRows. Eager 2× warm lives ONLY in static SSR walkers (`warm.ts collectRequirements` + `api.ts walkNode`, both via `nodeWalk.collectChildNodes`) which ignore visibleWhen/mode. Fix = make those 2 walkers view-aware (apply same `evalVisibility` gate); evalVisibility is `packages/core` engine-pure → crosses NO arrow.
3. **`(d)` privileged residue = EMPTY** — every real mode occurrence maps to (a) scope.timeBinding / (b) scope.metric swap / (c) when:view-is visibility. "Eliminate not relocate" VERIFIED total.
4. Real config uses `{op:eq,param:mode,is:…}` (System-A string form, 11 gates), NOT `mode-is`. P2 generalises the string-param gate to `view-is`.

## CONFIG-TANGLE (geostat.provisioning.json, 4214 lines, 3 pages: accounts L1-1240/gdp L1241-2302/regional L2303-3876)
7 tangle sites: T1 KpiSpec.mode split (one strip = two: ~4yr+6rng/page) · T2 two near-identical bars (range-bar/year-bar, `mode` hidden dup ×2) · T3 11 visibleWhen{param:mode} gates · T4 mode-clearing effects (1/page ×2 rules, ROOT of useFilterState gate+alwaysResolve) · T5 timeMode binding + modeOrder + mode-bar (3 decls/page) · T6 fromYear/toYear-vs-year split (time bound 3 ways, 23-24 refs/page) · T7 regional spanFrom/spanTo + alwaysResolve hoist. Genuine difference = ONLY what each KPI measures; rest = pure duplication. Reader runs mode-filter at 5 sites/page. Cost paid even at N=1.

## MINIMAL DESIGN (YAGNI)
`ViewAxis{param,views[],default?}` + `ViewDef{id,label,when?,scope:{timeBinding MANDATORY, metric? THIN-OPT}}`. DEFERRED additive keys (NOT built): scope.store/dims/blend, filters[], facet, multi-axis. view=f(state) on EXISTING selectors+evalVisibility, NO new interpreter; only new step = scope ctx by active view's timeBinding before interpretSpec/interpretKpi.

## RESOLVED OPEN Qs
Q1 name = **`view`** (axis; ids 'year'/'range'; fall back `perspective` if node.view collision astonishes) · Q2 active id in NEW generic `ctx.viewState:Record<axis,id>` slot (NOT dims, NOT timeMode; retires the Law-1 violation) · Q3 by-mode dead · Q4 multi-axis = plural viewAxes + viewState keys, zero rework, orthogonal · Q5 faceting deferred door (shape compatible) · Q6 navMode = SEPARATE peer axis (different region, folding = re-privilege) · Q7 SSR snapshot = **active-view-only DEFAULT + explicit `snapshot:'all-views'` escape** (URL carries view param → permalink view-specific → active-only is correct AND the optimization; prefetch-on-idle always wins) · Q8 blast radius gated by round-trip corpus + FF-BYMODE-DESUGAR-EQUIV + FF-SNAPSHOT-VIEW-EQUIV (row-identical per view) before P6.

## STRANGLER (additive, non-breaking, fitness-locked)
P0 types+ADR+modeRegistry→viewRegistry alias · P1 viewState slot + ctx-scoping (identity when no viewAxis = byte-identical; timeMode Postel-alias) · P2 view-is ops + Postel-alias {op:eq,param:mode} · P3 retire by-mode (dead) · P4 viewAxis parser + legacy desugar · P5 migrate 3 pages (delete effects/2nd bar/dup/mode-bar/modeOrder/timeMode; span→computed) gated by FF-SNAPSHOT-VIEW-EQUIV · P6 retire System A (gate/alwaysResolve/timeMode/mode-bar) · P-opt view-aware walkers (parallel after P2) · P-final Constructor Views panel. FFs: ONE-VIEW-NO-MACHINERY/VIEW-IS-PURE-FUNCTION/NO-PER-VIEW-DUP/VIEW-AXIS-GENERIC/SCOPE-DECLARATIVE/BYMODE-DESUGAR-EQUIV/VIEW-ROUNDTRIP/SNAPSHOT-VIEW-EQUIV/SSR-WALKER-VIEW-AWARE.

## VERDICT = READY. 3 small residual user decisions: confirm `view` name · delete-vs-shim by-mode (lean delete) · confirm active-view SSR default. P0 + P-opt = pure additive two-way doors, can start in parallel on nod.

## (#1 record below, retained)

## Root finding (the crux — verified in code)
**TWO parallel mode systems coexist, half-migrated:**
- **System A — privileged `timeMode` (legacy, owns DATA + FILTER path):** `ctx.timeMode` named field beside generic `dims` (`core/context.ts:57`, Law-1 violation the header apologises for); `ContextMapping.timeMode` (`filter-params.ts:292`); `by-mode` DataSpec keyed on `ctx.timeMode` (`data-spec.ts:181`, resolver `resolvers.ts:141-163`, warm-key `spec.ts:96,126`); `rangeKey`/`timeToggle`/`timeModes` (`filter-params.ts:113,248`); the **bar-visibility default-resolution gate + `alwaysResolve` hoist** (`useFilterState.ts:88-112,230`); imperative switch `applyEffects(timeModeKey,…)` (`SiteRenderer.tsx:175`).
- **System B — generic `Mode*` (newer, Grafana-inspired, owns VISIBILITY + NAV only):** open `ModeId=string` + `modeRegistry` (`mode/registry.ts`); `RenderContext.mode` (`react/.../context.ts:65`); `mode-is`/`mode-in`/`mode-not` (`visibility.ts:25-28`); `page.modeOrder`, `navMode`.
- B is ALREADY ~90% the reframe (registry-driven, open-string, Law-1) but stops at visibility/nav; A still owns data+filters. The reframe = FINISH B, RETIRE A.

## The reframe (decision)
Elevate the AXIS, not the privileged mode. `ViewDef = { id, label, when?, scope?, filters?, layout? }` + page-level `ViewAxis = { param, views[], default }`. `time-mode` (year/range) = first instance; `compare`(N37, `context.ts:75`) + `navMode` = peer axes (statechart orthogonal regions). Render every artifact as **view = f(active URL state)** on the EXISTING selectors + visibility evaluator — NO new interpreter. `scope` carries the per-view data difference (timeBinding year-pin vs [from,to] clamp — already first-class `data-spec.ts:115`; optional generic `store` key / `metric` ref) — bind the SCOPE not the store (store is orthogonal, already a routed manifest).

## 4-question resolution
- Q1 special vs N-of-N → **N-of-N, generic `view` axis** (REFUTES privileged-mode framing, UPHOLDS Law 1).
- Q2 shared content → **composition; TODAY partly DUPLICATED** (`account`/`mode`/`measure` in BOTH bars `geostat.provisioning.json:1084-1206`); reframe = declare once + Superset-style filter-scoping + `when`-gated nodes.
- Q3 mode↔datasource → **bind SCOPE not STORE** (locality win as state-scoped resolution; coupling smell if mode owns store).
- Q4 eliminate vs relocate → **elevating mode-as-is RELOCATES; reframe ELIMINATES** (by-mode/gate/alwaysResolve/effects/dup all dissolve). DECISIVE for the reframe.

## One-view property (user's headline, made structural)
`f(state)` with one state = `f`. A page with 0/1 ViewDef instantiates ZERO machinery. Today a one-mode page STILL drags timeMode/by-mode/gate (`§1.2 #8`) — proof the current model is mis-factored. Locked by **FF-ONE-VIEW-NO-MACHINERY**.

## Field research convergence
NO platform makes a time-specific "mode" a primitive. Power BI **bookmarks** (named captured state) = the model; Tableau params+actions; Superset tabs+**filter scoping** (Q2 answer); Grafana **custom variable** (mode=just a variable)+library panels; Vega-Lite **params drive everything, no mode**; Elm/Redux **view=f(state)**; OLAP **faceting/trellis** = "render view per axis value" (the deep form of "switch brings its unit"). Convergent primitive: **generic named state + pure-function view**.

## Data-binding doors compose on `ViewDef.scope`
semantic metric ref (R1, point↔CAGR = metric swap, kills by-mode KPI envelopes) · multi-store key · static snapshot · `blend` step (compare view) · `timeDimension` · Constructor "Views" panel. See [[data-binding-shipped]], [[adr-data-blending-decision]], [[adr-time-range-readiness-seam]].

## Fitness fns
FF-ONE-VIEW-NO-MACHINERY · FF-VIEW-IS-PURE-FUNCTION (no key-clearing on switch) · FF-NO-PER-VIEW-DUPLICATION · FF-VIEW-AXIS-GENERIC (Law1, no new timeMode) · FF-BYMODE-DESUGAR-EQUIV (Postel) · FF-VIEW-SCOPE-DECLARATIVE (Law2) · FF-VIEW-ROUNDTRIP.

## Strangler path
P0 name+ADR (modeRegistry→viewRegistry alias) · P1 ViewAxis parser + ctx-scoping (additive, falls back to modeOrder+timeMode desugar) · P2 view-is = mode-is aliases · P3 by-mode desugar · P4 migrate geostat page (delete effects/2nd bar/dup) · P5 retire System A · P-final Constructor Views panel. Non-breaking throughout (Postel + expand-contract).

## Open Qs for team (seams left)
naming (view/perspective/scenario) · where active-view-id lives in ctx (lean: separate generic `viewState` map, NOT in dims, NOT timeMode) · per-view node-shape diff (lean: metric-swap primary, when-gate for structural, by-mode only as deprecated escape) · multi-axis scope composition · faceting (later door) · navMode fold-in · permalink semantics · P4 migration blast radius.
