# AR-49 M1.3a — Wizard → Studio capability-parity checklist

> **Status:** M1.3a COMPLETE (additive, reversible) · **Date:** 2026-07-09 · **Branch:** `feat/ar49-m0-metric-first-authoring`
> **Scope:** re-home the remaining wizard-only capabilities into the flag-gated Studio shell so it reaches **full parity** with the 3-step wizard. This is the evidence the owner reads before authorizing **M1.3b** (delete `ConstructorWizard`/`WizardStepper`/`steps/*`/`WIZARD_STEPS` + flip `STUDIO_SHELL` default on).
> **Boundary respected:** the wizard is **untouched in behavior and remains the default**. Nothing was deleted; the default flag was not flipped. Where a wizard step and a Studio surface needed the same capability, the reusable body was **extracted** into a shared component consumed by both (no fork — Law 6 DRY, Law 7 Strangler). Store writes are byte-identical (same hooks/actions).

## How to preview parity (flag-on)
```js
localStorage.setItem('statdash.studioShell','on'); location.reload()
```
Flag off → the wizard exactly as today. Flag on → the Studio with every capability below reachable. Needs a running api+db for MetricPalette population and the source/spec lists.

## Legend
- **done** — reachable in the Studio via a relocated/mounted component, byte-identical writes.
- **done⁺** — reachable **and exceeds** the wizard (wizard had a stub).
- **M1.2** — already relocated in the prior scaffold milestone.
- **by-design-absent** — intentionally not in the Studio (the anti-waterfall inversion).
- **deferred** — a later milestone; **no capability is lost** in the meantime.

---

## DataStep → Data surface  (`studio/surfaces/DataSurface.tsx`)
The governed **Metric Palette** is the primary affordance; the full raw modeling is the **same `DataModelingPanel`** the wizard renders, demoted under a collapsed **"დამატებითი მოდელირება" (Advanced)** disclosure, lazy-loaded on open.

| Wizard capability | Studio home | Status |
|---|---|---|
| DataSource list / browse / select | Data → Advanced → `DataModelingPanel` | done |
| Add source (`SourceAuthoringPanel`) | " | done |
| Excel upload (`ExcelUpload`) | " | done |
| Delete source | " | done |
| Reorder sources (dnd-kit) | " | done |
| DataSpec list / select | " | done |
| `DataSpecEditor` (query/pivot/growth/transform/…) | " | done |
| `ShowMe` suggestions | " (+ bind-time on canvas) | done |
| **Metric Palette (M0)** | Data surface **top / default** | M1.2 |
| "Continue → Site" gate | — | by-design-absent (no waterfall) |

## SiteStep → Pages & Site surface  (`studio/surfaces/PagesSiteSurface.tsx`) + Style surface
Identity + navigation are the **shared `SiteIdentityEditor` / `NavEditor`** the wizard now also renders.

| Wizard capability | Studio home | Status |
|---|---|---|
| Site name | Pages&Site → `SiteIdentityEditor` | done |
| Default locale (ka/en) | " | done (top-bar toggle = deferred surfacing, not a lost capability) |
| Logo URL | " | done |
| Nav list / delete | Pages&Site → `NavEditor` | done |
| Nav **reorder** (dnd-kit) | " | done (was the last unrelocated site capability) |
| "+ add page" (wizard = `notify('coming soon')` **stub**) | Pages&Site → real `PageBrowser` (createPage / createFromTemplate) | **done⁺** |
| Theme token viewer (read-only) | Style surface (read-only) | done (writable editor = M1.4) |
| Continue / back gates | — | by-design-absent |

## PageStep → canvas + docks + top bar  (relocated in M1.2, verified)
| Wizard capability | Studio home | Status |
|---|---|---|
| `NodePalette` | Insert surface | M1.2 |
| `ChromePalette` | Insert surface | M1.2 |
| `OutlineTree` | Layers surface | M1.2 |
| `CanvasView` (live WYSIWYG) | canvas — always-mounted home | M1.2 |
| `Inspector` / `ChromeInspectorPanel` | RightDock | M1.2 |
| `VisibilitySection` | RightDock | M1.2 |
| `PageInspectorPanel` | RightDock | M1.2 |
| `PerspectivesPane` (+ preview) | RightDock | M1.2 (top-bar preview = deferred surfacing) |
| `FiltersDrawer` | RightDock | M1.2 |
| `PageWorkflowBar` (draft→publish/history) | TopBar | M1.2 |
| ⌘K `CommandPalette` | TopBar omnibar | M1.2 |
| metric bind (both gestures) | canvas + Data surface | M1.2 |
| "← Site" gate | — | by-design-absent |

---

## Net result
**Every wizard capability is reachable in the Studio.** The only wizard elements absent from the Studio are the three waterfall gate buttons (Continue/back), whose absence is the entire point of the milestone (spec §2). **No capability required deleting or altering the wizard** to relocate — extraction kept the wizard byte-identical. The M1.3b wizard-deletion boundary was **not** hit.

## Deferred (downstream, no capability lost now)
- Top-bar **locale / theme / perspective preview** surfacing — reserved TopBar regions (`StudioTopBar` comment); locale is fully settable via Pages&Site identity today.
- **Writable** theme/brand-token editor (StyleSurface) — **M1.4**.
- **Model mode** (Steward role — the raw editors move behind the role) — **M2**; until then they stay reachable under Data → Advanced.

## Executable evidence (per relocated surface)
- `features/data-layer/DataModelingPanel.test.tsx` — mounts, reads store, reveals the real `DataSpecEditor`, **writes** via `updateDataSpec`.
- `studio/surfaces/DataSurface.test.tsx` — metric-palette-first; Advanced collapsed by default; discloses the full panel on demand (no capability lost).
- `studio/surfaces/PagesSiteSurface.test.tsx` — mounts identity+nav; **writes** via `updateSite`/`removeNavItem`; "+ add page" opens the real create dialog.

## For M1.3b (owner sign-off gate)
When authorized: delete `features/wizard/{ConstructorWizard,WizardStepper}.tsx` + `steps/*` + `WIZARD_STEPS`, remove the `activeStep`/`completedSteps`/`goToStep`/`markStepDone` slice, point `App.tsx` at the Studio unconditionally (flip `STUDIO_SHELL` default). The shared `features/site/*` + `features/data-layer/DataModelingPanel.tsx` survive the deletion (they are the wizard-independent homes). A machine-checkable `FF-WIZARD-CAPABILITY-PARITY` can then lock this table.
