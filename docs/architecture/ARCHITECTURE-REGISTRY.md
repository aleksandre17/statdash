# Architecture & Vision Registry — statdash-platform

> **SSOT for every high-concept architecture/vision we commit to. Nothing here is lost.**
> The lead MAINTAINS this: every architectural vision the owner raises gets a card HERE, with its design doc + status + next action, **before we move on to anything else.** Depth lives in the linked `work/DESIGN-*.md` / ADRs / agent memories — this is the index that guarantees nothing falls through.
>
> **Status vocabulary:** `VISION` (raised, not yet designed) · `DESIGNED` (doc + phased plan exist) · `BUILDING` · `BUILT` (green, unit-verified) · `VERIFIED` (server real-browser / real-DB proven) · `DEFERRED` (owner decision) · `SUPERSEDED`.
> Reviewed at every Leader's Scan (see .claude/agents/orchestrator.md §Second Mandate) against `work/BENCHMARK-REFERENCE-PLATFORMS.md` (capability-comparison SSOT).
> _Last curated: 2026-07-01 (lead)._

---

## A. Responsive · Composition · Style (the current thrust — owner's #1)

> **Governing standard (owner, 2026-07-01): MAXIMUM, not minimum.** The layout system harnesses the FULL power of CSS Grid + Flexbox, exposed as a **JSON grammar of layout**, interpreted by the JS renderer, container-query-driven, per-breakpoint — at the grade of the strongest frameworks/platforms (Builder.io/Plasmic/Framer/Vega/Grid+Flex), with their patterns (registry/OCP, interpreter, responsive-value, composite, design-tokens). Maximum **agnosticism · dynamics · functionality · architecture · concept**, while staying compatible with the existing configs + services (config-is-data, rides NodeStyles/ResponsiveValue/DataStore seams). **The ARCHITECTURE is always maximal + framework-grade; capabilities are activated progressively per real need (YAGNI on population, never on the seams).**

| ID | Architecture / Vision | Concept (1-line) | Design | Status | Next |
|----|----------------------|------------------|--------|--------|------|
| **AR-1** | **Grammar of composition** | structure (layout nodes) + style (NodeStyles) = two axes of one node, resolved by `resolveStyle`+`@layer` cascade; kills the specificity fight | `DESIGN-responsive-composition.md` | DESIGNED · foundation BUILDING (a38b) | build P0 @layer + P1 resolveStyle |
| **AR-2** | **Layout-node adoption / section-uniformity** | sections/pages compose via `nodes/layout` (one primitive), retire bespoke divs, kill duplicate `row` grid, wire `align` | ↑ | **VERIFIED** (server `:3008` — uniform composition, equal-height, gdp nav populated, map definite height) | — |
| **AR-3** | **Declarative FILL contract** | inner element fills parent body height via config (`fill`→flex `1 1 0`+`min-height:0`+`height:100%`+chart `height:'100%'`); composable with inner-overrides-outer | (folding into AR-1/2) | **BUILT (`84999e2`)** — leaf growable-band + Apex `parentHeightOffset:0` + unbroken chain to Apex mount; fitness-locked (frozen-176 can't regress) | server re-verify chart-fill |
| **AR-4** | **Framework-grade universal style system** | `NodeStyles` = Chakra/Theme-UI `sx` responsive style-prop; +token-constrained (DTCG) +named PARTS (`data-part`, style any inner tag) +Constructor `StyleField` | `DESIGN-framework-grade-style-system.md` | DESIGNED | build S0+S1 (token-picker StyleField + surface view.styles) |
| **AR-5** | **Maximal JSON grammar of layout** | FULL CSS Grid power in JSON — `grid` node exposes `templateColumns/Rows/Areas` (`minmax`/`auto-fit`/`repeat`/`fr`/`min()`/named lines), `autoFlow/autoColumns/autoRows`, `columns` shorthand, `gap`, `align`/`justify`; template props are `ResponsiveVal`, container-query-driven via the shared var+flag cascade (`resolveGrid` + `layout.css` `@container`); per-child `colSpan/rowSpan/order` rides `LayoutItemProvider`. Schema Constructor-introspectable. First slice: 8 gdp+regional section groups migrated `columns`→`grid` (pairs reflow intrinsically via `repeat(auto-fit,minmax(min(100%,24rem),1fr))`). | `DESIGN-responsive-composition.md` §3 | **BUILT** (green: build:engine·typecheck·tsc panel·lint·check-laws·vitest 31 FF + 104 pkg; `resolveGrid`+`GridNode`/`GridShell`/`layout.css` + FF-GRID-MAXIMAL + FF-GRID-COMPOSITION) | server re-verify gdp+regional 360→3440 (reflow 2↔1); then breakpoint-tabbed StyleField authoring (AR-11) + populate templateAreas per real consumer |
| **AR-6** | **3-tier style-override cascade** | plugin-default responsive < config `styles` < inner-overrides-outer; ONE mechanism every node, `@layer` precedence (merge, not specificity fight) | `DESIGN-responsive-composition.md` §A | DESIGNED (part of AR-1) | rides AR-1 build |
| **AR-7** | **Named PARTS styling** | promote internal `styleKeys` → public `parts` manifest, emit `data-part`, `view.parts.<p>: NodeStyles` via same `applyNodeStyles` | `DESIGN-framework-grade-style-system.md` (S2) | DESIGNED | build mechanism; populate per real consumer |
| **AR-8** | **Panel sizing → CONTEXT-PROPORTIONAL height** | base (height on content box, definite map/chart height) VERIFIED. **ELEVATION (owner 2026-07-01):** a single `clamp(floor,fluid,cap)` is UNIFORM, not proportional — a SOLO panel must be TALLER than a PAIRED one. **DESIGNED (2026-07-01):** verdict = KEEP the honest band + the retired-`aspect-ratio` decision (fence honored), REFINE its frozen `64cqi` into a token band `clamp(floor, calc(var(--panel-ratio)*100cqi), cap)` where `--panel-ratio` is role-declared (map 0.72 / chart 0.52 / kpi opt-out) + context-responsive (`@container` breakpoints: wide/solo→shorter ratio→taller absolute; narrow/paired→taller ratio) + authorable (revive the retired `--ar-*` emission → `--panel-ratio`, Constructor-ready). Solo taller than paired BY CONSTRUCTION; composes with AR-5 width/chart-fill/equal-height/map-definite-height, no regression. 5-step Strangler build spec + 7 FFs. | `DESIGN-proportional-sizing.md` | **BUILT (2026-07-01)** — band middle term = `calc(var(--panel-ratio)*100cqi)`, `--panel-ratio = authored ?? role, × context-scale` (3 orthogonal INPUT vars → compose by fallback+multiply, NO same-property specificity fight); `@container 680/1040` shrink `--panel-ratio-scale` (solo≠paired); role via `data-content` (geograph.css `geo`=0.72 / chart.css `chart`=0.52, plugin-owned); resolver emits `--panel-ratio-<bp>` from `aspectRatio` (CSS W/H **inverted** to band H/W, Postel slash/colon/number) → node-styles cascade → `--panel-ratio-authored`; frozen `--size-panel-h-fluid`/`64cqi` deleted, bounds retuned to GUARD-only 320/640. Green: build:engine·typecheck·tsc panel·lint·check-laws·vitest 2136 + 7 new FFs (RATIO-DRIVEN-BAND/BAND-MONOTONIC/RATIO-CONTEXT-AWARE/RATIO-AGNOSTIC/MAP-DEFINITE-HEIGHT/EQUAL-HEIGHT-SIBLINGS/BAND-IS-FLEX-BASIS). map/equal-height/chart-fill(`84999e2`)/AR-5-grid NOT regressed. | REAL-BROWSER verify (batch redeploy): `/ka/gdp` `/ka/accounts` 630→1440→2560 — solo map taller than paired + near-square (no letterbox); paired cards equal height |

## B. Constructor · Authoring SSOT

| ID | Architecture / Vision | Concept | Design | Status | Next |
|----|----------------------|---------|--------|--------|------|
| **AR-10** | **Authoring ← engine schema SSOT** | Constructor consumes `describeApp()`/PropSchema, never forks; kills the DRY (showWhen/dot-path forks) | `DESIGN-authoring-schema-ssot.md` | P1 BUILT (`c1a635e`); P2/P3/P4 DESIGNED | build P2 saveGuard-describes, P3 chart fieldConfig, P4 dataLinks |
| **AR-11** | **Style authoring in Constructor** | `PropFieldType:'style'` StyleField (token-picker × responsive-tabs × part-selector) on the declared `enum-ref source:'tokens'` seam | `DESIGN-framework-grade-style-system.md` | DESIGNED | build with AR-4 |
| **AR-49** | **The Governed Canvas — authoring reconception (vision/umbrella)** | Reconceive the whole authoring paradigm: retire the disliked 3-step wizard (Data→Site→Pages, an artifact-ordered waterfall) for a **metric-first document-canvas** — *content-first, bind-progressively*. Core move: **define-vs-curate as a ROLE, not a step** — modeling (query/pivot/cube) relocates to a steward's **Model** mode; the author **Composes** by picking governed nouns (metric × dimension × perspective) from the **semantic layer**, never a query. The unclaimed market quadrant = *statistics-grade AND non-programmer-authorable* (LookML/Malloy power × Builder.io/Notion simplicity × ONS/Eurostat integrity). The semantic layer (AR-40) IS the simplicity engine (complexity is a property of a role, not a task). One additive PropSchema type (`metric-ref`/`dimension-ref`) is the only new vocabulary needed; arrow unchanged. Refuses Cube/Malloy-the-runtime (SQL-assuming, breaks Law 5), grows AR-40 instead. Umbrella that SEQUENCES AR-40/10/11/4/46/47/48; enabled-later by AR-31/36/41/43/44/28. | `SPEC-authoring-reconception-vision.md` (vision) · **`SPEC-authoring-reconception-M0.md` (M0 detailed design)** · **`SPEC-authoring-reconception-M1.md` (M1 detailed design)** | **VISION APPROVED** (owner) · **M1 BUILT (2026-07-09, owner-authorized)** — the "Studio" shell: one canvas always-home + Webflow-style activity-rail of summonable surfaces (Insert·Data·Layers·Pages&Site·Style·Model🔒) + Framer-style top bar + selection-contextual right dock; NO waterfall/gating. Shell-specific competitor study (Builder.io canvas-home · Webflow activity-rail · Power BI role-rail · Superset counter-example), BEYOND = governed metric palette as the data surface + role-as-lens-on-one-canvas. react-admin RETIRED (dead `<Resource>` fork + `AdminContext` + dataProvider/i18nProvider deleted; one live `useNotify` → our `notify` port on MUI Snackbar). Adds nothing but the notify port (value = subtraction + reorg); MUI→Radix exit flagged NOT ridden. 4 sub-Ms (M1.1 ra-retire ⭐first · M1.2 shell scaffold · M1.3 re-home+delete wizard · M1.4 Strata skin+writable theme), 16 build items, 7 FFs, capability-parity inventory (nothing lost), arrow untouched (apps-only). · **M0 BUILT (2026-07-09)** — converged gate green (core 714 / react 501 / geostat-render 127 / full 2675, 0 fail); chief-engineer verdict SHIP-WITH-FOLLOWUPS; FF-BIND-PARITY proves bind-via-metric == hand-authored byte-identity, and KPI read now honors metric default-dims via a shared `mergeMetricDims` (chart≡KPI, "one governed number on every surface"). Design: market study, differentiation thesis, paradigm, simplicity engine, library eval (retire react-admin · keep-our-canvas+steal-Puck · grow AR-40), target arch on the unchanged arrow, Strangler path, 6 milestones, 5 FFs, 4 rejected paradigms, brand "Strata" | **build M0**: governed metric/dimension catalog (`DimensionDef` peer of `MetricDef`) + `metric-ref`/`dimension-ref` as `enum-ref source:'metrics'/'dimensions'` (additive, no new PropFieldType) + Metric Palette bind affordance; lowers through the UNCHANGED `resolveMeasureRef`→DataStore seam (FF-BIND-PARITY proves byte-identity); 11 ordered build items (§8), critical path contracts→core→describeApp→panel catalog→palette. **M0 DONE.** Follow-ups: (a) KPI inline per-item metric-ref — needs a core `itemSchema` PropField widen + Inspector array-item editor (interim: Metric Palette bind); (b) `dimension-ref` runtime lowering seam (`resolveDimensionRef`) before any dimension-ref picker ships (vocabulary laid, no lowering yet); (c) retire panel-local `CatalogDimension` mirror → import engine `DimensionDef` (item-5 shipped it); (d) section-fed chart governed picker (M1). **M1 now DESIGNED** (`SPEC-authoring-reconception-M1.md`). **build M1**: M1.1 retire react-admin (notify port + delete dead fork/AdminContext/providers + drop dep; `FF-NO-REACT-ADMIN`) → M1.2 `Studio.tsx` shell (activity-rail + top bar + docks; surface slice replaces wizard slice; `FF-CANVAS-ALWAYS-HOME`/`FF-NO-WIZARD-GATING`; behind `STUDIO_SHELL` flag) → M1.3 re-home DataStep/SiteStep/PageStep into surfaces, wire real "+add page", delete wizard scaffolding (`FF-WIZARD-CAPABILITY-PARITY`) → M1.4 Strata token skin on chrome + writable theme editor (`themeOverrides` via StyleField; `FF-CHROME-TOKEN-DRIVEN`/`FF-THEME-EDIT-DATA`). **M1 BUILT (all sub-milestones, 2026-07-09)**: M1.1 react-admin retired (FF-NO-REACT-ADMIN bites) · M1.2 Studio shell behind flag · M1.3a capability parity (extract-and-delegate, no fork) + M1.3b wizard DELETED, Studio default, flag removed · M1.4 writable theme editor + Strata skin — surfaced+closed that `themeOverrides` was DEAD DATA (saved, never applied to DOM); now `buildThemeVars` applies it live (FF-CHROME-TOKEN-DRIVEN + FF-THEME-EDIT-DATA). Hardening: `setupCanvasRegistry`→explicit idempotent boot step (fixes latent empty/new-site bug) + `bootComposition` end-to-end test (closes the M0-class seam — real App→bootstrapCatalog→StudioShell→populated palette; verified RED if await dropped) + FF-NOTIFY-PORT + StudioShell theme-DOM leg; FF-WIZARD-CAPABILITY-PARITY formally retired (parity = per-surface guards). chief-engineer M1 verdict SHIP-WITH-FOLLOWUPS; gate green (panel 455 / core 714 / geostat-render 127; 0 fail). Committed on `feat/ar49-m0-metric-first-authoring` (7 commits, NOT pushed — owner call). **Deploy-time live-verification pending** (no Docker locally): /api/bootstrap→real palette, full live boot ordering, initFromApi sources/specs, live heavy-canvas render + theme repaint, ⌘K chunk, auth, +add-page create. Open follow-ups: KPI `itemSchema` inline picker; `dimension-ref` lowering seam; retire `CatalogDimension` mirror; StudioTopBar perspective-preview region. Owner calls: confirm "Strata" wordmark + hex palette (retunable live in the editor). Then **M2**: Model mode + Steward role gate |
| **AR-12** | **RX-16 one-map-node** | fold choropleth into `geograph`, delete `panels/map` stub, `capabilityGate` gates on `geo` cap (not type-sniff) | `DESIGN-map-consolidation.md` | DESIGNED | build (post section-uniformity) |
| **AR-13** | **Theme-switcher as a config-rendered chrome node** | owner 2026-07-01: a dark theme exists but there is NO switcher UI, and theme-switching is NOT a first-class config node (locale IS — `LocaleSwitcherShell`). Build a `theme-switcher` chrome node mirroring the locale-switcher: config-driven, agnostic, toggles `[data-theme]` dark/light, persisted. Maximal-adoption (every chrome capability is a node) | (mirror locale-switcher) | **BUILT (2026-07-01)** — `chrome/theme-switcher/default/` {`ThemeSwitcherShell` + `useTheme` persist-hook + `theme-switcher.css` + `index.ts` META `slot:'ThemeSwitcher'`, `defaultRegion:'inline'`}, registered via `chrome/index.ts` barrel (→ `Object.values(Chrome)` → `registerSlice`), embedded beside `LocaleSwitcher` in `AppHeaderShell` (`<ChromeSlot slot="ThemeSwitcher"/>`). `useTheme` = localStorage (`statdash-theme`) + `prefers-color-scheme` default, projects `[data-theme=light\|dark]` on `<html>` (rides e74414d token layer; zero authored colour); OCP-open to `system` (id ⇒ remove-attr ⇒ @media wins). Config node parity: inline-defaulted like locale (no provisioning entry needed; overridable via `site.chrome.ThemeSwitcher`). Green: build:engine·typecheck·tsc panel·lint·check-laws·vitest | REAL-BROWSER verify (batch redeploy): toggle flips dark + persists across reload; first visit honours OS scheme |
| **AR-14** | **Chart runtime participates in the theme (F5 + label-collision F10/F13)** | AUDIT §2-D: ApexCharts draws axis/grid/label/legend/tooltip chrome to SVG in JS — OUTSIDE CSS `var()` reach — so chrome stayed dim/dark on the dark surface even with the token layer complete (`e74414d`). Chart chrome must (1) READ its colours from the CSS tokens (agnostic, flips with `[data-theme]`), and (2) RE-RENDER when the theme flips at runtime (a baked SVG can't re-drive via CSS). Plus responsive label rotation/hide/trim so bar labels never overlap or clip. | (rides the AR-13 switcher + `e74414d` tokens) | **BUILT (2026-07-01)** — `apex/base.ts`: `chart.foreColor` getter = `cssVar('--color-text-secondary')` (Apex's single fallback ink; unset it defaulted to `#373d3f`→dim), `tooltip` getter = `{theme: isDarkTheme()?'dark':'light'}` where `isDarkTheme()` = luminance of the RESOLVED `--color-surface` token (agnostic — no theme-name/hue literal; media-query fallback for SSR/jsdom). Grid/axis/label/legend colours already routed through `cssVar` (resolve live at build). RE-RENDER: `utils/useThemeVersion.ts` = module-singleton `MutationObserver([data-theme])` + `matchMedia(prefers-color-scheme)` → `useSyncExternalStore` version, folded into a `key` on `Chart.tsx`'s `.chart-wrap__render` wrapper → remounts EVERY renderer (Apex builders + custom-SVG donut/treemap/hbar) so all `cssVar` reads re-run on the new cascade. LABELS: `contribution` x-axis `hideOverlappingLabels+trim` +responsive rotate (-35/-50 at BP_SM/XS); `cartesian` horizontal end-labels get generous right grid-pad (44/40/34 when dataLabels on) so the longest bar's outside label ("42 620.8") stops shearing. Fitness `apex/theme-chrome.fitness.test.ts` (both-modes: foreColor/grid/y-axis/x-axis/tooltip all differ light vs dark; foreColor never unset). Green: build:engine·typecheck·tsc panel·lint·check-laws·vitest (2145). donut-palette (`f8d8204`)/chart-fill (`84999e2`)/AR-8 NOT regressed. | REAL-BROWSER verify (batch redeploy): gdp+accounts+regional light↔dark @390/768/1440 — axis/label/grid/legend/tooltip readable in DARK, expenditure x-labels no overlap, regional hbar end-labels not clipped |

| **AR-15** | **Table overflow scrolls + beautiful themed scrollbars (owner)** | owner 2026-07-01 on the live dashboard: (1) tables wider/taller than their card were CLIPPED with NO scrollbar → data inaccessible; (2) wants BEAUTIFUL styled scrollbars (thin, rounded, themed) everywhere, not default OS chrome. ROOT of (1): `.data-table__wrap` is a flex item in the AR-8 band's `[data-view=visible]{flex-direction:column}` chain, whose default `min-width:auto` refuses to shrink below the table's min-content width, so a wide table pushed past the card edge instead of scrolling. | (this card) | **BUILT (2026-07-01)** — (1) `packages/plugins/panels/table/**/data-table.css` `.data-table__wrap` → `overflow:auto; min-width:0; max-width:100%` (root-cause: min-width:0 engages overflow-x inside the flex band; doesn't fight node-styles' band/mobile overflow-y layers or the sticky header/sr-table). (2) NEW `packages/styles/src/css/scrollbar.css` (imported into `css/index.css`) = shared `.scroll-fancy` utility + page-root `html` scrollbar, covering BOTH engines (WebKit `::-webkit-scrollbar*` pill w/ inset transparent-border + `scrollbar-width:thin`/`scrollbar-color` for Firefox, which inherits app-wide from `html`). Fully TOKEN-DERIVED (thumb=`--color-border-strong`, hover=`--color-border-interactive`, track=frame-border mix) → auto light+dark+tenant, zero hex, NO token added to tokens.css (AR-8 lane untouched). Thumb always-visible (WCAG discoverability). Applied via `scroll-fancy` class: table wrap (Simple/PivotTable), panel canvas-root (`CanvasView`), command list (`CommandPalette`); inner-sidebar converged in-place onto the same `--scrollbar-*` vars (kept its own selector so the mobile scrollbar-hide still wins). Fitness `packages/styles/src/scrollbar.fitness.test.ts` (overflow:auto + min-width:0 present, both-engine coverage, no-hex, import wired). Green: build:engine·typecheck·tsc panel·lint·check-laws·vitest. AR-8 band / chart-fill / equal-height / sticky-header / sr-table NOT regressed. | REAL-BROWSER verify (batch redeploy): a WIDE table (regional cross-classified) + a TALL table scroll inside the card with the pill scrollbar in BOTH light+dark; page/sidebar/canvas/cmdk scrollbars consistent |

## C. Data · SDMX · Platform

| ID | Architecture / Vision | Concept | Design | Status | Next |
|----|----------------------|---------|--------|--------|------|
| **AR-20** | **AgencyScheme identity keystone** | `stats.agency_scheme`+`agency` SSOT, FK-indirection over free-text `agency` (DB-08); keeps MT door open w/o building MT | ADR `ADR-multi-tenancy.md` §3 | BUILT + DB-VERIFIED (V38 `70cf6b7`) | apply to prod after AR-21 |
| **AR-21** | **V33 fresh-DB ordering fix** | migrate→ingest→migrate boot contract so clean-room `compose up` is deterministic (V33 corrective needs ingest data) | (escalated to architect) | VISION (defect found on server) | architect design the boot-order contract |
| **AR-22** | **TM orthogonal axis (DimBinding)** | time-mode = one perspective axis; `DimBinding`/`Selection` discriminant; fused literal killed | (built) | BUILT (`be95880`); P-final DESIGNED | P-final (delete `timeBinding`, panel migrate) |
| **AR-23** | **Grain rollup (OLAP)** | `granularity`→`point-series.grain`; cross-grain blend + rollup-router | CLOSE-BOARD | G4 BUILT (`a195a79`, data-gated); G5/G6 VISION | build G5/G6 when sub-annual data exists |
| **AR-24** | **Fail-soft chrome** | empty-manifest → brand-free fallback + app-root error boundary (ADR-0028 restored) | (built) | BUILT (`ff748d8`); VERIFIED-in-wild (429 graceful) | visual polish on server |
| **AR-25** | **Choropleth + donut color** | value→color scale (quantile sequential ramp, token-derived, agnostic); donut categorical palette | (built) | BUILT (`f8d8204`) | server re-verify |
| **AR-26** | **i18n content-contract (no monolingual leak)** | ROOT of audit F3 (Georgian bled onto every EN page): user-facing content fields were typed bare `string`, so bilingual content was STRUCTURALLY IMPOSSIBLE and provisioning authored them ka-only. Fix = WIDEN each such field `string→LocaleString`, RESOLVE at the existing render seam (no new mechanism, Law 4 — engine/charts stay locale-agnostic), AUTHOR bilingual in provisioning. Widened: **page-header badge** perspective-carrier arms (`PerspectiveCarrier = Record<perspectiveId, LocaleString>` — perspective×locale orthogonal, collapsed in `template.ts resolveCarrier`); **KpiTrendSpec static `value`** (resolved in `kpi.ts resolveTrend`); **geograph `title`/`label`/`unit`/`labelOverrides`** (resolved in `GeographShell`). Chart series `name` is data-derived (`row.series`), so the ROOT was the untagged transform-inject seam: `steps.ts tagCell` now brands authored bilingual `lookup`/`group`/`addField` literals so `resolveRowLocales` collapses them — series/total-label (`მშპ`→GDP, `სულ`→Total via lookup, retiring the monolingual `expr` ternary). ChartSeries.name stays `string` (post-resolution neutral output — widening it would re-leak `[object Object]` into ChartOutput). Fitness: `config-no-locale-leak.fitness.test.ts` — structural scan, no tenant-script codepoint outside a LocaleString locale arm (leak can't recur). | (this card) | **BUILT (2026-07-01)** — green: build:engine·typecheck·tsc panel·lint·check-laws·vitest 2138 + new leak-gate; provisioning 0 leaks (was ~20). Complements `config-label-completeness` (key-driven bags) with a content-driven scan the key gate structurally couldn't see. | REAL-BROWSER verify (batch redeploy): EN `/en/accounts` `/en/gdp` `/en/regional` — badge "Updated: 2025", chart legends "Resources/Uses", GDP real-growth trend "Real", expenditure total "GDP", Tbilisi-share "Tbilisi / Georgia", occupied-territory tooltip English |
| **AR-27** | **Table header-wrap + content-driven column sizing (owner)** | owner 2026-07-01, deeper defect BEYOND the AR-15 scroll fix (`57fc5dd`): long column HEADERS/titles did NOT wrap → forced an ultra-wide column that SQUEEZED the rest with no room. ROOT: `.data-table th { white-space:nowrap }` pinned a long header's column at its full single-line min-content, so under `table-layout:auto` that one column hogged the row and the others collapsed to their floor. | (this card) | **BUILT (2026-07-01)** — `packages/plugins/panels/table/**/data-table.css`: `.data-table th` → `white-space:normal; overflow-wrap:anywhere; max-width:var(--data-table-th-max,22rem)` (headers WRAP; `anywhere` lowers min-content so `auto` can shrink the column; token-capped so no header hogs even with slack) + `.t-num` → `white-space:nowrap` (figures never wrap mid-digit → numeric columns keep their legible min-content) + explicit `table-layout:auto` (content-driven, NOT fixed — right for stat tables). Composes with AR-15: the table WRAPS to fit the card and SCROLLS (`.data-table__wrap overflow:auto`) only when genuinely too wide even after wrapping; pivot worst case (many series × columns) degrades to wrap-then-scroll, never squeeze. Applies to both SimpleTable + PivotTable (shared sheet), incl. pivot group/sub headers + row-header labels. Fitness `data-table.layout.fitness.test.ts` (headers wrappable not nowrap · overflow-wrap:anywhere · max-width cap · .t-num nowrap · table-layout:auto · wrap overflow:auto not regressed). Green: build:engine·typecheck·tsc panel·lint(0 err)·check-laws·vitest 417 plugins+styles. Sticky header / sr-only a11y table / AR-8 band height / dark theme / scroll-fancy NOT regressed (node-styles.css·tokens.css·scrollbar.css untouched). | REAL-BROWSER verify (batch redeploy): a WIDE regional cross-classified PIVOT (multi-column series, worst case) + a SimpleTable with a long header — headers wrap to 2–3 lines and every column gets room; scroll only when still too wide; both light+dark |

| **AR-28** | **Rendering north-star (CSR now · SSG/ISR north-star · full SSR = YAGNI)** | owner 2026-07-01, triggered by soft-nav vs hard-refresh differences ("is hard-refresh the fix? are we building an SSR platform?"). VERDICT: the soft-nav issues are BUGS not CSR-inherent (scroll already fixed client-side by `RouteScrollManager`; chart crispness = Apex/Leaflet resize-on-mount bug — fix client-side via route-keyed remount, the AR-14 `useThemeVersion` seam) → SSR is NOT the remedy, do not buy SSR to fix a client-state bug. The genuine prize for a PUBLIC national-stats platform (SEO/crawlability HIGH·mission, no-JS/low-bandwidth MOD-HIGH, first-paint MOD, correctness LOW) is delivered by publish-time PRE-RENDER (SSG/ISR), NOT per-request SSR — and SSG KEEPS the static-nginx runner north-star (ADR-0026/0028) intact (pre-rendered HTML = static assets; charts/map hydrate client-side regardless — Apex/Leaflet client-only; the OWID/Datawrapper model). Full per-request SSR (b)/hybrid Node tier (d) breaks the static runner + adds a live Node tier whose only edge (per-request freshness) is unused by published stats content (governed by the existing publish FSM `page_version.is_published` → maps to ISR-on-publish). Enabled by `render(config)` purity (Law 2). | `DESIGN-rendering-architecture.md` | **DESIGNED** (assessment + phased path + 4 FFs + 5 rejected alts) | NOW: fix chart soft-nav bug as a bug (route-keyed remount) + land seam-hardening FFs (`FF-RENDERER-ISOMORPHIC` — highest-value: `renderToString` works in Node/jsdom, no window at module-load; `FF-RENDER-DETERMINISTIC`). NORTH-STAR (build on real SEO/discoverability trigger): R1 SEO scaffolding (meta/OG + schema.org `Dataset` JSON-LD + sitemap, ships on current CSR) → R2 SSG pre-render canonical published pages (new `entry-server.tsx`, same pure engine, static HTML on same nginx, charts hydrate) → R3 ISR publish-hook revalidation. R4 full-SSR only if a real per-request trigger (host-MT-fresh/personalization) appears — YAGNI today (MT itself deferred, AR-30) |

## C-int. Grammar of Interaction (cross-filter + runtime re-encode)

| ID | Architecture / Vision | Concept | Design | Status | Next |
|----|----------------------|---------|--------|--------|------|
| **AR-36** | **Runtime PIVOT / re-encode as a declarative capability (owner 2026-07-02)** | owner: the chart's encoding — `x=sector, series=geo (selected regions stacked)` ⇄ `x=geo, series=sector` — should be **runtime-swappable via events/filters**, NOT hardcoded in config as two `visibleWhen` A/B panels. This is the OLAP **pivot/rotate** operation elevated to a first-class declarative capability: encoding channels (`x`/`series`/`stack`/`color`) bind to STATE (param/selection/event), not to frozen literals, and a gesture rotates them. Directly extends the events grammar the owner already validated (`on[]`→emit→single CommandBus write point) — today filter/navigate; tomorrow **re-encode/pivot**. Owner flagged (rightly) that the LEAD should have proposed this (proactive-innovation mandate). Reframes the regional composition panel: ONE pivot-panel whose encoding reads from `_regionSel`/selection, replacing the two hardcoded `sectors`/`sectors-multi` panels. Converge the maximal target (this) BEFORE building the State-A/B swap → avoids the hardcode-then-rework loop. | `DESIGN-grammar-of-interaction.md` | **DESIGNED** (2026-07-02) — grammar = the encoding.ts golden rule ("data never pivoted; encoding says HOW") wired to STATE via the existing cross-filter spine. ONE new seam: `EncodingChannel` Postel-widened `string\|ChannelDef\|CtxRef`, `{$ctx:"_xDim"}` resolved in `interpretSpec` before `applyEncoding` (bare strings byte-identical). REUSE: `vars` derive (`op:if`) computes `_xDim/_seriesDim` from selection (same mechanism as `_regionSel`) → data aggregates by BOTH dims once, rotation = pure channel re-read (no re-query/transpose); directional select = existing `FilterAction` (zero new wiring); manual rotate = new `PivotAction` union arm folded through the SAME CommandBus point. Layer map respects arrow (pure resolve in core, action in react, config in apps; charts untouched). FOLD verdict: ONE panel — P2 folds the two bar orientations (proven same `DataRow[]`), P3 binds `chartType:{$ctx:_mark}` to fold the State-A donut too; both old panels deleted post-verify (Strangler). 6 FFs (ENCODING-POSTEL/PIVOT-AGNOSTIC/DECLARATIVE/ONE-WRITE-POINT/PERMALINK/ROUNDTRIP); 5 rejected alts. **P0–P3 now IN CODE** (`resolveEncodingRefs`/`resolvePipeRefs` live; composition folded to ONE `sectors` panel with `label/series:{$ctx:_xDim/_seriesDim}`, `by:{$ctx:_byDims}`, `chartType:{$ctx:_mark}`) — the region arm ships; derives key on `region` only. | **owner sign-off to build P0** (widen `EncodingChannel` + `resolveEncodingRefs` pre-pass — additive, byte-identical). Then P1 derive → P2 fold orientations → P3 fold donut → P4 explicit rotate verb → P5 Constructor surface (AR-10). SEQUENCING: land the in-flight State-A KPI double-count fix (same provisioning file) BEFORE P2/P3 (disjoint node subtrees but same JSON → rebase P2 onto it); P0/P1/P4/P5 are `packages/*`, zero collision. **Sector arm of the same panel → AR-38.** |
| **AR-38** | **Directional SECTOR arm of the regional cross-filter (owner 2026-07-03)** | second half of `SPEC-regional-crossfilter-behavior.md` (region arm ships/live). Owner: *"when a SECTOR is also selected — along with the individual regions — the selected sector should show."* select sector → pin `sector`, DISPLAY `geo` (that sector across regions); define the compound (region ∧ sector) semantics. Completes AR-36 §2.2's already-sketched two-param derive — the six live derives (`_xDim/_seriesDim/_mark/_byDims/_sortBy/_sortDir`) key on `region` ONLY, blind to `sector`; a selected sector narrows some panels but never REORIENTS the composition. | `DESIGN-directional-sector-crossfilter.md` | **DESIGNED** (2026-07-03) — **NO engine change** (AR-36 P0–P3 seams already in code). Chosen semantics = **sector-priority** compound tiebreaker (sector active → `series=sectorLabel`, `x=geoLabel`, regardless of region) — grounded in SPEC line 23 ("select sector → DISPLAY geo"), owner intent, and permalink-determinism (rejects region-priority AR-36-sketch for the compound case + rejects recency state). Compound = INTERSECTION (that sector, in those regions), not replace. Deltas, all provisioning: (1) extend the 6 derives to a two-param truth table (nested `op:if` w/ `nin`/`or`); (2) **harmonize the `sector` unselected sentinel `"_T"→""`** (peer with `region`, kills a latent Law-1 asymmetry) so ONE store-agnostic clause `sector:{"$ne":"_T","$ctx":"sector"}` narrows on BOTH ExternalStore + ApiStore-wire (+4 companion `default:"_T"` edits; ~15 KPI clauses already safe). KPIs+comparison already sector-aware → free. 4-state matrix (A none/B region/C sector/D compound) is the acceptance spine; B⇄C are the AR-36 pivot mirror. Selection surfaces already exist (filter-bar `select` + range `point:click`) → P1 needs no new interaction. Opt P3 = click-a-sector-bar via state-bound `FilterAction.key/fromField` (mirrors `resolveEncodingRefs`, YAGNI-gated); opt P4 = multi-select sector (nearly free). 4 new FFs (DIRECTIONAL-TRUTH-TABLE/SECTOR-COMPOUND-FILTER/DIM-SENTINEL-SYMMETRY/SECTOR-DERIVE-AGNOSTIC) + reuse XF-ONE-WRITE-POINT/PIVOT-PERMALINK; 6 rejected alts. | **owner confirm** the sector-priority tiebreaker + the sentinel `_T→""` harmonization, then route **P0 sentinel+companions → P1 compound clause → P2 two-param derives** (all provisioning-only, verify each on BOTH stores in real browser) → opt P3 click-to-pin (react) → opt P4 multi-select. COLLISION: `feat/chart-lowcardinality-render` edits the same `sectors` node's chart child (colors/bar-width) — disjoint sub-objects but same JSON → land that branch FIRST then rebase P0–P2; re-verify its low-cardinality render against the NEW `x=geo`/1-series orientation |

## E. i18n — full language synchronization

| ID | Architecture / Vision | Concept | Design | Status | Next |
|----|----------------------|---------|--------|--------|------|
| **AR-37** | **Full i18n sync — every layer switches language, seamlessly (owner 2026-07-02)** | switching language must flip EVERY plugin/chart/table/config/KPI/chrome string + pagination + DATA values synchronously, no reload, no mixed-locale. **ROOT (from code study, not the PLAN's hypothesis): the locale SSOT is NOT fragmented** — `useLocale()` already = URL `LocaleContext ?? manifest default`, one reactive source. Three concrete roots: **R1** `<html lang>`/`dir` never bound (nothing writes `documentElement.lang`; frozen at `index.html` en); **R2** coverage/authoring holes — some fields not routed through the resolve seam + some authored MONOLINGUALLY in provisioning (a plain string ∈ LocaleString is locale-frozen → titles/KPI-labels stay EN on /ka); **R3** i18next global `language` stuck `en` (`changeLanguage` never called, `useT` masks per-call). Fix = COMPLETE the resolve-at-boundary seam (not a new mechanism, KISS/Chesterton) + bind the document + authoring-completeness gate. Reactive-first re-render; `useLocaleVersion` remount ONLY for the imperative Leaflet map (charts already recompute via memo dep). | `DESIGN-i18n-full-sync-and-integrity-badges.md` (+ sibling AR-39 doc) — absorbs `PLAN-i18n-full-sync.md` | **DESIGNED (2026-07-03)** — 3-root cause, target arch, state matrix, P0–P4 Strangler, 5 FFs (2 power gates: FF-AUTHORING-LOCALE-COMPLETE catches the monolingual-author root + FF-RENDER-NO-LOCALE-LEAK scans the rendered tree both directions), rejected alts | build **P0 NOW** (bind `<html lang>`/`dir` in LocaleGuard + `changeLanguage` + `localeDirection` registry — isolated, zero collision with in-flight chart/table branches). Then per §6 sequencing: land in-flight branches → P2 backfill after #3 → P1+AR-38 bundled per shell → P3 map → P4 Constructor+leak-gate |
| **AR-39** | **Data-integrity badge consolidation (owner 2026-07-03)** | the "Prelim." badge repeats per-panel (chart/table/kpi/gauge/map each render one via `PANEL_TITLE_BADGE`) — noise + wasted space. Consolidate to ONE canonical section-level indicator (Eurostat/ONS/IMF surface provenance at the DATASET/section scope + per-cell flags with one legend, NOT a per-chart pill). Model = **activate the `NodeStatusContext` publish/subscribe seam `SectionShell` already pre-reserved** (Option D's "if a real aggregate-status consumer appears…") — AR-38 IS that second consumer (YAGNI satisfied): panels PUBLISH `resolvePreliminary` upward, the section (information expert, GRASP) OR-folds + renders ONE indicator folded into its existing methodology affordance; standalone panels degrade to a local badge (Postel). Per-cell OBS_STATUS flags + footer legend retained. Law 9 preserved (all reachable via disclosure, not color-only). Localized via AR-37's seam. | `DESIGN-integrity-badge-consolidation.md` | **DESIGNED (2026-07-03)** — canonical model, 2 FFs (FF-ONE-INTEGRITY-INDICATOR + FF-INTEGRITY-REACHABLE), 3 rejected alts (section-reads-child-rows / author-only / restyle-smaller) | **depends on AR-37 P0+P1**; edits the SAME panel shells as AR-37 P1/P4 → BUNDLE per shell (touch each shell once). Build after chart+table branches land, per AR-37 §6 combined order |

## D. Deferred / Crown (owner-gated or consumer-gated)

| ID | Architecture / Vision | Concept | Status | Gate |
|----|----------------------|---------|--------|------|
| **AR-30** | **Multi-tenancy SaaS (POOL)** | one `tenant_id`+RLS isolates 5 planes below the port; agency=tenant; surpass Grafana/Looker | DEFERRED (owner) | perfect single-tenant first; seam preserved — see orchestrator `project_mt_deferred.md` |
| **AR-31** | **Perspective Lattice (crown)** | N orthogonal axes → 2^N permalink views | VISION | build WITH a real 2nd consumer (vintage/revision toggle) or it's adoption-debt |
| **AR-32** | **`_geoMode` → declared axis** | promote underscore-private computed var to a declared perspective axis | VISION | after the lattice |
| **AR-33** | **SDMX DQAF + REST surface** | quality indicators + SDMX-REST serve on the open `?format=` seam | VISION | build additively per real consumer (not speculative) |
| **AR-34** | **Table header FREEZES on scroll + stays column-aligned — table OWNS its stickiness (owner)** | owner 2026-07-01 on the live dashboard, 3rd defect on the same table BEYOND AR-15 (scroll) + AR-27 (header wrap): (1) the header did NOT freeze on vertical scroll; (2) header looked MISaligned with the body. DIAGNOSIS: NOT a split header/body — Simple/PivotTable already render ONE `<table>` with `<thead>` inside the `.data-table__wrap` scroll container (shared column tracks → alignment is structural). ROOT (both defects, one cause): the ONLY `position:sticky` header rule lived in `node-styles.css` (AR-8) GATED behind `[data-height]`/`[data-aspect]`/`[data-view=visible]` layout-container attributes — so any table panel WITHOUT those attributes (the live case) had a non-sticky header that scrolled up & out of view, making the body look headerless/misaligned. Stickiness is an INTRINSIC property of a table whose own wrap scrolls, not of a layout container. | (this card) | **BUILT (2026-07-01)** — `packages/plugins/panels/table/**/data-table.css`: `.data-table thead th { position:sticky; top:0; z-index:2; box-shadow:inset 0 -1px 0 var(--color-surface-frame) }` — the table component OWNS its sticky header UNCONDITIONALLY (SSOT + SoC), no longer dependent on node-styles' layout-gated rules. Set on the `th` CELLS (works under `border-collapse:collapse`; IDENTICAL mechanism to the committed node-styles rules → idempotent in the band case, and node-styles' more-specific `@media (<768px) { position:static }` mobile-reflow reset still wins). Show-through guard = existing opaque themed `background:var(--color-surface-raised)` on `.data-table th` + inset box-shadow divider (paints WITH the cell → scrolls correctly, unlike a collapsed border-bottom that can detach). Pivot two-row grouped header: `.data-table thead tr:nth-child(2) th { top:var(--data-table-head-row-h,2.25rem) }` offsets the sub-column row beneath the group row (no overlap); token seam (Protected Variations). Because header + body are ONE table, the frozen header rides the SAME column tracks as the body → alignment is structural, not synced. Fitness `data-table.layout.fitness.test.ts` +6 (FF-TABLE-HEADER-STICKY: position:sticky · top:0 · z-index · opaque token bg no-hex · box-shadow divider token no-hex · pivot sub-row offset). Green: build:engine·typecheck·tsc panel·lint(0 err)·check-laws·vitest 423 plugins+styles (was 417). AR-15 scroll (`overflow:auto`/min-width:0/`.scroll-fancy`) · AR-27 header-wrap · AR-8 band height · sr-only a11y table · dark theme NOT regressed (node-styles.css·tokens.css·scrollbar.css untouched). | REAL-BROWSER verify (batch redeploy): scroll a TALL table (Simple, long header) + a WIDE cross-classified PIVOT (2-row grouped header) in a panel WITHOUT `[data-height]`/`[data-aspect]` — header FREEZES at top, stays column-aligned with body, opaque (no rows bleed through), pivot group+sub rows both freeze without overlap; probe `<thead>` th computed `position:sticky`; both light+dark |
| **AR-35** | **The SNA "National Accounts" PIVOT table — header↔column alignment + freeze, FIXED & REAL-PAGE VERIFIED (owner's #1)** | owner frustrated across many turns: AR-15/27/34 fixes hit `SimpleTable`/`.data-table` but NEVER the table the owner was actually looking at. ROOT of the miss: the live `/ka/accounts` "National Accounts" table has a `series` encoding (Resources/Uses), so `DataTable` dispatches to **`PivotTable`** — a DIFFERENT codepath the generic fixes bypassed (a prior probe even mis-measured a 2-col `SimpleTable` elsewhere on the page). TWO real defects, both PivotTable-specific: (1) **MISALIGNMENT** — the flat single-column pivot header hardcoded `className="r"` (right) while the body td keyed on `col.align === 'r'`; the SNA `value` column has NO `align`, so header=right / body=left → the "რესურსები"/"გამოყენება" labels sat right of their digits (SSOT violation: header & body derived alignment from DIFFERENT sources). (2) **NO FREEZE** — this table is UNBOUNDED (no AR-8 `[data-height]` band around it), and `.data-table__wrap` had `overflow:auto` but NO bounded height, so the wrap grew to full content height → the PAGE scrolled and the sticky header left with it (AR-34's sticky rule was correct but a scroll container needs a bounded height to freeze). | (this card) | **BUILT + VERIFIED (2026-07-01)** — (1) `PivotTable.tsx`: single `alignClass(col)` source (pivot value cells are numeric → RIGHT by default per IMF/Eurostat, `align:'l'` opts out), applied IDENTICALLY to flat series header th, multiCol sub-header th, body td, and tfoot td — header now always sits above its digits (SSOT). (2) `data-table.css`: `.data-table__wrap { max-height:var(--data-table-max-h,70vh) }` gives the wrap an intrinsic bounded scroll viewport so the AR-34 sticky header FREEZES even when unbounded — table scrolls INSIDE the panel with the `.scroll-fancy` scrollbar instead of pushing the page; composes with the band (`max-height` only caps, never forces height); mobile `@media(≤768px)` unsets it + releases the freeze (inline reading), placed AFTER the sticky rule so source order wins. Fitness: `data-table.layout.fitness.test.ts` +5 (FF-TABLE-BOUNDED-SCROLL: max-height token · overflow:auto composes · mobile resets both · reset-after-sticky order) + NEW `pivot-align.fitness.test.tsx` (rendered-DOM: header th class === body td class per column; `align:'l'` lockstep; multiCol per-sub-col). Green: build:engine·typecheck·tsc panel·lint(0 err)·check-laws·vitest 19/19 table. AR-15/27/34 · AR-8 band · sr-table · dark NOT regressed. | **VERIFIED on `/ka/accounts` (live-API dev, Playwright probe `work/probe-sna-table.mjs`, PivotTable(flat)): BEFORE (deployed :3002) — header thTextAlign `right` / body td `start`, `aligned:false`; wrapMaxHeight `none`, height==scrollHeight (not bounded, header can't freeze). AFTER (my fix) — both cols `thTextAlign:right & tdTextAlign:right → aligned:true, allAligned:true`; wrapMaxHeight `630px` (70vh), scrollHeight 1151 > clientHeight 630 (`boundedTaller:true`), thTop 558→558 across a real 200px internal scroll (genuinely frozen). Screenshots `work/table-truth/accounts_main_1440_AFTER_table.png`.** Deployed-server re-verify pending next redeploy |

## F. Lead-originated proactive initiatives (2026-07-03 — I propose these, unprompted, from analysis of THIS session's bug-class + reference platforms)

> Thesis: the whole session's firefight (stale `ctx.rows`, cache collisions, the 161766 double-count, panel-to-panel number drift) is not random — it is the symptom of TWO missing architectural layers. The reference platforms that got this right don't have our bug-class because their architecture makes it *structurally impossible*. Stop patching symptoms panel-by-panel; install the layers that kill the class.

| ID | Initiative | Concept + reference benchmark | Why (grounded in what we hit) | Status |
|----|-----------|-------------------------------|-------------------------------|--------|
| **AR-40** | **Semantic / Metrics Layer (governed) + landing Featured-Slider (first consumer)** | Every measure declared ONCE in the governed `MetricDef` registry; every surface (KPI/chart/table/**featured-slider**) resolves a metric-ref through the ONE `resolveMeasureRef` seam + existing DataStore — never re-derives. The spine EXISTS in code (`core/data/metric.ts`, manifest→boot delivery); AR-40 = **completing it** (make `interpretKpi`'s point-read metric-aware = the last non-unified path; +`format` governed once) + proving it with the landing **featured-slider** (yellow-cell → `FEATURED.json` → semantic layer → config → carousel, replacing today's HARD-CODED `stats-carousel` strings `"204 000"`/`"54 100"` — already stale). Benchmark: **Cube.dev** (`dataSource`-on-measure) · **LookML** (define-vs-curate → "featured" is a curation, not a metric flag) · **Malloy/dbt** (measure-algebra = calc metrics). REFUSED: filters/joins/sql on `MetricDef` (kept thin). | The 161766 double-count + "chart≠table" + `$ne:_T` scattering + the hard-coded slider are all *metric-definition/re-derivation* bugs. One governed definition + one resolution path makes an inconsistent number structurally impossible, and makes the Constructor's metric-picker trivial. | **DESIGNED (2026-07-04)** — doc `SPEC-AR40-semantic-layer-and-featured-slider.md`: schema (+`format` only), U1 KPI-metric-aware unification, `FeaturedItemSpec`+`interpretFeatured` (reuses `interpretKpi`), new `featured-slider` node (a11y/i18n/Constructor-ready), 4-phase Strangler (P0 spine→P1 populate→P2 node→P3 landing migrate), 11 fitness fns, 4 elevations noted (→AR-31/42/43, AR-10 picker). **✅ DONE + LIVE (2026-07-04, all phases)** — lead-approved the 6 decision points (owner directive "you decide, lead"; no sign-off bounce). **P0** spine: `resolveValue`/`resolveTrend`/preliminary now metric-aware via `resolveMeasureRef` (the last non-unified path), +`format` on MetricDef/wire (additive), gdp-total→`gdp.current` byte-identical, FF-RAW-CODE-IDENTICAL. **P1** 11 metrics populated (units/format, methodology omitted where unknown). **P2** `featured-slider` node — `FeaturedItemSpec`→`featuredToKpiSpec`→`interpretKpi` (live values/warm-symmetry/preliminary/i18n free); format precedence `item>metric>fallback` (closes the P0 loop); cross-dataset `FeaturedStoreResolver` on the KPI seam; full a11y (tablist/tabpanel/roving-tabindex/reduced-motion/sr-only trend). **P3** landing `stats-carousel` Strangler-retired (hard-typed `"204 000"`/`"54 100"` DELETED — grep 0; node kept for editorial). Converged gate 2492/0; DEPLOYED (cd0ebb8) + BROWSER-VERIFIED — landing featured-slider live (GNI 98035, GDI 107781, 3 tabs, drill-links, ka/en, 0 console errors); stale 54100/204000 deleted. Foundation for AR-42/43. Elevations still registered: drill-deep-link→AR-42, lineage-on-slide→AR-43, Constructor metric-picker→AR-10. |
| **AR-41** | **Reactive dataflow core** | Derived data (pivot rows, vars, KPIs) as a reactive graph that auto-recomputes ONLY its dirty dependents on selection/param change. Benchmark: **Observable runtime · signals (fine-grained reactivity) · incremental computation**. | The entire State-B fight — stale `ctx.rows`, the module-cache collision, cache-key node-uniqueness — is the "manual invalidation" bug-class. A reactive core removes the class: you can't serve a stale derivation. | PROPOSED (lead) — kills the class we firefought all day |
| **AR-42** | **Grammar of Interaction (full) → Constructor-authorable** | Elevate the `on[]`→CommandBus events I built into a COMPLETE declarative interaction grammar: pivot/rotate, drill-down, brush, linked-highlight, cross-page params — bound to state, authored visually. Benchmark: **Vega-Lite selections/params · Observable reactive inputs · Grafana panel editor · Builder.io**. | We already have events + the AR-36/38 pivot seam; formalizing them turns statdash from a dashboard into a PLATFORM statisticians author without devs (Phase-2 Constructor's reason to exist). | PROPOSED (lead) — natural evolution of the events grammar |
| **AR-43** | **Data lineage / provenance surface** | Every rendered number traces to its SDMX source → vintage → revision → methodology, as a first-class lineage graph, not scattered badges. Benchmark: **ONS/Eurostat/IMF data-integrity standards · OpenLineage · OECD.Stat**. | This is an NSO (Geostat) product — provenance IS the product's credibility. AR-39's "Prelim" badge is a fragment; the full vision is reference-grade dissemination trust. | PROPOSED (lead) — reference-grade for a national statistics office |
| **AR-44** | **Explorable / narrative + a11y-first dissemination** | Auto-generated narrative summaries, keyboard-first exploration, screen-reader data-table parity, "explorable explanation" UX. Benchmark: **Our World in Data · Observable Framework · Reuters/Pudding graphics · WCAG 2.2**. | Elevates from "dashboard someone reads" to "data story anyone can explore" — the differentiator vs a static NSO site. | PROPOSED (lead) — product differentiation |
| **AR-45** | **Cartesian ApexOptions builder decomposition (400-line God-function → slice pipeline + family descriptor)** | `apex/cartesian.ts` (397 lines, at the hard bloat ceiling — routine edits now blocked) builds ApexOptions for SIX families (bar/hbar/line/area/waterfall/combo) with per-family conditionals (`isWaterfall/isCombo/isStackedArea`) scattered across series/colors/fill/stroke/dataLabels/tooltip + the full responsive[] array — classic divergent-change + shotgun-surgery. Fix = **Pipe-and-Filter of pure slice-builders** (`series/colors/axes/marks/data-labels/grid/chrome/responsive`, each `(output,ctx)→sub-object`) composed by a thin `build.ts` assembler, with per-family variation LIFTED into a compile-exhaustive `FAMILY_TRAITS: Record<CartesianFamily,…>` descriptor **resolved once into discriminated enums** on a `CartesianContext` (slices `switch(ctx.fillMode)`, never `type===`). Static descriptor (closed engine-owned set), NOT a runtime registry (YAGNI vs the plugin-registry idiom). Promote `cartesian.ts`→`cartesian/` dir; `index.ts` barrel keeps the `./cartesian` seam byte-identical (only consumer = `toApexOptions.ts` + 6 tests). Benchmark: **Grafana panel adapter · Vega-Lite renderer · Fowler extract-class/replace-conditional-with-polymorphism**. | The bloat gate is blocking edits (a 3-line change had to trim comments); adding a 7th family today = ~8 scattered touches. Pure structural debt, owner-approved to fix at highest standard. | **DESIGNED (2026-07-07)** — doc `SPEC-cartesian-decomposition.md`: 12-module target layout (each <200 lines), 9-step Strangler order (green after each — 6 test files drive `buildCartesian` end-to-end as the net), OCP win (new family = 1 descriptor entry, compiler-forced), 7 risks handled (theme-freeze/responsive-formatter-re-carry/combo-array-order/spacer-sentinel-SSOT/axis-hidden-SSOT), new `cartesian-decomposition.fitness.test.ts` (exhaustive traits + no-`type===`-in-slices + no-module-scope-cssVar + <200 lines). Pattern C chosen over pure-strategy (DRY) + inline-conditionals (no OCP win). | **owner sign-off to build** (Strangler steps 1→9, one commit per step, gate `tsc -b`+vitest 571+check-laws+lint each). Then follow-on (separate, YAGNI-gated): fold `contribution.ts`/`hbar-diverging.ts` onto the shared slices once the second consumer is real |
| **AR-46** | **Extension / Plugin SDK — externalize the registry as a versioned capability contract** | The `registerSlice`/`registerSpec`/`registerMigration`/`registerMetrics` seams are rich but monorepo-INTERNAL — a new capability = editing packages. Elevate them into a contract-first, versioned `@statdash/plugin-sdk`: a public `NodeSliceMeta`/PropSchema/DataSpec contract + a capability manifest (`plugin.json`-class: id·version·caps·engine-compat range) + a scaffold + a compat gate so a third party (or the Constructor itself) ships a node/spec/datasource/metric without touching engine internals. Benchmark: **Grafana signed plugin SDK + `plugin.json`** · **Backstage plugin API** · **Retool custom components** · **Superset viz plugins**. The SKILL canon "ship capabilities, not one-offs / the Constructor sees only what's registered" made external. REFUSED: a runtime code-injection plugin model (config stays data — Law 2); keep it schema+registration only. | The Constructor's palette IS the registry; hardening the registry into a stable external SDK is the single highest-leverage Phase-2 enabler, and turns "add a capability" from an engine PR into a registered plugin. Benchmark gap row #12 (Lag). | **PROPOSED (lead-scan 2026-07-08)** — from `BENCHMARK-REFERENCE-PLATFORMS.md` scan #1 |
| **AR-47** | **Config authoring governance — draft→review→publish FSM + change-audit + version diff/rollback** | `page_version` gives version ROWS but no editorial WORKFLOW: a draft→review→publish state machine, a config-change audit trail (who·when·why), a version DIFF, and one-click ROLLBACK. Distinct from AR-43 (DATA lineage — provenance of numbers); this is CONFIG governance — provenance of the authored artifact. Benchmark: **Grafana version history + provisioning-as-code** · **Builder.io publish workflow + revision log** · **Contentful/Sanity editorial workflows** · **Superset ownership**. Folds in the AR-11-adjacent authoring UX gaps (live preview, draft-diff). | The moment non-devs author (Phase 2), multi-author safety is table-stakes; today an edit overwrites with no review gate, no audit, no rollback. Benchmark gap rows #11+#13 (Lag). Naturally absorbs the authoring-role half of the RBAC gap (#10). | **PROPOSED (lead-scan 2026-07-08)** — from `BENCHMARK-REFERENCE-PLATFORMS.md` scan #1 |
| **AR-48** | **Headless export / embed / snapshot delivery port** | Install one headless delivery PORT with three facets over ONE `ViewSnapshot` SSOT: **extract** (per-section CSV/Excel/**PNG**), **embed** (signed single-card iframe), **permalink** (view-state URL). **KEY FINDING (design): NOT greenfield — the backend is ~80% BUILT to reference grade** (the benchmark's "export = STUB" is STALE): data export CSV/XLSX/SDMX-JSON is live (`core/data/export/` + `EXPORT_MENU` DI + `data:export` cmd + per-section `NodeExportContext`); snapshot serialization exists (`renderPageToJSON`/`renderPageToHTML`, `RenderTarget`); persistence exists (`SnapshotStore` port + pg `config.snapshot` **V36** + `SnapshotEnvelope` contract); embed mint/read routes exist (`/api/snapshots` JWT+audit, `/api/embed/:token?sig` HMAC 403/404/410, `EMBED_SECRET` prod gate) — all MOUNTED. Real AR-48 = (1) NAME the port (`ViewSnapshot` SSOT unifies permalink-state + `renderPageToJSON`-data + reference-metadata-provenance), (2) WIRE the fully-built-but-UNCONSUMED embed/snapshot backend to the client (`renderPageToJSON` called only in tests; geostat has zero mint/embed-render code), (3) COMPLETE 3 facet gaps: **image export** (PNG/SVG, app-layer OCP format), **provenance-on-export** (join `ReferenceMetadataContract` — csv footer / xlsx sheet / png caption), **card-scoping** (`EmbedParams.scope.nodeId`). Rides `render(config)` purity (Law 2 / AR-28) — pinned snapshot = static HTML on the same nginx, client-side image capture (no Node/Puppeteer tier). Benchmark: **Grafana snapshot + iframe embed + CSV/image export** · **Datawrapper/OWID embed codes** · **Superset export**. REFUSED: per-panel bespoke button · server-side render tier · live-URL-only citations (pin by default) · embed-as-config-node. | A public national-stats platform is judged on embed/export/citation; the mission-level use case is currently a dark backend seam. Non-MT (AR-30 `tenant_id` inert forward-add), static-runner-preserving (AR-28). Benchmark gap row #14 (mis-scored Lag → really "built backend, unwired client + 3 facets"). | **DESIGNED (2026-07-08)** — `DESIGN-delivery-port-export-embed-snapshot.md`: `ViewSnapshot` SSOT + 3-facet port, seam map across the arrow, provenance flow, pinned/live reproducibility model, 5-phase Strangler (P0 name+correct-docs · P1 provenance · P2 wire-embed-loop · P3 image · P4 north-star) each with a FF, 6 rejected alts, 6 owner decision points (D3 cross-origin embed CSP = the one-way door flagged). Observation Duty: fix stale `plugins/CLAUDE.md` "stub" note + benchmark row #14. |

## Standing hunts (living inventories — mine these, don't re-discover)
- `HUNT-adoption.md` — built-but-under-adopted capabilities (AD-1…AD-6).
- `HUNT-antipatterns.md` — active anti-patterns/hardcodes/DRY (Root A/B/C — A+B+C addressed).
- `HUNT-future-vantage.md` — future capability roadmap + seams to open now.
- `HUNT-violations-inventory.md` — the vital-few violation inventory.

---

### Maintenance rule (binding on the lead)
1. Owner raises an architecture/vision → **add a card HERE immediately** (before moving on). No vision lives only in a chat message.
2. Every design doc / ADR produced → linked here with a status.
3. Status advances only on evidence (BUILT=green, VERIFIED=server-proven).
4. Consult this at session start + before routing UI/composition/platform work — it is the SSOT of committed architectures.
