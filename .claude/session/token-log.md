# token-log — HOT ledger (current period)
> Append-only: one line per run (kit `05`). Rotate with `/rotate` when this passes ~1 period / ~20KB;
> overflow goes to `.claude/session/archive/` (gitignored). Prior period: `archive/token-log-2026-07-01.md`.

[2026-07-03] SESSION ROLLUP: runs=0 total_tokens=0  (SessionEnd hook — measured)
