# ════════════════ RESUME HERE — SEAMLESS HANDOFF (2026-07-02) ════════════════
> New session: read THIS block, then `.claude/agent-memory/orchestrator/MEMORY.md` (doctrines) +
> `platform/work/ARCHITECTURE-REGISTRY.md` (committed visions) + the specs named below.
> Continue AS IF UNINTERRUPTED. Nothing is lost.

**STATE (2026-07-02):** branch `main`, all render-parity work MERGED + DEPLOYED to prod (SSH `geostat-deploy` 192.168.1.199, `ops/compose/docker-compose.prod.yml`, project `statdash-prod`, geostat :3002 / panel :3003). Full suite 2250 tests 0-fail at merge. Rollback armed: tags `pre-parity-deploy`=b5bf242, `pre-parity-origin-main`=57a34e5, DB dump `/tmp/statdash-prod-backup-20260702-094036.dump`, `:rollback` images. Live = "data as it was" (correct pre-regression values) VERIFIED by real-browser.

## THE HARD TASK IN PROGRESS: Regional cross-filter "old logic" + 2 missing GDP charts
Goal: restore the linked cross-filter experience (owner ref shots `scriness/img_1,5,6,11`) via CLEAN architecture (no hardcode, DRY/SSOT, refine-existing) + restore 2 dropped GDP charts. **Verify by LIVE DISPLAY, never by a proxy** ([[feedback_verify_the_purpose_not_the_proxy]]).
- **Spec (the target):** `platform/work/SPEC-regional-crossfilter-behavior.md` — State A (no selection): by-region donut(all) + comparison(all) + national KPIs. State B (region selected): composition RE-PURPOSES to sectoral-structure (x=sectors, stacked by selected regions, per img_5); comparison = only selected; KPIs = sum; map highlights. Directional: select-region→show `sector` dim, select-sector→show `geo` dim.

### PROGRESS — what is DONE this session
- **✅ Missing-chart DATA UNBLOCKED (database-architect, committed to workbook).** Both series were FOUND in git `191bc0e` old static `apps/geostat/src/data/gdp/raw.ts` and ADDED to `DATA/canonical/GDP_ANNUAL.xlsx` — ADDITIVE only, existing-row drift = 0, 0 dup dimKeys:
  - `non-observed-economy-share` (approach `_Z`, contribution_role="") — NOE bar. 2010=23.1% … 2025=12.4%.
  - new approach `GFCF` + 5 measures `gfcf-{dwellings,other-buildings-and-structures,machinery-and-equipment,cultivated-biological-resources,intellectual-property-products}` (contribution_role=add) — the `structural` donut. **Author note: donut = share of the 5-slice TOTAL (NOT % of `gross-capital-formation`, which is broader).** +96 DATA rows, geo=GE, zero publish-gate risk (identity gate keys on explicit codes, not contribution_role). STRUCTURE vintage left as-is.
- **✅ Cross-filter query-path DIAGNOSED (debugger).** The "all 11 regions show" symptom is **NOT a code defect** — `$ctx:geo` resolves correctly at 4 layers (`buildObsFilterParam`, `matchesFilter`, `extractRequirements`, warm/read parity) + server `= ANY`; repro with `geo='R2,R5'` → exactly 2 regions. `sectors-multi` is **correctly encoded by SECTOR** (not geo) — NO `id=sector` change needed. Live "11" is therefore EITHER (a) a **stale/un-rebuilt bundle** serving pre-fix wire code, OR (b) `region→geo` mapping (`useFilterState.ts:184-200`, config `context.dims.geo:"region"`) not reaching the chart ctx. **Pin (a)vs(b) after the coming redeploy** (fresh bundle) — most likely (a), resolved by the deploy itself.

### AGENTS DONE (all 3 finished; do NOT spawn more — session limit)
- **✅ database-architect** — DONE (measures added to canonical, above).
- **✅ debugger (State A KPI fix) — APPLIED ON DISK, UNCOMMITTED, NOT YET TESTED.** The agent completed a clean, coherent edit before hitting the session limit: all **14 regional GVA KPIs** in `geostat.provisioning.json` changed `geo:{$ctx:"geo", default:""}` → `geo:{$ctx:"geo", $ne:"_T"}`. Rationale (CORRECT, better than `default:'_T'`): `$ne:_T` = "constituent regions, never the `_T` total row" → State A sums the 11 leaves = national total ONCE (kills the 161766≈2× double-count); State B sums the selected subset. ONE expression governs both states (DRY), matches the existing `regions-bar` `$ne:'_T'` pattern. **JSON validated (parses; 0 remaining `default:""`).** STILL NEEDS: run the regional/store-filter fitness+parity suite (PARSE the log) to confirm State-A total ≈ 80979/93022 single, State-B = sum-of-selected.
- **✅ architect (AR-36 design) — DONE, `platform/work/DESIGN-grammar-of-interaction.md`, card DESIGNED.** Grammar: a pivot is the existing Grammar-of-Graphics `encoding.ts` rule wired to STATE via the cross-filter spine (no new plumbing). ONE seam: widen `EncodingChannel` → `string | ChannelDef | CtxRef` so `encoding.label:{$ctx:"_xDim"}` resolves in `interpretSpec` before `applyEncoding` (bare strings byte-identical). `_xDim`/`_seriesDim` computed by a `vars` `op:if` derive from selection params (same mechanism as `_regionSel`); data aggregates by BOTH dims once, rotate = pure re-read of same `DataRow[]` (no re-query). Directional select needs ZERO new wiring (existing FilterAction + derive); only a manual "swap" gesture adds a `PivotAction` arm through the SAME CommandBus point. **Verdict: ONE panel, folded in Strangler moves** (P2 the two bar orientations = provably same node; P3 the State-A donut via `chartType:{$ctx:"_mark"}` + empty-series degrade; delete `sectors`/`sectors-multi` post-verify). Phases P0 seam → P1 derive → P2 fold orientations → P3 fold donut → P4 explicit rotate verb → P5 Constructor (AR-10). 6 fitness gates, 5 rejected alts. **AWAITS OWNER SIGN-OFF on P0** (the `EncodingChannel` widening + `interpretSpec` pre-pass — additive, byte-identical, foundational). Collision note: P2/P3 edit the SAME regional composition sections (~L3440–3908) as the KPI fix — **land the KPI fix first, then rebase P2/P3.**

### AR-36 — NEW VISION (owner 2026-07-02, registered in ARCHITECTURE-REGISTRY.md §C-int)
Owner: the chart encoding `x=sector,series=geo` ⇄ `x=geo,series=sector` should be **runtime-swappable via events/filters**, NOT hardcoded as two `visibleWhen` A/B panels. = OLAP **pivot/rotate** as a first-class DECLARATIVE interactive capability; encoding channels bind to selection/param; a `pivot` verb added to the validated `on[]`→CommandBus events grammar. The regional composition SHOULD fold the two hardcoded `sectors`/`sectors-multi` panels into ONE pivot-panel. Converge THIS maximal target before building the swap ([[feedback_converge_maximal_target_first]]) → avoid hardcode-then-rework. **Lesson saved:** the LEAD should have proposed this ([[feedback_proactive_innovation_mandate]] — bind-to-state over branch-on-state).

### 🔴 RESUME HERE (2026-07-02 late) — regional cross-filter State B STILL broken; my fixes deployed but INSUFFICIENT
**Live now:** `main` HEAD `6f1b686` deployed to prod. geostat rebuilt via GENUINE no-cache (classic builder `DOCKER_BUILDKIT=0`, image removed, vite ran from source) → bundle `assets/index-8Q_G7s2F.js`. The hash is stable across rebuilds because it deterministically builds the SAME source — **my code IS live, NOT a stale-bundle/cache problem** (ruled out definitively). Server build clone `/tmp/statdash-build` @ 6f1b686 confirmed has my `dataPipe0` fix (grep ×3). Deploy = drove `docker-compose -f ops/compose/docker-compose.prod.yml -p statdash-prod build --no-cache geostat` + `up -d --force-recreate geostat` over SSH myself (no agent). NOTE: server uses `docker-compose` (hyphen, v5) + `DOCKER_BUILDKIT=0` to force a true rebuild (BuildKit cache-mounts survive `--no-cache`). Service names: `geostat`,`panel`,`statdash-api`.

**What WORKS (probe-confirmed):** GDP `structural` %-donut PAIRED beside income treemap (img_2, same row); theme dark legible; State B `_mark` swaps donut→bar; map highlights 2; State-B KPI sums (9081).

**What's STILL BROKEN (the remaining root — my 2 fixes were insufficient):**
- **State B composition:** shows x-axis = 11 REGION names (not sectors), series=1, data all-regions. My `resolveNodeRows` data-pipe fix (resolve `node.data.pipe` `{$ctx:_byDims}` before interpretSpec, commit 6f1b686) SHOULD rotate the grain — but if State B still shows regions, EITHER (a) `ctx.vars` lacks `_byDims`/`_xDim` at resolveNodeRows (vars-threading divergence: `_mark` resolves in plugins `useChartOutput` but encoding/pipe refs in react `resolveNodeRows` don't — investigate whether the inner-page `vars` (provisioning ~L4740) reach `resolveNodeRows`'s `ctx.vars`), OR (b) the deeper **bug 2**: the chart obs-query geo doesn't scope to the selected regions (val path scopes, obs path doesn't — `regions-bar` ALSO shows 11 in State B). Bug 2 is the val/obs SSOT divergence (store-api `applyClientFilter` only re-applies `$ne`, not the positive filter; obs path trusts the wire/server; `toWireValue` (store-filter.ts:40) mishandles an ARRAY ctx.dims.geo → `String(array)`='R3,R4' scalar). **PIN bug (a) vs (b) with a WORKING browser probe FIRST (don't theorize-fix).**
- **State A KPI still 161766** (doubled) was the symptom; my `ctx.exclude` threading (commit 6f1b686: store-api-pointread.ts + store-impl.ts `_val`/`_valAt` pass ctx.exclude → matchedValues) SHOULD fix it live. UNVERIFIED live.

**PROBE TOOLING BLOCKER (must solve to verify):** my bash env has NO local playwright; server docker `mcr.microsoft.com/playwright:v1.46.0-jammy` fails `Cannot find package 'playwright'` (ESM resolution from `/`). The earlier probe AGENTS ran LOCAL playwright in their sandbox (worked). FIX: either set up local playwright (`npm i playwright@1.46 && npx playwright install chromium`, probe `http://192.168.1.199:3002` directly — reachable), OR fix the docker module resolution (find playwright path in image, `.cjs`+NODE_PATH, or `-w` the right dir). Existing probe scripts: `platform/work/verify-shipped-428783e.mjs`, `stateB.mjs`, `verify-shipped.mjs`. Shots: `platform/work/verify-shots/`.

**NEXT (do this):** (1) get a WORKING browser probe; (2) probe live State B → pin whether sectors-x now shows (data-pipe fix worked) + whether geo scopes to 2 (bug 2) + State A KPI single (exclude fix worked); (3) fix the pinned remaining root (vars-threading to resolveNodeRows, or bug-2 obs-scoping — make applyClientFilter re-apply the positive filter too = val/obs SSOT, the clean root); (4) verify live by DISPLAY vs img_5; (5) then say done. Class-M (DataStore/interpretSpec contract) — consider chief-engineer review before final merge.

**TOMORROW (owner ask):** full i18n sync — plan written `platform/work/PLAN-i18n-full-sync.md` (every plugin/chart/table/config/chrome switches language synchronously; `<html lang>` currently stuck 'en' on /ka; mixed-locale live). Design via architect → AR-card.

### ⚠️ LIVE PROBE GROUND-TRUTH (2026-07-02, deployed 0a16509 vs img_1/img_5) — TWO REAL BUGS the earlier theorizing missed
Probe scripts `platform/work/verify-shipped.mjs` + `stateB.mjs`; shots `platform/work/verify-shots/*.png`; raw `_report.json`/`_stateB.json`. Direct-reachable `http://192.168.1.199:3002`. 0 console errors, 0 `[object Object]`, 0 React#31. Structural donut (5 slices, %, no center) + noe-share (16 bars 12–23%) RENDER fine.
- **BUG 1 — State-A regional KPI STILL doubles (161766 ≈ 2×80979) despite `geo:{$ctx:geo,$ne:'_T'}`.** So `$ne:'_T'` is INEFFECTIVE when `$ctx:geo` is EMPTY (no selection) on the live ApiStore/val path — fails to exclude the `_T` row → sums `_T`+11 leaves. Passed ExternalStore fitness, FAILS live wire (proxy-masking class). MUST root-cause the empty-`$ctx`+`$ne` resolution on the REAL path; State-A national KPI must = ~80979 single; State-B still sums only selected. Add a REAL-PATH fitness (not ExternalStore).
- **BUG 2 (foundational) — chart obs-query path does NOT receive the region selection.** Selecting 2 regions → `?region=R3,R4`; the KPI *val* path scopes (161766→9081) but the CHART query path does NOT: `regions-bar` still 11 bars, `sectors-multi` still 11. So `region→geo` (`context.dims.geo:"region"`) reaches the KPI val ctx but NOT the chart obs-query ctx. Any pivot panel will ALSO fail to scope until this is fixed. Root-cause where the selection fails to reach the chart query ctx vs the val ctx.
- Secondary: 'Tbilisi share' KPI = 469.3% in State B (share denominator wrong under selection). Map highlight works (2 regions thick). Selection gesture = clicking map polygons → `?region=` CSV (max 10).
- `a894af9889ce3fcef` (engine-specialist) is building the AR-36 pivot for the regional composition + owns the query-scoping (bug 2 is in its P2 scope). I CANNOT live-message it (no SendMessage tool) → on its return I verify its branch against BUGS 1+2 on the REAL path + fix bug 1 (KPI) if uncovered, before deploy. Do NOT spawn a second agent on the same regional files (collision).

### LIVE STATUS (2026-07-02, DEPLOYED)
- **MERGED to `main` (`0a16509`, pushed) + DEPLOYED to prod.** API image `sha256:75532ea884ff` (API-only rebuild — provisioning is baked into the API image + re-applied by `runProvisioning`; geostat/panel bundles byte-identical, render the new charts/KPI-fix from the API's provisioning at runtime). DB stays V38 (no migration); additive ingest published `{"published":384}` (288+96) — `approach=GFCF`=80 rows, `non-observed-economy-share`=16 rows CONFIRMED in gold + served by `/api/stats/observations` + `/api/bootstrap`. Health/smoke green (:3002/:3003→200).
- **Rollback armed:** DB dump `/tmp/statdash-prod-backup-20260702-160124.dump` (restorable-verified); `:rollback` image tags api=`91ad25c4`/geostat=`e86814f8`/panel=`2ed3320f`; git tag `pre-gdp-charts-deploy`=`0dc7d54` (pushed). Prior parity tags still present.
- **IN FLIGHT:** (1) live real-browser DISPLAY verification (capture-probe agent → TEXT + `platform/work/verify-shots/*.png`) — I READ the shots vs img_5 before declaring done; (2) AR-36 P0 build (engine-specialist, isolated worktree, own branch — NOT merged).
- **Ingest-driver defects flagged (owner follow-up):** `ops/scripts/ingest-canonical.sh` — stale `EXPECTED_OBS[GDP_ANNUAL]=288` (now 384); `publish_job` should treat a converged-409 as success (re-ingest auto-publishes codelists before the explicit POST → `set -e` bailed; resolved non-destructively).

### DONE ON DISK THIS SESSION (committed to main 0a16509, unit-gate GREEN)
- **✅ State A KPI double-count FIXED** — 14 regional KPIs `geo:{$ctx:geo,$ne:'_T'}` (constituent leaves, never the `_T` total; correct in all 3 states, DRY). Fitness `FF-REGIONAL-KPI-GEO-BINDING` (crossFilterLinkage) rewritten to LOCK the corrected invariant (forbids wildcard/`_T` default, requires `$ne:_T` — guards BOTH the double-count AND the `_T` dead-cell).
- **✅ 2 charts RE-AUTHORED** in `geostat.provisioning.json`, canonical declarative pattern (mirrors income donut / growth bar — best-in-class Grammar-of-Graphics, NOT copied-because-legacy):
  - `structural` GFCF donut — new grid gated `perspective-is:year`, `query{measure:*, filter{geo:GE, approach:GFCF, time:$ctx}}`, aggregate→lookup→sort, `encoding.pct:{sumOf:value}` (share of the 5 slices); centerLabel Total; %-share table. Placed after `income`.
  - `noe-share` bar — added beside `growth-dynamics` in the range grid (`perspective-is:range`), `query{measure:*, filter{geo:GE, approach:_Z, measure:non-observed-economy-share}}`, `fromDim/toDim`, `label:time`; the range grid `templateColumns` → responsive `repeat(auto-fit,minmax(min(100%,26rem),1fr))` (AR-5 canonical) so growth+NOE pair + reflow. `format:'pct'` confirmed safe (`fmtPct` appends % w/o ×100; NOE values already percent).
  - **nav:** added `structural` + `noe-share` anchors, removed the stale re-add `_comment`.
  - Gates updated for the legit additions: `chart-presence` golden gdp inventory +`structural:donut`+`noe-share:bar`; `perspective-migration-equiv` gate count 10→11 (per-gate equivalence still holds).
- **UNIT-GATE GREEN:** provisioning fitness 84/84 · geostat 85/85 · store-filter · check-laws all-clean · lint 0-error (2 pre-existing warnings). JSON valid.
- **✅ AR-36 designed** (`DESIGN-grammar-of-interaction.md`, card DESIGNED) — awaits owner sign-off on P0.

### NEXT STEPS (resume here)
1. **Owner decision (2 asks):** (a) commit these changes (on `main` → BRANCH FIRST per rule) + **deploy to prod** (real-server side-effect → needs owner OK; backup-first, build branch, smoke, rollback-ready, NEVER `down -v`); (b) **AR-36 P0 sign-off** to start the runtime-pivot build.
2. Remaining converged gate before deploy (not yet run, low-risk since JSON+tests only): `build:engine` + geostat-tsc + `tsc -b apps/panel` + FULL test — PARSE the log.
3. After deploy (fresh bundle) **VERIFY LIVE by DISPLAY** vs `SPEC-regional-crossfilter-behavior.md` + img_5: State A donut+bar+national-KPI (single, not 2×); select 2 regions → composition=sectoral-structure of those 2, bar=only 2, KPI=sum, map highlights; select sector → region-comparison of that sector; **+ the 2 restored charts render** (structural donut on year mode, noe-share bar on range mode). This fresh bundle also resolves the cross-filter "11 regions" (a)stale-bundle vs (b)mapping question. Only THEN say done.

### SAFETY / where we are, defensively
- Prod is LIVE + GREEN on the render-parity baseline; the cross-filter/missing-chart work is NOT yet redeployed — **live prod is safe** at the parity baseline. Rollback assets listed above.
- Working tree: `DATA/canonical/GDP_ANNUAL.xlsx` modified (measures added, verified 0-drift); provisioning being edited by the KPI-fix agent; `ARCHITECTURE-REGISTRY.md` + orchestrator memory updated (AR-36 + proactive-innovation lesson). NOTHING committed yet this task — commit at the converged gate.
- Owner constraint (this session): **token limit — do NOT spawn new agents**; reuse the 3 in-flight via SendMessage; lead may do config edits directly if needed to stay within budget.

**RHYTHM:** disjoint parallel agents → converged gate (build:engine + geostat-tsc + `tsc -b apps/panel` + lint + check-laws + full test, PARSE logs) → logical commits (NO backticks in -m) → push → deploy → LIVE-DISPLAY verify. Never commit/deploy/"done" on agent self-report or green proxy alone.

# ════════════════ (older log rotated out) ════════════════
> Cold/older layers (06-27 → 07-01, landed work) moved to `docs/layers/2026-07-01-context-cold-log.md`.
> This file keeps only the hot resume head. Rotate again with `/rotate` when it grows past ~15KB.
