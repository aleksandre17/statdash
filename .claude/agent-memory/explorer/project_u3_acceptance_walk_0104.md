---
name: project_u3_acceptance_walk_0104
description: Result of the U3 one-pipeline-grammar acceptance walk (2026-07-22/23) on 192.168.1.199:3013 — 4/5 checks clean pass, 1 partial (steward JSON pane still shows two accordion blocks); re-check 2026-07-23 found the duplicate-content issue fixed but a second, differently-labeled block still present
metadata:
  type: project
---

U3 rewrote the stored corpus onto one pipeline grammar (gdp 8 + regional 6 + accounts 4 specs + 12 stored specs, all PUT 200) and shipped the U1 live-switch + R1 store-cascade + R5 ascent fixes. Acceptance walk done via chrome-devtools MCP against `http://192.168.1.199:3013` (admin/dev_admin_pw_123), screenshots in `work/authoring-truth/0104/60-u3-*.png`.

Result per check:
1. **Pages render TRUE** — PASS. gdp («მშპ»), regional, accounts all render real data on the studio canvas (gdp expenditure waterfall shows real component values: 88425.6 / 22256.7 / 49585.4 / 55669.6 / 104598.1). 0 console `[error]` messages on any page (only benign React-Router future-flag warnings + axe "duplicate form field id" issues).
2. **ONE dialect in the pane** — PARTIAL/FAIL as literally specified. Opened the gdp expenditure chart's Data door (button "გახსენი მემკვიდრეობითი მონაცემები") → workbench → "სტიუარდი — რეალური query" section. The `"შენახული ჩანაწერი (pipeline)"` (stored) and `"დაშლილი — engine desugarToPipeline"` (decomposed) JSON blocks are now **content-identical** (same pipe steps, same encoding — only key order differs: `type` before `pipe` vs after). So stored ≡ canonical does hold data-wise. But the UI still renders them as **two separate expandable accordion sections**, not consolidated into the single block the brief called for. Same two-block pattern reproduced at the Specs-floor workbench (`/studio/data?dataFloor=specs`) on an unrelated spec (GDP_ANNUAL), confirming this is a systemic UI-consolidation gap, not a one-off.
3. **R1 (live rows)** — PASS. The "წყარო (Get)" step's "ცოცხალი მონაცემები" pane showed 5 real rows (GE geography, EXP approach, real values like 49585.42, 88425.6, etc.), not 0.
4. **R5 (no bounce-down)** — PASS. Clicked the same chart-overlay point 5 times in succession; selection climbed monotonically: chart → style-distribution wrapper → section → grid → inner-page (page root), where it plateaued. No bounce back to a lower level at any point.
5. **Specs floor** — PASS. `/studio/data?dataFloor=specs` list renders (~25 spec entries incl. F2-VERIFY/STEP-A/DU3 probes); opening one (GDP_ANNUAL) opened the workbench cleanly, 0 console errors (1 benign axe issue only).

**Why this matters:** Check 2 is the one open residual — the fix made stored/decomposed pipelines byte-equivalent (modulo key order) but nobody removed the now-redundant second accordion in the steward JSON pane UI. Anyone picking up "close out U3" should treat this as the remaining item, not re-litigate whether the data is correct (it is).

**How to apply:** Before claiming U3 is fully closed, verify whether a follow-up ticket exists for collapsing the steward-pane accordion to one block. If asked to re-verify check 2, look at `"სტიუარდი — რეალური query"` section's two expandable buttons directly — don't just diff the JSON (which will look "fixed" since content coincides).

**Re-check 2026-07-23 (commit 3af53567, gdp page, expenditure chart):** the literal named duplicate is gone — the second block is no longer titled `"დაშლილი — engine desugarToPipeline"` and no longer content-duplicates the pipeline. It has been replaced/renamed to `"დაბლა-გატანილი ObsQuery"` (down-pushed ObsQuery) showing genuinely different, narrower content (just `{filter, measure}`, not the full 8-step pipeline). So: the specific regression named in the brief (structural duplicate of the same pipeline under two labels) is fixed. But the pane still renders **two** accordion blocks under "სტიუარდი — რეალური query" (`"შენახული ჩანაწერი (pipeline)"` + `"დაბლა-გატანილი ObsQuery"`), so a literal "exactly ONE JSON block" reading of the acceptance criterion still fails — it's just no longer a duplicate, it's now two distinct artifacts shown side by side. Screenshot: `work/authoring-truth/0104/61-one-block.png`. 0 console errors on this page/door. Anyone re-verifying should decide whether "ONE block" means "no duplicate content" (now true) or "single accordion" (still false) before closing this out.
