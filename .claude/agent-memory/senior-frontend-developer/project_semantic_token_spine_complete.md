---
name: semantic-token-spine-complete
description: Pfinal of the semantic-token/theming spine — all shell color stragglers tokenized, FF-TOKEN-ONLY flipped to ERROR + FF-TENANT-OVERRIDE added. New role inventory, the cssVar() chart-fill util in @statdash/styles, the DonutChart SoC split, gate locations + allowlist.
metadata:
  type: project
---

Pfinal of the semantic-token spine ([[adr-semantic-token-theming-spine]], [[semantic-token-theming-spine-p0]], [[semantic-token-byte-identity-gotcha]]) is DONE and green (1104 tests; build:engine+geostat+panel+typecheck+lint(0 err)+test all pass).

**New SEMANTIC ROLES added** (in `packages/styles/src/css/tokens.css` `:root`, brand-neutral defaults; brand-ish ones rebound in `apps/geostat/src/shared/styles/index.css` `[data-tenant="geostat"]`):
- `--color-trend-positive`/`-negative` (KPI/sparkline direction; geostat `#2A9D8F`/`#E76F51`). Always paired with a glyph (↗/↘) — never color-only (WCAG 1.4.1).
- `--color-accent-chip-bg` (= accent-muted) + `--color-accent-chip-border` (geostat `#CCE8F5`).
- `--color-heading-display` (hero title; geostat `#0d3b66`).
- `--color-breadcrumb-separator` (geostat `#D8E4E8`).
- `--color-surface-translucent`/`--color-border-translucent` = `color-mix(... var(--color-surface) 80%/40%, transparent)` → byte-identical `rgba(255,255,255,.8/.4)` for the frosted app-header.
- `--color-chart-grid` (`#F0F5F3` apex grid), `--color-surface-hover` (`rgba(0,0,0,.04)` overlay), `--color-skeleton` (`#e8e8e8`).
- STATUS family: `--status-obs-{preliminary,estimate,revised,confidential}-*` (SDMX OBS_STATUS badges — a DISTINCT taxonomy from `--status-*` alert tones, adopted at the shells' live cell-badge values) + `--status-total-*` (tfoot aggregate row).
- `--color-error-*` (node-error warm-terracotta surface — perceptibly warmer than the cooler `--status-negative-*`, so its own family) + `--color-danger-fg` (`#dc2626` required-asterisk red).
All mirrored into `tokens/color.ts` COLOR/STATUS and the user-facing ones into `catalog/color.ts` descriptors (bilingual {ka,en}).

**ΔE collapses applied** (cohesion-first, ratified per ADR §3 — NOT byte-identical, dE00 3-10 but same design intent): `#5A7A8A`/`#6B8899`/`#6B8896`/`#718096`→text-muted; `#445A66`/`#2D3748`/`#3D4F5C`/`#334155`→text-secondary; `#B0C4CC`/`#8FA4AE`→text-faint; off-whites (`#F8FAFA`/`#F7FAFA`/`#F2F6F8`/`#f8f9fa`/`#f8fafc`)→surface-raised/sunken; `#CBD5E0`/`#CBD5E1`/`#cbd5e1`→border-strong; `#EFF3F3`/`#f0f0f0`→surface-frame; `#EAF4FB`/`#F0F8FF`→accent-muted. (Earlier the byte-identity-gotcha memory said "leave these literal until a pixel-diff gate accepts the trade" — Pfinal IS that acceptance point; there's no allowlist for frozen color debt so they HAD to collapse or get a distinct role.)

**Chart data-viz fills (var() invalid in SVG attrs / Apex strings / JS-parsed):** new util `cssVar(name, fallback)` exported from `@statdash/styles` (`utils/cssVar.ts`) — reads `getComputedStyle(documentElement).getPropertyValue(name)` with the literal fallback for SSR/jsdom. Used by all apex builders (base/cartesian/pie/contribution/hbar-diverging/treemap), DonutChart/DonutTip, HeroGraphic, annotationUtils. TreemapChart/HBarDivergingChart/StatsCarouselShell use plain `var()` (their literals are in React `style={{}}` objects, where var() IS valid). `mapColorUtils.ts` DEFAULT_PALETTE stays literal (allowlisted — sequential ColorBrewer-Blues, Leaflet-fed + JS-interpolated). Accent fallbacks `?? '#0080BE'` → `cssVar('--color-accent', '#0080BE')`.

**DonutChart split (bloat ceiling 400):** the post-edit-laws hook hard-blocks edits to files ≥400 lines. DonutChart was 403. Split into `donutGeometry.ts` (pure label-placement math + Slice/Lbl types + build()) + `DonutTip.tsx` (hover tooltip) + `DonutChart.tsx` (~95-line view). Clean SoC, not just a line-count dodge.

**The GATES (Pfinal flip):**
- FF-TOKEN-ONLY: `packages/plugins/nodes/__tests__/token-cohesion.fitness.test.ts` (@vitest-environment node). Scans `packages/plugins/**` + `packages/react/src/**` + **`packages/charts/src/**` + `packages/core/src/**`** (.css/.ts/.tsx) for hex/rgb/hsl; ERROR on any match. The charts+core extension (Wave-1 canonicalization 2026-06-28) closed a guard hole: the JS axis above plugins was UNSCANNED, so `charts/colors.ts` `DEFAULT_ACCENT_COLOR='#0080BE'` (the exact geostat brand accent) shipped as a "neutral" default and couldn't fail the build. NO dir-level exclusions; only `dist/`/node_modules, `*.test.*`, `*.stories.tsx` are out. Strips `cssVar(...)`/`var(--)`, `rgba(0,0,0,…)` black overlays, AND now `//` line comments (hex in a doc comment is not a color — added so `core/src/field/config.ts` colorStops example doesn't false-positive). Allowlist is now **value-aware**: an entry MAY pin `literals:[…]` so ONLY those exact values are exempt in that file and a brand hex slipped beside a sanctioned seed still FAILS (no file-blanket hiding). 4 entries: mapColorUtils palette + PropSchemaForm `#000000` (whole-file) · `charts/src/colors.ts` pinned `['#6B7B8D','#E53E3E']` (neutral series-grey + action-red wire-seeds; accent removed) · `core/src/registry/resolvers.ts` pinned `['#00A896','#E76F51']` (growth +/- semantic). Test asserts each pinned literal still present (no stale exemption) and `ALLOWED.length<=4`.
- FF-TENANT-OVERRIDE: `packages/react/src/engine/tenant-override.fitness.test.tsx` (@vitest-environment jsdom). Loads the real tokens.css + `[data-tenant=test]{--color-accent:#ff00ff}`, asserts the computed `--color-accent` flips and `cssVar()` observes it, zero shell edits. (jsdom DOES resolve the custom-prop cascade :root→[data-tenant] — verified; it does NOT do var() substitution, so assert on the custom-prop value the shell consumes.)

**GEOGRAPH STRAGGLER — CLOSED 2026-06-24:** `nodes/geograph/default/components/GeoMap.tsx` `#0080BE`(FILL_COLOR)/`#fff` tokenized via `cssVar('--color-accent','#0080BE')`/`cssVar('--color-surface','#fff')` — module-level `fillColor()`/`strokeColor()` arrow fns (Leaflet PathOptions is JS-fed, var() invalid). Byte-identical (jsdom returns fallbacks; un-themed render unchanged). The `geograph/` dir-level exclusion REMOVED from FF-TOKEN-ONLY; `sep` import dropped (became unused). No new allowlist entry needed (geograph has no real Leaflet palette — the choropleth DEFAULT_PALETTE lives in `panels/map/default/mapColorUtils.ts`, already allowlisted). Green: all 3 builds + typecheck + lint(0 err) + test(1104).