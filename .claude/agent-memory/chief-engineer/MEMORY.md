# Memory Index

## Auto-relocated (memory-home-guard — reconcile into a topic section)
- [Verification is manual](project_verification_is_manual.md) — NO executing CI; every green/BUILT/VERIFIED = one-time local run; ci.yml stale (@geostat scope) + never-run; 18 DB-gated + 12 e2e suites never ran; corpus metric-ref adoption=0; FF-DATA-BOUNDED absent (2026-07-15 coherence audit)
- [Systemic findings](project_systemic_findings.md) — cross-cutting classes distilled from retired review snapshots (async cache-key store-axis, ~~dead 304~~ [now fixed], i18n bare-string, query time hardcode, unit/measure model, prod-hardening doors, palette stubs, adoption meta-fitness, WCAG floor, live≠HEAD, ADR-0023 residuals) — VERIFY before acting
- [Kit false-green classes](project_kit_false_green_classes.md) — cp1252 hook stdout crashes (truncate SessionStart) + engine-stale project.json paths; how to re-detect a gate that greens while broken
- [AR-38 default asymmetry](project_ar38_default_asymmetry.md) — verified invariant: DimFilterRef `default` honored on KPI path ONLY (query path ignores it → must use `$ne:_T`); page vars resolve $ctx vs filterParams (literal '') not ctx.dims
- [Color-token JS-axis gap](project_color_token_jsaxis.md) — FF-TOKEN-ONLY guards CSS+plugins/react JS but NOT charts/core; brand #0080BE leaks there; pair name-scans with value-scans for brand-neutrality
- [DB i18n divergence](project_db_i18n_divergence.md) — silver ingest validator hardcodes ka|en, diverges from V13/V14 gold config.locale contract; approver preview can green-light a submission gold then rejects
- [self-policing mandate](feedback_self_policing_mandate.md) — owner must never be the one who discovers anti-patterns; every stated law needs a machine gate, false-green worse than no gate
- [Dev deploy topology gap](project_dev_deploy_topology_gap.md) — dev :3013 container mounts ONLY panel/src; packages/* baked into image → panel-only rsync never deploys engine changes (false-RED trap; how to docker-cp a package)
- [Value-cell fold fake-0](project_valuecell_fold_fake_zero.md) — folded timeseries/growth value-cell head has no store-home → cross-cube measure fabricates 0 per coord (Law 11), not honest no-data; GDP=0 vs GVA=real (2026-07-20)
- [Inheriting-child data door](project_inheriting_child_data_door.md) — card 0112 live-repro: data-less chart/table children of a data-owning section expose a DATA facet+door; open-with-no-edit writes a shadowing empty spec → empty workbench (S2) + vanished visual (S3, in-session-only). Plus CanvasOverlay re-measure RACE (S1). 0109 exposed all 3; E0/E1 caused none
- [Obs measure-drop](project_obs_measure_drop.md) — CONFIRMED-LIVE: buildObsFilterParam pins measure for `val` but NOT for `obs` top-level q.measure → metric-ref query charts fetch covering slice, collapse to last-wins measure (growth); config+server correct, render lies (Law 11). Config PUT is NOT the fix — STOP (2026-07-20)
