---
name: data-workspace-ux-backlog
description: Owner's 2026-07-20 live UX review of the data workspace (:3013) — 5 queued craft/IA items to fold into the ADR-051 DU6 polish + new mini-waves
metadata:
  type: project
---
2026-07-20 — while the ADR-051 data-workspace program was mid-flight (Step A live), the owner did a live UX review of :3013 and raised 5 items. QUEUED (WIP=1 — did not interrupt the F2/Step A honesty fix). These are craft/IA, mostly Model-floor + data-workspace polish.

1. **Metrics scattered chaotically** across different places → hard to use. Metric management/IA is fragmented; wants ONE coherent home/catalog for metrics. (Relates to the object-model spine + the Model floor.)
2. **Core functions forward on data-table elements.** By "perspective panel" the owner means the **full-screen DataWorkbench** (the 3-pane shaping panel = DataWorkbench + FieldWells + VerbPalette — owner-confirmed 2026-07-20, NOT the separate Perspectives feature). It's powerful and STAYS so; but the MOST BASIC table ops (pick measure, sort, filter, columns) should be surfaced ON the element (a lightweight popover?), so a simple op doesn't require opening the whole workbench. Progressive disclosure — serves [[pipeline-full-power-simple]] (simple for non-programmers, powerful when needed).
3. **Sources floor — classifiers not all visible.** `/studio/data?dataFloor=sources` — every source should show ITS classifiers (dimensions/code-lists/SDMX classifications); currently hidden → confusion.
4. **Model floor UI too complex/cluttered.** `/studio/data?dataFloor=model` — redistribute more CANONICALLY and BEAUTIFULLY, both functionally and visually. → this IS ADR-051 **DU6** (the "beautiful data UI" polish pass), now owner-confirmed with specifics.
5. **Add popup/modal panels where appropriate** — use popovers/modals for interactions that clutter the inline UI.

6. **Draft → explicit publish (NOT silent auto-save).** 2026-07-20 incident: the persistence fix (`39a32e99`) made workbench edits auto-PUT on every change; the owner experimented on the regional pipelines and the auto-save silently CORRUPTED the stored specs. Owner: "until it stabilizes, don't save what I edit anymore." → shipped an immediate reversible **authoring-hold** (pause persistence, honest "not saving" chip). The real fix = a canonical **draft/dirty → explicit Save/Publish (+ discard/undo)** model like every serious editor (Power Query/Retool apply gestures) — auto-save-on-every-keystroke is wrong for an authoring tool. ALSO exposed (2026-07-20 regional restore): the corruption was ONE field — `config.data_source[regional].datasetCode` flipped REGIONAL_GVA→GDP_ANNUAL, so every `dataSource:"regional"` spec silently read the national cube (25 rows not 110). Restored from git provisioning (`apps/api/provisioning/geostat.provisioning.json`). Two data-integrity guards MISSING: (a) **no version history** on `config.data_source`/`config.data_spec` (destructive UPDATE — one auto-save from unrecoverable; only git saved it; versioning is pages-only); (b) **no PUT validation** that a source's `nonTimeDims`/`classifierDims` are a subset of the datasetCode's real DSD dims (server accepted a `sector` dim on GDP_ANNUAL which has none). Fold draft/publish + versioning + PUT-validation into the persistence/workspace design.

7. **Reference-class VISUAL refresh of chart rendering.** 2026-07-20 the owner shared the live published GDP page (portal :3012) — the "GDP annual dynamics" section = ONE story in 4 coordinated elements (nominal-level bars · per-capita line $ · real-growth YoY diverging bars w/ 2020=−6.3 · unobserved-economy-share bars) + 3 KPI stat cards, per-element chart/table toggle + copy-link + download. Owner: "observe the STRUCTURE — the STRUCTURE is a clean canonical Section⊃Elements⊃metric-binding reference (what the Constructor must reproduce); but the VISUAL is outdated" (flat gray bars, heavy dark theme). → queue a reference-class chart-visual refresh (theme/typography/color/spacing) as craft/DU6. STRUCTURE = good/reference; VISUAL = dated. Also SPOTTED (unprompted, autonomous-cognition): the per-capita chart stops at **2017** while its 3 siblings reach 2025 — a real data/binding GAP to investigate. Published page matches provisioning baseline (the authoring scratch that broke does NOT touch it).

**Owner's explicit license (2026-07-20, restated):** "I may be wrong — what seems good to me may not be the best-standard concept. You are not a slave: reject, look into the foundations, refine, strengthen the idea, bring it to the level of the best platforms/frameworks and the best international standards." → treat these 5 as INPUTS to evaluate critically against reference-class + SDMX/international standards, NOT literal orders; keep the best, refine/strengthen or respectfully push back with reasoning where the owner's instinct isn't the strongest concept. Reinforces [[autonomous-cognition]] + [[architecture-leads-project-migrates]] + principled refusal.

**How to apply:** after Step A/F2 land, run a focused UX/craft design pass (platform-architect + a live walk of these exact URLs) → propose the canonical+beautiful data-workspace redistribution covering all 5, fold #4 into DU6 and open mini-waves for #1/#2/#3/#5. Owner picks. Benchmark reference-class (Power Query/Retool/Grafana/Airtable) for metric catalogs, progressive disclosure, and modal patterns. Part of the [[project_canonical_panel_ia]] trunk; honor the craft bar [[craft-completeness-bar]].
