---
name: pre-action-grounding-gate
description: The recurring root failure — acting BEFORE grounding in the reference/live-truth/whole-target/our-own-capabilities. Mandatory, VISIBLE pre-action gate run before ANY build/fix so the doctrine is EXECUTED, not passively recalled.
metadata:
  type: feedback
---

**The owner's repeated pain (2026-07-02), named precisely:** despite all the doctrine already in memory, the lead keeps failing to be proactive/foresighted — shipping a local/partial answer, the owner catches it, the lead patches. Concrete misses this session: (1) declared the cross-filter "not a code defect, stale bundle" by THEORIZING instead of verifying live; (2) placed the restored `structural` donut SOLO though the reference (`scriness/img_2`) shows it PAIRED beside income; (3) built AR-36 P0 (the runtime pivot) then was about to HARDCODE `img_5` instead of USING what we just built (maximal-adoption) — the owner had to connect it.

**The single ROOT cause:** the lead works FORWARD from the immediate task and its own reasoning, instead of first ANCHORING to ground truth — the visual reference (`scriness/`), the live/real behavior, the COMPLETE target, and OUR ALREADY-BUILT capabilities — and reasoning BACKWARD from the correct end-state. The doctrine ([[converge-maximal-target-first]], [[reference-result-not-impl]], [[maximal-adoption-doctrine]], [[verify-the-purpose-not-the-proxy]], [[elevate-dont-patch-proactive-design]]) was held PASSIVELY (recalled after the fact) instead of EXECUTED as a pre-condition. Needing the owner to catch the miss IS the failure.

**The fix = a MANDATORY, VISIBLE PRE-ACTION GATE.** Before starting ANY non-trivial build/fix/design (esp. UI/render/feature work), the lead runs and SHOWS this 5-point gate FIRST — no editing/routing until it's done:
1. **GROUND in the reference + live truth.** LOOK at the relevant `scriness/` shot(s) and the actual live/current state — never reason from code alone. The screenshot is the RESULT to match; read it.
2. **State the WHOLE correct target** — all states/pages/modes of the end-state (e.g. regional State A AND State B), not just the immediate slice. Converge the maximal target before touching anything.
3. **USE WHAT WE ALREADY BUILT.** Check the ARCHITECTURE-REGISTRY + existing capabilities — adopt them fully (a pivot/events-grammar/registry we shipped), never hardcode a one-off where our own mechanism fits. "What we build, we use maximally."
4. **WHO + HOW.** Route judgment to the right agent (not the lead typing); pick the method that fits (root-cause-first for a defect, real-browser for UI, prep-then-delegate).
5. **VERIFY against the reference, not a proxy.** Done = the live experience matches the reference shot, read with my own eyes — never green-tests/agent-✓ alone.

**How to apply:** this gate is the FIRST action, executed visibly, every time — turning the passive memory into an active pre-condition. If I skip it and ship forward-from-the-task, that is the exact recurrence to catch in myself. Companion + operationalization of all the doctrines above; this is the "why do I hold it in memory yet not do it" answer: because it wasn't a GATE. Now it is.
