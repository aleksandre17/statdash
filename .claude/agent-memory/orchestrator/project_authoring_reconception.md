---
name: authoring-reconception
description: Owner-mandated holistic reconception of the panel/authoring platform — vision-first, market-differentiating, existing code adapts to the new concept
metadata:
  type: project
---

Owner launched (2026-07-09) a bold, holistic reconception of the authoring platform (`apps/panel` + config/plugins/styles): UI **and** functionality **and** conceptual model **and** architecture — one initiative, not three. Frame: study the best JSON/declarative-rendering platforms, extract concepts, and build "something that does not yet exist on the market — powerful yet simple, for non-programmers."

**Why:** Owner wants a market-leading, differentiated Constructor, not incremental gap-patching. Explicitly asked the lead to be innovator/leader/ideologue.

**How to apply:**
- **Vision leads, existing adapts** (Law 7 / Strangler-Fig) — do NOT constrain the concept to fit current code; reshape code to the concept. Owner said this directly.
- Owner **dislikes the current 3-step Data→Site→Pages wizard** ("doesn't fit") — the authoring paradigm is open for total reconception, not tweaking.
- Open to adopting best-in-class packages IF they strengthen **and** simplify simultaneously (his central tension: power + simplicity).
- Working assumptions in play: single-tenant-first (Geostat) with MT seam preserved not built ([[project_mt_deferred]]); brand/visual identity is ours to propose.
- Sequence: vision/concept proposal (platform-architect, Opus) → owner sign-off → detailed architecture → phased Strangler-Fig build. Authoritative artifacts: `docs/architecture/proposals/SPEC-authoring-reconception-vision.md` (AR-49) + `ARCHITECTURE-REGISTRY.md`. Recon confirmed `apps/panel` is already a mature ~19k-LOC schema-driven builder — this is refactor+reconceive, not greenfield.

**STATUS (2026-07-09): direction LOCKED.** Owner explicitly delegated all reversible technical decisions to the lead ("you are the leader now"). Lead DECIDED: (1) vision APPROVED — metric-first "Governed Canvas" + Model/Compose role split (the 3-step wizard's root flaw = role-conflation, not step count); (2) first milestone = **M0: semantic layer + metric palette** (complete AR-40 → browsable governed metric catalog + additive `metric-ref` PropSchema + bind-to-metric affordance; additive/reversible, breaks nothing); (3) brand **"Strata"** adopted provisionally (rebrand = token preset = data not code, so trivially changeable — not gating). Key law guard: grow OUR SDMX/OLAP-native semantic layer, REFUSE Cube/Malloy as a runtime (breaks Law 5 fromSDMX-only + Law 2).

**M0 BUILT (2026-07-09)** — all 11 items shipped additively across 6 parallel agents in 3 waves + a root-cause fix; chief-engineer verdict SHIP-WITH-FOLLOWUPS; converged gate green (full 2675 tests, 0 fail; core 714 / react 501 / geostat-render 127; lint 0; tsc clean). Delivered: `DimensionDef` catalog (peer of `MetricDef`), `metric-ref`/`dimension-ref` as additive `enum-ref source:'metrics'/'dimensions'` (no new PropFieldType), Metric Palette + bind, provisioning seed (6 dims, 17 metrics), chart `data.query.measure` picker, FF-BIND-PARITY. Root-cause fix: KPI read now honors metric default-dims via shared `mergeMetricDims` (chart≡KPI). Committed to a feature branch (NOT pushed — awaiting owner). **Open follow-ups** (in AR-49 registry Next): (a) KPI inline per-item metric-ref needs core `itemSchema`; (b) `dimension-ref` needs a `resolveDimensionRef` lowering seam before shipping a picker; (c) retire panel `CatalogDimension` mirror → engine `DimensionDef`; (d) section-fed chart picker (M1). **Next milestone M1:** dissolve the 3-step wizard + retire react-admin — designed as "The Studio" (`SPEC-authoring-reconception-M1.md`; canvas-always-open + summonable icon-rail, role-as-lens; 4 sub-milestones M1.1 retire-react-admin → M1.2 shell → M1.3 re-home+delete-wizard → M1.4 Strata skin). Deferred (out of M0): brand "Strata" theme-editor build.

**M0 LIVE-BOOT DEFECTS (found 2026-07-09 via owner's "see it live") — FIXED.** M0 passed all tests + QC but was non-functional live: (A) panel had no metric-registration boot seam, (B) `main.tsx` missing `i18next.init()`. Fixed at root cause (bootstrapCatalog + initPanelI18n SSOT) + guards. Lesson: [[panel-live-boot-verification]].

**M1 "The Studio" BUILT (2026-07-09) — owner authorized ("გააგრძელეთ").** All 4 sub-milestones + hardening shipped additively, each committed on `feat/ar49-m0-metric-first-authoring`: M1.1 react-admin retired · M1.2 Studio shell (flag) · M1.3a parity (extract-and-delegate) + M1.3b wizard DELETED (Studio default) · M1.4 writable theme editor + Strata skin (found+fixed themeOverrides = dead data). Then closed the recurring M0-class boot seam: `setupCanvasRegistry`→explicit boot step (fixed latent empty-site bug) + `bootComposition` e2e test (RED if await dropped). chief-engineer verdict SHIP-WITH-FOLLOWUPS; green (panel 455 / core 714 / geostat-render 127).
**Branch has 7 commits, NOT pushed** — awaiting owner on push/PR. **Deploy-time live verification pending** (no Docker locally; verify at deploy like the render-parity epic did on prod). Owner brand calls open: confirm "Strata" wordmark + hex palette (retunable live). **M2 BUILT (2026-07-09, owner-authorized "გააგრძელეთ")** — Model mode + Steward role + in-tool metric authoring. M2.0 role lens (useRole, author|steward, default author, swappable auth seam, NOT a security boundary) · M2.1 modeler relocated to steward (author Data = palette-only, FF-AUTHOR-NO-QUERY) · M2.2 Metric Editor → saveSemanticCatalog → PUT /api/config/site → live palette (steward.e2e proven in Chromium); +agg/description on the wire (HONEST: agg governance-only, no consumer yet — editor carries an honesty caption). Lead one-way-door calls: blob-now / local-role-pref / calc-deferred / provisioning per-id-merge (steward metrics survive re-provision). chief-engineer M2 verdict SHIP-WITH-FOLLOWUPS; gate 2826/0.
**Playwright+axe ADOPTED** (the top package-eval finding — fixes the green≠works class): real-browser e2e; already caught 3 live-only defects (chrome-less crash, kpi spec-less throw, non-idempotent hydrate) all root-cause-fixed. Offline run needs a gitignored bridge — see [[panel-live-boot-verification]]. TanStack Query DEFERRED (lead call). Package eval verdict: build-our-own was right almost everywhere; only Playwright worth adopting now.
**Branch: many commits (M0+M1+M2+fixes), NOT pushed** — owner deferred deploy, said keep working. Open follow-ups (in AR-49 registry): agg-consumer, live-delete unregister seam, App.tsx mock idempotency, server-side PUT validator, calc editor (M2.5), dimension authoring (M2.4). **AR-49 LIVE-VERIFIED (2026-07-09)** on the isolated staging twin `statdash-stg` (192.168.1.199, ports pg 5457/api 3007/panel 3008/geostat 3009) — PROD UNTOUCHED (proven: prod containers/images/ports intact). Verified live on real api+Postgres: governed catalog serves (17 metrics/6 dims), steward authoring persists to Postgres + survives reload, re-provision per-id merge preserves steward metrics. Panel DOM not browser-rendered in deploy env (alpine-chrome) — owner can open :3008. Live build caught + fixed a real prod blocker: `pnpm-lock.yaml` drift for @playwright/test (regenerated, committed). Staging left running for owner to click; teardown `docker-compose -f docker-compose.staging.yml -p statdash-stg down`. Deploy = Model A (server git-clone at /tmp/statdash-build tracks main; fetch branch by name + checkout -B FETCH_HEAD; server builds images; api provisioning JSON baked into image → rebuild api no-cache). Staging compose has a pre-existing flyway `-target` bug (dies at V33) — worked around in throwaway.

**M3 (non-programmer data pipeline) DESIGNED** (`SPEC-authoring-reconception-M3-pipeline.md`): Metric Recipes over the live `MetricDef.calc` engine; honest boundary — derive-meaning = non-programmer, define-raw-data (SDMX/DSD) = stays expert. **M3.0 calc/derived-metric editor BUILT** (visual measure-algebra, in-house expr-tree not Blockly, cycle-guarded, live-compute proven; apps-only). **M3 REMAINING AWAITS OWNER ONE-WAY-DOORS**: (1) growth-as-a-governed-noun = the ONE engine change (relative-time coordinate, packages/core+expr+contracts) — gated; (2) recipe library placement panel-vs-plugins; (3) bless the honesty boundary as doctrine; (4) in-house expr-tree confirmed. **Everything committed on `feat/ar49-m0-metric-first-authoring` + pushed to origin (NOT merged to main, prod untouched).** Natural stopping point for autonomous build — M3.1 Recipes + M3.2 growth need owner calls.
