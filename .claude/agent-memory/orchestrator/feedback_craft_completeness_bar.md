---
name: craft-completeness-bar
description: Every surface must clear the reference-class CRAFT + completeness bar, not just architectural correctness. See and close the thousand small gaps proactively, unprompted.
metadata:
  type: feedback
---

Hold every surface to the reference-class craft + completeness bar. A surface is NOT "done" when it is architecturally correct — only when it is ALSO craft-complete: beautiful, complete, nothing raw/weak/ugly/half. SEE and CLOSE polish/UX/completeness/data-rendering gaps **proactively, unprompted** — the owner must never have to enumerate "the thousand small things."

**Trigger (put this in your own head so the owner never writes it again):** *"Would the RIGHT reference-class leader for THIS concern ship this as-is? If not, it's unfinished — close the gap without being asked."* Craft is part of DONE, not polish-later.

**Benchmark PER CONCERN (owner correction 2026-07-15 — one generic set is wrong):** feel/polish/craft → Figma · Linear · Stripe · Vercel · visual-builder/authoring → Builder.io · Framer · Webflow · dashboard/data-viz/panel-plugin → Grafana · Looker · Observable · framework/DI/composition → Angular · Nest · Backstage · UI primitives/a11y/ownable-craft → **Radix · React Aria · Headless UI**. The last one is load-bearing: **MUI is likely the craft CEILING + the agnosticism debt** (audit: 82/228 MUI-locked); Radix headless + the DTCG token spine = full craft control + ownable + a11y + tenant-agnostic. MUI→Radix exit is roadmap-sanctioned (Stage 3 P-SCALE) — revisit the audit's "flag-don't-fix" if craft becomes the priority.

**Why:** owner (2026-07-15, exhausted, repeatedly "you implement nothing / no result"). The lead operated at ARCHITECTURE altitude (composition roots, ADRs, blueprints) while the actual panel SURFACES were weak at the craft level — raw statistical data rendered ugly, no JSON/data-grid viewer, blank chart boxes, unstyled raw object dumps in the inspector (`presentation.crumbs · 6 fields · by · op`). Owner gave ONE example ("at minimum you could have brought a JSON-rendering package so raw data looks nice") of a thousand, then asked "what should I put in your head so you SEE these yourself?" The gap = the world-class bar was applied only at the architecture level, never at the tangible surface/craft level.

**How to apply:**
- Every re-lay / build step's DoD now INCLUDES craft-complete (reference-class polish + completeness), not just canonical/gate-green. Bake it into every agent brief.
- Proactively HUNT craft gaps: data rendering (proper data grid — TanStack/AG-Grid class; structured/tree viewers for objects, never raw text), designed empty states, loading skeletons, visual hierarchy, spacing, micro-interactions, honest-but-beautiful states.
- Bring REFERENCE-CLASS solutions (a real library where idiomatic), not hand-rolled weak versions. This is the [[autonomous_cognition]] directive applied at the SURFACE, and the surface half of [[full_ownership_reference_grade]].
- The bar is BEST, not good: raw/weak/ugly on any surface a user touches = unfinished, fix it.
