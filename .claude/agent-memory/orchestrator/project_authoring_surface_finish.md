---
name: authoring-surface-finish
description: Owner 2026-07-23 morning — finish the authoring surface FIRST (best UI, simple+most-powerful, non-professional-ready); lead's personal pen, specialists as knowledge input; = ONE-PIPE U4+E2a arc
metadata:
  type: project
---
Owner directive 2026-07-23 (morning after ONE-PIPE U1/U3 fired): **finish the authoring surface first** — «საუკეთესო UI, მარტივი და უძლიერესი», so a NON-professional can realize their ideas; built on international concepts, canonical architecture, patterns, principles. **The lead works on this personally** (design pen — same precedent as the ONE-PIPE design doc), *using the other agents' knowledge* as input.

**Why:** ONE-PIPE made the grammar one at rest; the authoring surface is now the visible half of the unification — the owner wants the payoff surface completed before anything else. Trigger question was whether the "გაფართოებული / დაუმუშავებელი" (Advanced/raw) disclosure survives — lead's answer (accepted by moving straight to "let's finish it"): the escape hatch stays as D8's second zoom; content re-maps in U4 (query-branch dead post-U3 → reattach via simple-view projection; SpecTypePicker becomes projection-chooser; rename "raw"→"canonical").

**Scope =** ONE-PIPE U4 (D4 resolver contract · D8 simple views · D2 registry docs) + E2a step-builder UX, FOLDING IN [[data-workspace-ux-backlog]] items #2 (basic ops ON the element via popover), #5 (popovers/modals de-clutter), #6 (draft→explicit publish + versioning + PUT validation — auto-save corruption incident). Three-zoom target: element popover (basic ops) → workbench step-builder (7 verbs) → canonical JSON (now honest: shows exactly the stored artifact). Capability-parity gate BINDING before any branch retires (QuerySpecEditor Advanced enumeration = the checklist).

**How to apply:** this is the active WIP=1 card-0104 arc. Design WRITTEN 2026-07-23: `docs/architecture/proposals/DESIGN-authoring-surface-three-zooms.md` (Z1–Z9, W0–W5) — three zooms over one artifact; W0 = one-dialect hygiene FIRST (recon surprise: `buildSuggestedSpec.ts:44` emits `type:'query'`, normalize-on-write unbuilt — sugar re-entered the corpus post-U3). Backlog #6 turned out ALREADY SHIPPED (E0/ADR-052 lifecycle walked 8/8) — design adds NO new persistence. Parity checklist (recon §2, on the card) = the W3 retire gate. Owner picks, then waves. FF-SIMPLE-VIEW-ROUNDTRIP pins D8.
