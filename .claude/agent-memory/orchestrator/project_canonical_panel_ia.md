---
name: canonical-panel-ia
description: Card 0102 — owner-mandated whole-panel circle-break: derive THE canonical site-builder object-model spine (Site⊃Page⊃Skeleton/Template⊃Section⊃Element⊃Data) to end the panel's parallel-concept chaos; the new trunk
metadata:
  type: project
---
2026-07-19 — the owner escalated past all tactical work: "whatever we do, we've fallen out of the CANON; the panel is CHAOS." He gave his own hierarchy — (1) DATA upload+view (isolated, beautiful UI) · (2) SITE create + site params/canonicity · (3) PAGE create+assemble + CHROME (unordered) + the **page SKELETON/FRAME "which GOT LOST"** · (5) rest = logical continuations — admitted he may be wrong, and mandated: study ALL reference site-builder CORES deeply (visions/UI/structures/concepts/methods/architectures), lay out & set up a REMEDY, "show me we really resemble a site-builder platform + a framework core base."

**Lead hypothesis (offered):** the panel grew PARALLEL concept-surfaces (Sources/Model · Insert/Layers · Site/Pages · Style/chrome) with NO shared spine; the reference class runs ONE nested containment — **Site ⊃ Page ⊃ Skeleton/Template (chrome regions) ⊃ Section ⊃ Element ⊃ Data-binding** — and our LOST first-class concept is the **page-skeleton/template**, root of the chaos + of "I can't assemble a site" (0101 is its symptom).

**Action:** apex READ-ONLY study `platform-architect@fable` → `docs/architecture/proposals/STUDY-canonical-panel-ia.md`: benchmark Webflow/Framer/Builder.io/WordPress-Gutenberg/Plasmic/Puck/Wix/Retool/Grafana/Form.io concept-SETS · map our actual concepts (file:line) · verdict (mis-factored/conflated/LOST) · propose the canonical spine EXTENDING ADR-038/041/042/049 (no fifth grammar) · Strangler remedy sequence where 0100-P2b + 0101 fit. Owner picks → roadmap re-sequences.

**Why it matters / how to apply:** this is now the TRUNK — it SUPERSEDES the framing of [[assembly-by-declaration]] (0100 = the DATA-binding axis, now one element of the bigger IA) and absorbs 0101. It's the definitive answer to the standing framework-grade dissatisfaction [[framework-grade-verdict]]. This IS the circle-break pattern [[circle-break-root-study]] at whole-panel-IA scale. Do NOT let it collapse back into leaf work; keep the canonical spine driving. Card: `work/items/0102-canonical-panel-ia.md`.

**2026-07-20 — DATA-AXIS circle-break (owner: "one concept, two independent sources; not ready in any aspect; we move too slowly").** platform-architect study `docs/architecture/proposals/STUDY-canonical-panel-ia.md` PROVED the data fork: "working with data" = an archipelago of Sources(`/studio/sources`)+Model(`/studio/model`) as two peer destinations bridged by a teleport (`store/sourcesHandoff.ts` — the likely FREEZE cause) · TWO editors for one DataSpec (the good `DataWorkbench` + the legacy 8-discriminant `DataSpecEditor` mounted as "Raw editor (advanced)" in `DataModelingPanel.tsx:229` & `DataFacetField.tsx:199`) · TWO residences (inline `CanvasNode.props.data` vs named `NamedDataSpec[]`, which can't reference each other). Root: the **0082 GRAMMAR is done** (pipeline spine, source=step 0) but **SURFACE + RESIDENCE never caught up**. Reference class (Power Query/Retool/Grafana): ONE data object, ONE editor, source=step 0, elements reference by name. **Owner blessed FULL autonomy → "build the BEST UI/platform, research the reference class, go faster."** Direction: **Option A first — "One Data Workspace, source is step 0; retire the duplicate raw editor + Sources/Model teleport"** (surface unification, engine untouched, Strangler); **Option B later — elements reference a named pipeline** (residence unification, needs its own ADR). platform-architect authoring the ADR + reference-class UI model + Strangler wave plan. Non-pipeline kinds (timeseries/growth/row-list) only have the raw editor → can't delete it until they fold. **How to apply:** drive A wave-by-wave, WIP=1, each live-J-walked; don't reopen the settled 0082 grammar; keep this on the 0102 trunk.

**2026-07-20 — capability + simplicity + library study (platform-architect, feeds [[pipeline-full-power-simple]]).** VERDICT: pipeline already RESOLVES nearly everything; the authoring surface SURPASSES Power Query/Grafana/Tableau Prep on the perception model and holds a unique quadrant (governed × simple × no-query-language). "Does everything, simply" is closer than the wave plan implied — blocked by a small precise set: **GAP-1 (load-bearing) = the missing value-cell `source` variant** — the ONE engine primitive that folds `timeseries`/`growth`/`ratio-list`/`row-list` onto the spine byte-identically; it is the PRECONDITION of DU4 (→ new wave **DU4a**, engine/architect-owned) and belongs as **ADR-046 Addendum 4** (architect to author; `desugar.ts:180-190` already escalates it). **GAP-2** (those kinds authored only in legacy editor) auto-closes once GAP-1 lands. **GAP-3** = `lookup.from`/`join.with`/`blend.from` are raw-JSON textareas → independent DU4 P-OFFER task via a new `role:'source-ref'` picker. Simplicity doctrine = 7 rules R-S1…R-S7 (each a fitness fn), 4 live violations mapped to waves. **LIBRARY decision: adopt exactly ONE — TanStack Table + Virtual (headless), scoped to the Floor-1 raw-data grid, YAGNI-gated on a real >1k-row consumer; BUILD/keep everything else** (workbench, verb palette, ExprAutocompleteInput all beat off-the-shelf; adopting their runtimes would break Law 2/4/9). ADRs PROPOSED not-yet-written: ADR-046 Add.4 · annotate ADR-051 (DU4a + GAP-3) · lightweight TanStack adoption ADR. **How to apply:** these are QUEUED for when DU1→DU3 land — author ADR-046 Add.4 + DU4a design when we approach DU4 (WIP=1, don't fan out now); ADR-051 itself needs no amendment.
