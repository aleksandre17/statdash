---
name: live-deploy-mechanism
description: How the LIVE 192.168.1.199 demo (geostat :3002 / panel :3003 / api) is actually deployed — server git clone + prod-only compose, diverges from RUNBOOK
metadata:
  type: project
---

The ACTUAL live-demo deploy path (verified 2026-06-28 by container `com.docker.compose.project.*` labels), which DIVERGES from `ops/RUNBOOK.md` §B and is NOT the per-module geostat-kit drivers.

**Why:** the runbook B.3/B.4 (`-f docker-compose.yml -f docker-compose.prod.yml`, `geostat api deploy --prod`) is stale doc drift. The kit's node-api (rsync) / node-vite (tar) drivers ship the LOCAL tree; the live stack does NOT use them.

**FAST-ITERATE HOT-SWAP (no rebuild — proven 2026-07-03 ~16:22 UTC, for FRONTEND-ONLY geostat SPA fixes):** breaks the slow 3-image 15-min cycle. (1) Build dist LOCALLY vite-ONLY (never `tsc -b`, matches the Dockerfile builder stage) with the SAME baked args as prod — `cd platform && VITE_API_URL="" VITE_API_STATS_URL="" pnpm --filter ./apps/geostat exec vite build` (empty urls = relative /api single-origin; nginx proxies /api/→statdash-api:3001; a non-empty url would bake a wrong origin). ~7s. (2) `tar -czf … .` the `platform/apps/geostat/dist` CONTENTS → `scp -F ops/config/ssh/config … geostat-deploy:/tmp/geostat-dist.tgz` → extract → `docker cp /tmp/geostat-dist/. statdash-geostat:/usr/share/nginx/html`. Web root = `/usr/share/nginx/html` (index.html + assets/ + data/georgia-regions.geojson). NO container restart needed (nginx serves the new index+hashed assets immediately; index.html isn't cached). BACKUP first: `docker cp statdash-geostat:/usr/share/nginx/html /tmp/geostat-html-backup-<ts>`. `:latest` image is UNTOUCHED → a `docker restart`/recreate REVERTS the hot-swap (low-risk, ephemeral). Verify the REAL bundle loaded: `curl :3002/` must show the new `index-<hash>.js` AND a Playwright `response` listener must confirm the browser fetched that exact hash 200 (guards against the 4-prior-proxy false-reads). Smoke: `:3002`/=200, new asset=200, `:3002/api/bootstrap`=200, api `/health`=ok. ARGS/paths NOTE: `ops/` is at REPO ROOT not under `platform/`; threads reset cwd → use ABSOLUTE ssh-config path.

**MAP-BLANK FIX STATUS — 5th FAILURE (2026-07-03 ~16:23 UTC), pivot to declarative-choropleth rewrite:** branch `fix/map-reproject-on-show` @ **4d177a2** (frontend-only, GeoMap.tsx `invalidateSize` + deferred `fitBounds(dataBounds)` on hidden→shown; only GeoMap.tsx + 1 test vs b9503aa) hot-swapped via the path above (bundle `index-Cj_GVGO5.js`, browser-confirmed loaded 200). Live-verified `/ka/regional` composition/map panel: **STILL HARD-BLANKS** on select-region-while-map-HIDDEN(table view)→toggle-back-to-map — real=0/degen=13 all `d="M0 0"`, overlayBBox 0×0, container valid 537×327, NO crash (visibility-gate holds), 0 pageerrors, NO self-heal @2.5/5s, across 3 iterations (region param multi-adds correctly R11→R11,R9→R11,R9,R6). CONTROL (plain toggle, no select) = 13 real, bbox 513×261, occupied-red `#dc2626`×2 present. Shot `platform/work/verify-shots/map-reproject/01-select-while-hidden-toggle.png` = blank white panel w/ only title+toggle. `invalidateSize`+deferred-`fitBounds` does NOT re-seed the projection for a layer that was mounted/restyled while 0×0 — same root as the prior 4 attempts (RepairOnShow, setStyle-in-place, visibility-gate). The imperative Leaflet-mutation-while-hidden approach is exhausted; the owner is pivoting to a DECLARATIVE choropleth rewrite. Verify script `platform/work/verify-map-reproject.mjs`.

**🔴 CRITICAL LANDMINE — prod postgres data is on the EPHEMERAL container layer (discovered 2026-07-03 ~17:00 UTC, caused a data-loss incident):** the `statdash-postgres` container is `timescale/timescaledb-ha` (Spilo), whose real `PGDATA=/home/postgres/pgdata/data`. But `docker-compose.prod.yml` mounts the named volume `statdash-prod-pgdata` at the WRONG path `/var/lib/postgresql/data` — so the named volume holds NOTHING; the live DB (2353 obs, flyway v38) lives in the container's WRITABLE LAYER. It survives `restart`/`stop`/`up --build` (same container) but is **DESTROYED by `docker rm`/`up -d --force-recreate`/recreate of the postgres service**. Prior deploys never recreated postgres (they used `up -d --build` w/o `--force-recreate`, or `--no-deps <svc>`), so it survived by luck. A brief-directed `up -d --force-recreate` (this session) recreated postgres → fresh initdb → flyway re-ran V1→V32 and FAILED at V33 (`expected >=11 regions parented to _T, found 0`), blocking the whole compose-up (api/ingest/geostat/panel gate on flyway `service_completed_successfully`). RECOVERY (proven this session): the pre-deploy `pg_dump -Fc` backup restored everything — `docker exec -i statdash-postgres pg_restore --no-owner --no-acl -d "$DBURL"` after `psql -c "CREATE EXTENSION IF NOT EXISTS timescaledb; SELECT timescaledb_pre_restore();"` then `SELECT timescaledb_post_restore();` → 2353 obs, flyway v38 (with V33 already applied → flyway returns to no-op), regional page_version re-provisioned on api boot. NOTE: `pg_restore --data-only | grep '\t'` shows 0 rows for timescaledb hypertables (data is in compressed chunks) — VERIFY a backup by RESTORING into a throwaway db and `SELECT count(*)`, not by streaming. RULES GOING FORWARD: (1) **NEVER `up -d --force-recreate` the whole stack** — it recreates postgres and wipes the DB. Use `up -d --no-deps --force-recreate <geostat|panel|statdash-api>` to redeploy app images without touching postgres. (2) ALWAYS `pg_dump` backup BEFORE any postgres-touching op (the backup is the ONLY recovery). (3) The real fix (owed, needs lead sign-off): change the compose postgres mount to `statdash-prod-pgdata:/home/postgres/pgdata` (the actual PGDATA parent) so data persists across recreate — currently a committed-infra one-way-door, flagged not done.

**🟠 SECOND LANDMINE — prod DISK fills to 100% and a no-cache 3-image build is what tips it (hit 2026-07-03 ~16:04 UTC, i18n deploy):** `/dev/sda2` is 147G. A genuine `--no-cache` build of all 3 app images creates a burst of dangling intermediate layers + build cache; the server was already ~94% full, so mid-deploy it hit **100% (0 avail)** — `pg_dump`/`cat >/tmp` fail "No space left on device" and postgres WAL is at risk (it stayed writable this time by luck). The deploy itself still SUCCEEDED (build had just-enough space, containers recreated healthy). REMEDY (safe, reversible, done this session — restored 100%→67%, freed ~50G): `docker builder prune -f` (Build Cache was 79GB with **0 active** — always safe to drop) + `docker image prune -f` (**dangling/untagged ONLY — NEVER `-a`**, which removes the `:rollback` tagged images since they're not attached to a container; NEVER `--volumes`, which would touch the DB volume). Do this AFTER the deploy is verified (keeps the just-built layers). GOING FORWARD: check `df -h /` BEFORE building; if <15G free, prune build cache first. The real fix (owed): a cron `docker builder prune` or a build-cache size cap.

**CURRENT STATE (2026-07-03 ~18:25 UTC): DATA INGEST of `main` @ `081d796` COMPLETE + anchor-verified (data-refresh: GDP deflator + REGIONAL revision/2024). NO image rebuild.** This was a DATA-only ingest into the EXISTING running postgres (NOT a code/image deploy). Clone `/tmp/statdash-build` fetched+reset to **081d796** (was 4727c76); the delta touched `DATA/canonical/{GDP_ANNUAL,REGIONAL_GVA}.xlsx` + `ops/scripts/ingest-canonical.sh` anchors (399/415/1665) + `DATA/canonical/FEATURED.json`. The `ingest` one-shot BIND-MOUNTS `../../DATA/canonical:/canonical:ro` + the driver script from the clone (NOT baked) → a git checkout on the clone makes the new canonical+script live, no rebuild. **BACKUP (restore-verified, mandatory — this ingest MUTATES gold):** `/tmp/statdash-prod-backup-20260703-221933.dump` (811,549B, 879 TOC); verified by RESTORING into a throwaway db `statdash_backup_verify` (timescaledb pre/post_restore + `pg_restore -U statdash --no-owner --no-acl`; NOTE: MUST pass `-U statdash` or pg_restore defaults to nonexistent role `postgres`) → 2353 obs (384/415/1554) clean, then dropped. **POSTGRES UNTOUCHED:** id `449fe998eaa5` IDENTICAL before/after (ran ingest via `docker-compose run --rm ingest` / isolated `docker run` on statdash-net — only `depends_on api-healthy`, never recreates pg). flyway max=38 unchanged (no migration — deflator is a new CL_MEASURE MEMBER, not a new dim; GDP 4-dim DSD already fits). **CONVERGED-409 GOTCHA HIT (as documented):** the sanctioned driver `run --rm ingest` aborted (`set -e`/`curl -f` on 409) at GDP_ANNUAL **codelists** publish — the codelists job auto-CONVERGED to `published` (even WITH the new deflator member: SCD-2 upsert during staging → publish is a no-op → explicit POST publish = 409) → left GDP **facts** `b522704f` STAGED-unpublished. RECOVERY (clean, no orphan submissions): probed the 2 GDP jobs (codelists=published, facts=staged), then ran a **409-tolerant driver** (`scratchpad/tolerant-ingest.sh`) that (1) published the already-staged GDP facts `b522704f` DIRECTLY (no re-upload → no duplicate submission), (2) ACCOUNTS upload → 409 whole-dataset skip (unchanged), (3) REGIONAL upload 202 → codelists converged(409-tolerated)+facts published. Tolerant `publish_job`: check-if-already-published → poll staged → POST publish treating 409 as converged-success → poll published. **ANCHORS ALL GREEN (the acceptance):** served counts GDP_ANNUAL=**399** / ACCOUNTS_SEQUENCE=**415** / REGIONAL_GVA=**1665** (in-DB match; total 2353→**2479**, +15 GDP deflator +111 REGIONAL new-2024 — the 2010-15 REVISION is SCD-2 in-place so adds 0 to count); `gdp-deflator` measure = **15 obs (14 A + 1 P)**, 2025 obs_status=**P** (val 4.6366); GDP 2010 total = **22148.65** (unchanged); REGIONAL `_T/_T` 2010 GVA = **22148.65** (REVISED, now reconciles with GDP 2010). Smoke green (:3002/:3003=200, /api/bootstrap=200, /health=ok). Disk 92G free (35%), no prune needed (ingest = no build). **GOTCHA for next time:** serve queries use `limit`≤10000 (limit=20000 → HTTP 400, API caps it). Prior-live code state (4727c76 images) UNCHANGED — this ingest shipped no new bundle. (Prior FRONTEND state below.)

**PRIOR STATE (2026-07-03 ~17:58 UTC): `main` @ `4727c76` DEPLOYED + live-DISPLAY verified (FRONTEND-ONLY relocalize + UI-polish batch).** Prior-live was **`0bba4bb`**. Delta `0bba4bb..4727c76` = 16 files, packages/react + packages/plugins ONLY (NO provisioning/api/migration/DATA). **geostat+panel rebuilt ONLY** genuine no-cache `DOCKER_BUILDKIT=0` (api NOT rebuilt/re-provisioned — zero provisioning delta): new geostat=**127fce5e3122**, panel=**63332271093d**; api UNCHANGED **cc807fc27837**. **`up -d --no-deps --force-recreate geostat panel`** only — postgres **449fe998eaa5** id IDENTICAL before/after (Up 4h→5h), api e7bf9e64cf39 not recreated. Anchors GDP=384/ACCOUNTS=415/REGIONAL=1554, total obs **2353**, flyway untouched. Smoke green; served bundle **index-9gW3fvto.js** (browser-confirmed loaded + re-localized). Disk 93G→92G free (35%, no prune needed — 2-image build ~1G). **Rollback armed:** backup `/tmp/statdash-prod-backup-20260703-174617.dump` (811,549B, 90 TABLE DATA, harmless circular-FK warn), `:rollback` RE-TAGGED to true prior-live 0bba4bb (api=**cc807fc27837**/geostat=**2a3c7119893e**/panel=**bc1eb1f5097b** — were stale ecddc17-era 4327d0179889/3914b45585d0/4722645e1bc9), git tag `pre-relocalize-deploy`=**0bba4bb** pushed from local. **LIVE-DISPLAY VERIFIED (Playwright→:3002, shots platform/work/verify-shots/relocalize/, scripts platform/work/verify-relocalize*.mjs):** (1) HEADLINE RELOCALIZE-ON-CLIENT-TOGGLE **FIXED** — on `/ka/regional`, click ENG (`.locale-switcher__btn` labels `ქარ`/`ENG`) with a window sentinel proving NO reload: map region aria-label (`აჭარის ა.რ.`→`Adjara A.R.`), KPI label (`მთლიანი დამატებული ღირებულება`→`Gross Value Added`), KPI unit (`მლნ ₾`→`GEL mn`), KPI subtext (`წინა წელთან შედარებით`→`compared to previous year`), chart legend + category (region) labels ALL flip ka→en IN PLACE. This was THE bug (data-derived label cache now keyed on locale). Before/after shots 01a/01b. (2) perspective-tab-group flush LEFT (groupLeft==parentLeft 292, align-self flex-start, justify normal). (3) KPI-strip ONE freshness badge above grid (`წინასწარი მონაცემები`), 0 per-card P pills, 3 cards, ka+dark. (4) section header order link·info·preliminary·toggle (toggle LAST, verified DOM child order + GDP shot). (6) hero title ONE line (`white-space:nowrap`, height==lineHeight 58). (7) non-reg: map 13/13 real paths 0 degenerate, income treemap 5 tiles renders, `/en/regional` route-load 0 ka-leak htmlLang=en. **🟡 ITEM 5 (range from→to reading) NOT SATISFIED ON LIVE — honest residual, NOT a deploy fault:** the RangeShell from→to template (`filter-control__range` + lead/mid/trail `filter-range-word` from `range` i18n ns) SHIPPED in the bundle, but NO live geostat page uses a `type:"range"` filter param (`grep '"type":"range"'` = 0 hits). accounts/gdp `დინამიკა` mode renders two bare `type:"select"` year dropdowns (fromYear/toYear) whose `suffix` config (`-დან`/`-მდე`/`from`/`to`, 6 occurrences) is DEAD — no source (filter-bar/controls/react) renders `suffix`. So dynamics shows bare year selects with NO connector words (shot 05a-range-accounts-ka.png = `2025`·`ყველა`·`2010`, no დან/მდე). Achieving the brief's reading needs a PROVISIONING change (switch fromYear/toYear to a single `type:range` param, or wire suffix rendering) — which THIS brief explicitly forbade. RangeShell code is correct but unwired. The 3 `getComputedStyle` pageerrors on nav are the pre-existing documented ApexCharts-teardown, non-breaking. (Prior-live before this: `0bba4bb`, deployed ~16:49 UTC — details below.)

**PRIOR STATE (2026-07-03 ~16:49 UTC): `main` @ `0bba4bb` DEPLOYED + live-DISPLAY verified (UI batch: income treemap markers, sidebar state redesign, perspective-tab-group left, low-cardinality bar px-cap).** Prior-live was **`ecddc17`**. Delta `ecddc17..0bba4bb` = 10 files, 3-app-image scope: `geostat.provisioning.json` (income node title + treemap derive) → API rebuild + re-provision; `packages/charts/special.ts` (treemap markers) + `packages/plugins` (inner-sidebar.css, perspective-bar.css, TreemapChart.tsx, apex/base.ts+cartesian.ts+hbar-diverging.ts) → geostat+panel. NO migration/DATA/re-ingest; NO map/geo/choropleth files touched (map non-reg inherent from ecddc17 d3-geo). **LANDMINE-1 (disk):** pre-build `df -h /`=48G free (67%, <60G threshold) → `docker builder prune -f` (51GB, 0 active = safe) + `docker image prune -f` (dangling only) → freed ~50G → 93G free (33%) BEFORE building. **LANDMINE-2 (pg):** postgres NOT recreated — `up -d --no-deps --force-recreate statdash-api geostat panel` only; postgres id **449fe998eaa5** IDENTICAL before/after ("Up 3 hours"). 3 images genuine no-cache `DOCKER_BUILDKIT=0` (~5min): api=**cc807fc27837**, geostat=**2a3c7119893e**, panel=**bc1eb1f5097b**. **RE-PROVISION CONVERGED:** gdp `config.page_version` **v14→v15 published** on this boot (accounts=16/regional=23 UNCHANGED → gdp-scoped, correct); new published gdp config has income title `Generation of income account`/`შემოსავლების ფორმირების ანგარიში` + treemap `derive isTotal/pctBase` (has_pctbase/istotal/derive all true — the total-tile-kept + pct-base-excludes-total change). Anchors GDP=384/ACCOUNTS=415/REGIONAL=1554, total obs **2353**, flyway untouched. Smoke green (:3002/:3003=200, /health=ok, /api/bootstrap=200). **Rollback armed:** backup `/tmp/statdash-prod-backup-20260703-163137.dump` (809,645B, 879 TOC, expected circular-FK warn), `:rollback` RE-TAGGED to TRUE prior-live ecddc17 (api=**4327d0179889**/geostat=**3914b45585d0**/panel=**4722645e1bc9** — geostat/panel rollback were STALE at ce8753d-era 42ae93d761ae/797f1c8cfa7b, re-tagged from current :latest), git tag `pre-ui-batch-deploy`=**ecddc17** created+pushed from local (did not pre-exist). **LIVE-DISPLAY VERIFIED (Playwright→:3002, shots platform/work/verify-shots/ui-batch/, scripts platform/work/verify-ui-batch-*.mjs):** (1) INCOME TREEMAP `/ka/gdp` — 5 tiles, total `=` marker on GDP-at-market-prices 104,598.1 (no pct, largest full-height left col), 4 `+` components summing 100% (operating-surplus 39% + compensation 33.3% + mixed-income 14.5% + net-taxes 13.2%), uniform blue rgb(0,128,190); the pctBase derive correctly excludes total from % base (shot 01-income-treemap-ka.png). (2) SIDEBAR states light+dark — inactive labels muted slate (light rgb(90,107,125)/dark rgb(138,143,156), NOT near-black), active=accent rgb(0,128,190)+weight-600+4px left indicator bar+8% accent row tint; focus-visible rings + hover:not(.is-active) lift are in the CSS (shots 02-sidebar-light/dark.png). (3) PERSPECTIVE-TAB-GROUP — now `align-self:flex-start`+`margin-inline-start:24px`, `justify-content:normal` (was flex-end), width 191 vs parent 1176 (hugs tabs), left-anchored 24px inset; compact segmented pill "წლიური"/"დინამიკა" at LEFT under filters (shot 03-perspective-row-context.png). (4) LOW-CARDINALITY bars `/ka/regional` — 0-sel=11 hbars @36px; 1-region(Tbilisi)=1 bar thickness 107px in 514px plot (fillRatio 0.21, ≤128px hbar cap, NOT a full-plot stripe — old percent-taper would've been ~64% block); 2-region=proportional (Tbilisi 37,059.5 vs Imereti 5,518.9) (shots 04-comparison-1region/2region.png). (5) NON-REG — /en/regional fully English (htmlLang=en, "Regional Accounts"/"Gross Value Added"/"GEL mn", 0 Georgian leaks) + /ka Georgian; map renders 11/11 real region paths (occupied-red Abkhazia+S.Ossetia, selected-amber Adjara, blue ramp — shot 05-map-nonreg-after-hidden-select.png). (Prior-live before this: `ecddc17`, deployed 2026-07-03 ~16:25 UTC — details below.)

**PRIOR STATE (2026-07-03 ~16:25 UTC): `main` @ `ecddc17` DEPLOYED + verified (i18n leak-proof + integrity-consolidation batch).** Prior-live was **`ce8753d`** (provisioning sha `7a632e89`). This deploy: 3 app images genuine no-cache `DOCKER_BUILDKIT=0` rebuilt (api=**4327d0179889**, geostat=**3914b45585d0**, panel=**4722645e1bc9**) then **`up -d --no-deps --force-recreate statdash-api geostat panel`** (per-service — postgres id `449fe998eaa5` IDENTICAL before/after, "Up 3h", flyway/ingest NOT triggered). Baked provisioning sha in new api = **dff8dadf** (=ecddc17 blob); the ONLY provisioning delta was the regional page title placeholder `"regional"`→real bilingual `{en:"Regional Accounts", ka:"რეგიონული ანგარიშები"}` (methodology LocaleString ships via the plugins/react SectionMethodology engine, NOT the JSON). **Re-provision CONVERGED:** `config.page.title` regional now the real bilingual + `config.page_version` regional **22→23 published** on this boot (gdp v14/accounts v16 UNCHANGED → regional-scoped, correct). Anchors GDP=384/ACCOUNTS=415/REGIONAL=1554, total obs=**2353**, flyway untouched. Smoke green (:3002/:3003=200, /health=ok, /api/bootstrap=200). Rollback armed: backup `/tmp/statdash-prod-backup-20260703-160400.dump` (809,597B, 879 TOC, 90 TABLE DATA, harmless circular-FK warn), `:rollback` RE-TAGGED to TRUE prior-live (api=**b4332fe4741f**/geostat=**42ae93d761ae**/panel=**797f1c8cfa7b**), git tag `pre-i18n-deploy`=**ce8753d** pushed from local. **LIVE-DISPLAY VERIFIED (Playwright→:3002, shots platform/work/verify-shots/i18n-deploy/):** (1) i18n LEAK FIX — on FRESH route load `/en/regional|gdp|accounts` app content is **Georgian-codepoint-FREE** (KPI labels/units, section/panel titles, badges, chart legends, view toggles, footer all English) and `/ka/*` fully Georgian; htmlLang flips; PROVEN by the two clean route screenshots (regional-ka.png = "რეგიონული ანგარიშები"/"მლნ ₾", regional-en.png = "Regional Accounts"/"GEL mn"/"Tbilisi…"). The engine-resolved fields that USED to leak now track locale on route load. (2) AR-39 CONFIRMED on GDP/2025 (the ONLY preliminary slice — 24 `obs_status='P'` rows, GDP_ANNUAL 2025): each of the 4 GDP sections shows EXACTLY ONE consolidated `.section__integrity` header indicator (`წინასწ.`/`Prelim.`), **ZERO scattered per-panel pills**, disclosure via the `.section__icon-btn` aria=`მონაცემთა სანდოობა`/`Data integrity` opens `.section__methodology` `.section__integrity-note`=`წინასწარი მონაცემები`/`Preliminary data` — and BOTH track locale. (3) NON-REG map d3-geo: the historically-blanking leaflet path (table→select-region-while-map-HIDDEN→toggle-back) is FIXED by the declarative d3-geo rewrite — 13 real regions / 0 degenerate / no crash across a 3× storm, region multi-select accumulates (R3,R5,R11), occupied-red=2, map-click ''→R3 adds a highlight hue (screenshot nonreg-map-after-hidden-select.png shows real Georgia shapes, occupied red + amber selected). AR-38 sector-select reorients composition donut→bar (x=11 regions). FiraGO + hbar height clean. **🟡 HONEST RESIDUAL (reported, NOT a deploy fault — state of ecddc17, and separate from the flagged runner-chrome EmptyState/ExportBar English-on-ka owner-decision):** on an IN-APP locale TOGGLE (`.locale-switcher__btn` labels are `ქარ`/`ENG`, not KA/EN) via client-side SPA nav, nav+sidebar+page-title+badges+**section titles** all flip correctly, BUT two DATA-DERIVED label groups stay in the pre-toggle locale until a full reload: (a) the 4 KPI-strip card labels+unit+subtext, (b) the donut/hbar chart **legend + category (region) labels**. Both resolve CORRECTLY on a fresh load (so permalink/URL entry — the primary path — is 100% right). Root hypothesis: KPI/chart label resolution is memoized in a rows/spec/promise cache keyed on dims but NOT on locale, so a locale-only client transition returns the stale cached labels; the render-ctx locale wiring (37549ab) reactively re-resolves config-text (titles/nav) but not the cached data-derived labels. Frontend follow-up (key the label cache on locale / force re-resolve on locale change). Verify scripts: platform/work/verify-{i18n-leak,i18n-toggle,toggle-patient,ar39-gdp2025,nonreg-map}.mjs + verify-d3geo-ar38-item2.mjs.

**How to apply (this is the real procedure):**
- SSH target: `geostat-deploy` (= administrator@192.168.1.199:22, key `ops/config/ssh/id_rsa`, via `-F ops/config/ssh/config`). Reachable on LAN; Docker 29.1.3.
- CURRENT STATE (2026-07-03 ~11:44 UTC): `/tmp/statdash-build` on `main` at **`b9503aa`** (composition batch; chief-engineer APPROVED). Provisioning DELTA cf90f77→363aacb (row:click key now `{$ctx:_selKey}`, sector-select `visibleWhen: perspective-not range`, range `onEnter` also clears `sector:null`, adds `_selKey:region`) + packages/react engine (new `ActionField = string|CtxScopeRef`, state-bound key lowered via `resolveActionField`→`resolveRef` dims→vars) + packages/plugins (PivotTable series/column WCAG select `onRowSelect`/`selectedIds`; GeoMap VISIBILITY-GATE `{mapVisible && <GeoJSON>}`). ALL THREE images rebuilt genuine no-cache (`DOCKER_BUILDKIT=0 docker-compose -f docker-compose.prod.yml build --no-cache statdash-api geostat panel` then `up -d`): api=**f9d737c06a29**, geostat=**f8ec251025f1**, panel=**01b3d7727656**. `:rollback` RE-TAGGED to true prior-live (f763053): api=**09cd4521771d** / geostat=**cafca25eedf4** / panel=**21b0ff1de503**. Git tag pushed FROM LOCAL: `pre-composition-deploy`=**f763053**. Backup `/tmp/statdash-prod-backup-20260703-153154.dump` (809,174 bytes, 862 TOC, no warnings; user=statdash). flyway exit0 (no-op) + ingest exit0 (whole-dataset 409-SKIP-TO-SERVE all 3 — the clean idempotent path at ingest-canonical.sh:123, NOT the per-job-publish 409 gotcha; anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 + 4-dim GDP 22148.65 + REGIONAL _T/_T 21821.57). **RE-PROVISION CONVERGED (load-bearing this deploy):** baked provisioning sha256 in running api = **82e9e991…** (=b9503aa blob; was 7e23fd0d); `runProvisioning` appended+published `config.page_version` **regional v22** on this boot (gdp v14/accounts v16 UNCHANGED → delta is regional-scoped, correct; note api logs provisioning at info/debug — only 409 warns surface at level≥40, so verify via the page_version table not logs). Smoke all green (:3002/:3003=200, /api/bootstrap=200, api /health=ok). **LIVE-DISPLAY VERIFY (local Playwright→192.168.1.199:3002, shots platform/work/verify-shots/composition-batch/): ITEM 1 MAP RECOVERY STILL FAILS — HARD BLANK (rollback deferred to lead).** select-region-while-map-HIDDEN(table view)→toggle-back-to-map = permanent blank: real=0/degen=13 all `d="M0 0"`, container a valid 537×327, NO self-heal @1.5/4/7s, NOT healed by resize nudge. The visibility-gate (`{mapVisible && <GeoJSON>}`) KILLED THE CRASH (crashCard=false now; f763053 threw a NaN error-boundary) but NOT the blank — the fresh-mounted <GeoJSON> STILL projects to M0 0 because the base-map projection origin is stale at mount and `RepairOnShow`'s `invalidateSize` does not re-project already-added vector paths. Control (plain toggle, NO select) = 13 real (fine). REAL FIX still owed: defer the GeoJSON mount ONE TICK after `invalidateSize`, or force `map._resetView(center,zoom)`/re-add the layer on the show transition. Shots diag-item1-map.png + 01-map-recovery.png = blank white panel. **ITEM 2 PARTIAL:** multi-select ADD works (map R9→R9,R7; geograph SimpleTable row R9→R9,R4). Composition PIVOT series-column headers are now interactive WCAG buttons (role=button + aria-pressed; 2 regions→2 selectable pressed cols) and clicking a selected region's column TOGGLES it OFF (R9,R7→R9 — WAS inert, now live). BUT the pivot renders ONLY currently-selected regions as columns, so there is NO unselected column to click-to-ADD from the pivot (item2_hasUnselectedCol=false) — the brief's literal 'click a 2nd region's column → becomes 2 regions' is NOT achievable; pivot columns REMOVE/toggle selected regions, adding is via map/geograph-table. **ITEMS 3/4/5/6 PASS:** State-A #geo-map SimpleTable row→`region` single-select (''→R5); range mode = 0 sector selectors (year mode = 1) AND year→range switch clears `sector` (SEC01→''); comparison hbar CHART view = **560px** (horiz, 1103 wide); FiraGO 400/500/600/700 all document.fonts.check=true + bodyFF=FiraGO, `<html lang>` ka→en flip (url /ka→/en/regional), /ka/accounts scroll (scrollH 824 > clientH 574, moved 250), State-B composition chart 2 series DISTINCT fills blue rgba(0,90,156)+orange rgba(232,113,10). One KNOWN non-breaking getComputedStyle pageerror on chart↔table toggle (pre-existing ApexCharts-teardown, documented). Verify scripts: platform/work/{verify-A,verify-B,verify-colors,diag-item1,probe-pivot-select,probe-reveal-pivot,recon-composition}.mjs. ROLLBACK NOT taken (deferred to lead per brief; prod functional — map only blanks on the specific hidden-select path, map-click selection + all other items work). (Superseded prior state 2026-07-03 ~10:20 UTC: `/tmp/statdash-build` on `main` at **`f763053`** (map-setstyle root-fix e763ed8: selection highlight now drives `layer.setStyle()` IN PLACE instead of keying `<GeoJSON>` on `selectedGeos` — the choropleth key is now `choroplethLayerKey(colorByGeo)` (SCALE only, no selection), so a row-pick no longer remounts/re-projects the layer while hidden; `resolveFeatureStyle`/`featureStyle`/`hoverStyle` extracted to choropleth.ts as the SSOT shared by the mount `style` prop AND the setStyle effect; RepairOnShow hardened with `boundsAreFinite` guard + `clientWidth/clientHeight>0` guard + whole-body try/catch as defense-in-depth. FRONTEND-ONLY — `git diff 675d948..f763053` = 5 files (geograph GeoMap.tsx + choropleth.ts + 3 fitness tests), ZERO provisioning/API/migration delta). Rebuilt **geostat+panel ONLY** genuine no-cache classic-builder (`DOCKER_BUILDKIT=0`). New images: geostat=**cafca25eedf4**, panel=**21b0ff1de503** (api UNTOUCHED, still 09cd4521771d, Up 15h not recreated). `:rollback` RE-TAGGED to TRUE prior-live (675d948): geostat=**c7304c0c8603** / panel=**8672f774b7a7** / api=**09cd4521771d**. Git tag pushed from LOCAL: `pre-map-setstyle-deploy`=**675d948**. Backup `/tmp/statdash-prod-backup-20260703-101655.dump` (809,174 bytes, 877 TOC, harmless circular-FK warning; NOTE the pg_dump user is `statdash` NOT `postgres` — role `postgres` does not exist; db=statdash, creds in `../config/api/.env.prod` DATABASE_URL). Recreate cascaded flyway(exit0,no-op)+ingest(exit0, anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 matched, 4-dim GDP)+geostat+panel; api stayed up. Smoke all green (:3002/:3003=200, /api/bootstrap=200, api /health=ok). **LIVE-DISPLAY VERIFY — ACCEPTANCE ITEM A STILL FAILS (crash→BLANK, rollback deferred to lead).** The CRASH IS FIXED: 4-iteration storm of table→select-region-while-hidden→toggle-back = crashCard=false, invalidLatLng=0, 0 console/page errors every time (was hard error-boundary "Failed to load component — Invalid LatLng (NaN,NaN)" on 675d948). setStyle selection colour DOES apply (amber count grows 1→4 across storm) and occupied stays red (#dc2626 ×2). BUT the map is PERMANENTLY BLANK after that path: all 13 paths degenerate `M0 0`, overlayBBox 0×0, though the container is a real 537×327 and mapVisible=true (shot A1-recovered-map.png = white panel with only the region subtitle). Isolated w/ probe: plain toggle-back WITHOUT select RECOVERS fine (13 real, like old P1); the ONLY differentiator is a selection change while hidden; the blank is NOT healed by a real viewport resize NOR a dispatched resize event (hard blank, no self-heal). Map-CLICK selection from a FRESH map WORKS PERFECTLY (click→region param ""→R11, 13 real paths, selected=amber #e8a33d + occupied=red + ramp, tooltip; shot D-mapclick-fresh.png). Non-regression B PASS (/ka/accounts scroller scrollH=824>574). ROOT HYPOTHESIS: the setStyle fix removed the selectedGeos-keyed remount, but a selection change while the container is 0×0 STILL corrupts the Leaflet projection to NaN (same underlying root — mutating the layer while display:none); RepairOnShow's new try/catch + size-guard now SWALLOWS the failure instead of throwing → the crash became a silent permanent blank rather than an actual re-projection. The real fix must ensure the layer is re-projected against a valid box on show AFTER a hidden restyle (e.g. force a full `_reset`/re-add or defer the setStyle until visible + invalidateSize+reset on show), OR keep the layer entirely untouched while hidden and apply the pending selection style on the show transition. Verify scripts: platform/work/{verify-map-setstyle,verify-map-setstyle-probe,verify-map-setstyle-click}.mjs; shots platform/work/verify-shots/map-setstyle/. (Superseded prior state 2026-07-03 ~09:45 UTC: `/tmp/statdash-build` on `main` at **`675d948`** (regression-fix BATCH fbb9f67: GeoMap `RepairOnShow` map-repair-on-hidden→shown, Chart.tsx horizontal-chart height bound + internal scroll, HBAR_MIN_HEIGHT 240→380; FRONTEND-ONLY — only `packages/plugins`, verified `git diff b5ae777..675d948` = 5 files (GeoMap.tsx + Chart.tsx + apex/base.ts + 2 fitness tests), ZERO provisioning/API/migration delta). Rebuilt **geostat+panel ONLY** genuine no-cache classic-builder (`DOCKER_BUILDKIT=0`). New images: geostat=**c7304c0c8603**, panel=**8672f774b7a7** (api UNTOUCHED, still 09cd4521771d). `:rollback` RE-TAGGED to TRUE prior-live (b5ae777): geostat=**0d1f8b29d5d3** / panel=**4cdcceb16933** / api=**09cd4521771d**. Git tag pushed from LOCAL: `pre-regression-fix-deploy`=**b5ae777**. Backup `/tmp/statdash-prod-backup-20260703-094137.dump` (809,174 bytes, 877 TOC, 90 TABLE DATA, harmless circular-FK warning). Recreate cascaded flyway(exit0,no-op V38)+ingest(exit0, anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 matched)+geostat+panel; api stayed up. Smoke all green (:3002/:3003=200, /api/bootstrap=200, api /health=ok). **LIVE-DISPLAY VERIFY — ACCEPTANCE ITEM A FAILED (rollback deferred to lead).** B PASS: /ka/accounts horizontal-chart scroller scrollH=824>clientH=574 overflowY=auto, actually scrolled 0→250, content reachable. C PASS: comparison apex hbar canvasHeight=**380** (=new floor, horizontal wide bar 888px) not a 240 sliver; wrap maxHeight=646px overflowY=auto (B fix applied). Prior wins hold: FiraGO 400/500/600/700 loaded, `<html lang>` flips ka↔en, map renders healthy when NOT hidden (occupied-red + selected-amber, live tooltip). **A (map recovery on select-while-hidden) FAILS — the fix regressed blank→HARD CRASH.** Isolated w/ 2-probe control: P1 (table→map toggle, NO select) RECOVERS fine (13 real paths, 0 degenerate); P2 (table→select-region-while-hidden→map = the brief's exact A path) CRASHES the geograph shell: `Error: Invalid LatLng object: (NaN, NaN)` + `[renderNode] shell crashed {type: geograph}`, #geo-map panel replaced by error-boundary card "Failed to load component — Invalid LatLng object: (NaN, NaN)" + Retry (shot A1-recovered-map.png). ROOT: `RepairOnShow` (new in GeoMap.tsx) calls `map.fitBounds()` on re-show, but the `<GeoJSON key={choroplethLayerKey(selectedGeos)}>` remount that fired while the container was 0×0 already NaN-corrupted Leaflet's projection; `fitBounds` on that NaN state THROWS synchronously (unhandled in the effect) → React error boundary. `invalidateSize()` alone does not re-seed a valid projection center. PROPOSED real fix (deeper root, per the component's own docstring TODO): drive the selection highlight via `layer.setStyle()` on the existing layer instead of keying `<GeoJSON>` on `selectedGeos` — eliminates the hidden-remount window entirely; OR guard RepairOnShow's fitBounds behind a valid-size+valid-bounds check and `map.setView(center)` before fitBounds. Verify scripts: platform/work/{verify-regression-fix,verify-map-recovery,verify-map-control,verify-c-hbar}.mjs; shots in platform/work/verify-shots/regression-fix/. (Superseded prior state 2026-07-03 ~09:07 UTC: `/tmp/statdash-build` on `main` at **`b5ae777`** (typography/i18n/low-cardinality UI BATCH: self-hosted FiraGO woff2 in packages/styles, low-cardinality chart render, ApexRenderer visibility-gate `useContainerVisible`, DataTable band-scroll fix, i18n-P0 `<html lang>`/`dir`+i18next sync, hero subtitle restored-but-flag-gated-OFF; FRONTEND-ONLY). Rebuilt **geostat+panel ONLY** genuine no-cache classic-builder. New images: geostat=**0d1f8b29d5d3**, panel=**4cdcceb16933** (api UNTOUCHED, still 09cd4521771d). KEY DECISION: the chief-engineer brief said "provisioning changed → API must re-provision", but the net `geostat.provisioning.json` delta across ba95362..b5ae777 is ZERO (8214f19 emptied hero → fc86841 restored it identical; git blob cf90f77 identical both refs; running api image's baked JSON sha256=7e23fd0d == b5ae777's, already applied on its last boot) → NO api rebuild/re-provision needed, DB already in sync. Verified the CONDITION: fresh geostat dist emits 4 firago-*.woff2 (400/500/600/700, hashed e.g. firago-400-normal-Dw0lLwRH.woff2) AND index-D3clw6XA.css has 6 FiraGO @font-face + url(/assets/firago-*.woff2) refs; served as font/woff2 200. `:rollback` tags = TRUE prior-live (ba95362): api=09cd4521771d / geostat=**2c7d6e4a72f3** / panel=**9d9c87b257a5** (had to RE-TAG geostat/panel rollback — they were STALE at ff9d020's 087c9aa/d45d042; api was already correct). Git rollback tag pushed FROM LOCAL: `pre-typography-batch-deploy`=**ba95362**. Backup `/tmp/statdash-prod-backup-20260703-125130.dump` (809,174 bytes, 862 TOC, 38 stats/config data entries, harmless timescaledb continuous_agg circular-FK warning). Recreate cascaded flyway(no-op, schema V38)+ingest(exit0, anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 matched, EXPECTED_OBS already 384)+geostat+panel; api stayed up (not recreated). LIVE-DISPLAY VERIFIED (local Playwright → :3002, script platform/work/verify-typography-batch.mjs): FiraGO 400/500/600/700 document.fonts status "loaded" (real, not fallback), body+Georgian-text computed fontFamily=FiraGO; `<html lang>`=ka `dir`=ltr, locale toggle ka↔en flips lang+chrome no mixed-locale; State-B (R3+R4) composition CHART = stacked bar x=sectors series=2 regions in DISTINCT blue(0,90,156)+orange(232,113,10) (NOT grey-on-grey), comparison hbar 2 thick filled bars; composition TABLE view scrolls (18 rows, 720>318) with a frozen column header (freeze is scroll-scoped-to-body, `th` reads position:static — NOT sticky, but visually pinned) + export/link bar reachable; table→selchange→chart-back REPAINTS apex, NO NaN; hero landing has NO visible subtitle (flag-gated OFF), layout intact; 0 [object Object]/React#31 across all steps. ONE CAVEAT (reported to lead, non-blocking): a single caught uncaught PAGEERROR `getComputedStyle ... parameter 1 is not of type 'Element'` fires ONCE on the composition chart→TABLE view toggle (ApexCharts-unmount-class teardown on a detached/null ref — NOT the useContainerVisible hook, which uses clientWidth/offsetParent not getComputedStyle; NOT the NaN crash); non-breaking (table+chart both render); latent/unobserved before because prior State-B verifies never toggled table view → frontend follow-up ticket. (Superseded prior state 2026-07-03 ~07:17 UTC: `/tmp/statdash-build` on `main` at **`ba95362`** (merge of c0f70d0 = regional cross-filter State-B fix: node-unique async `_promiseCache` key in `packages/react/useNodeRows.ts`; FRONTEND-ONLY, no migration/DATA/provisioning delta). Rebuilt **geostat+panel ONLY** (fix is in packages/react → bundled into both SPAs; api NOT rebuilt — no backend change). GENUINE no-cache classic-builder rebuild (`DOCKER_BUILDKIT=0 … build --no-cache geostat panel`). New images: geostat=sha256:2c7d6e4a72f3, panel=sha256:9d9c87b257a5 (api untouched, still 09cd4521771d). `:rollback` tags = TRUE prior-live (ff9d020): api=09cd4521771d / geostat=087c9aa4675f / panel=d45d042db237. Git rollback tag pushed (from LOCAL repo — the server clone has NO github push creds, `fatal: could not read Username`): `pre-statebfix-deploy`=**ff9d020**. Backup `/tmp/statdash-prod-backup-20260703-071225.dump` (809,174 bytes, 877 TOC entries, 38 stats/config data entries, pg_restore -l verified; harmless circular-FK warning on timescaledb continuous_agg for -Fc dump). Prod DB unchanged (flyway V1→V37 no-op re-apply; ingest one-shot exit 0, anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 all matched — EXPECTED_OBS already correct at 384). GOTCHA HIT THIS SESSION: the fix commits (c0f70d0 + merge ba95362) were UNPUSHED local commits — origin/main was still at ff9d020; had to `git push origin HEAD:main` FIRST before the server `git fetch` could see ba95362. Also the server clone canNOT resolve a bare sha (`git reset --hard <sha>` fails "Use '--' to separate…") — MUST `git reset --hard FETCH_HEAD` after `git fetch origin main`. LIVE-DISPLAY VERIFIED (local Playwright → :3002): State A composition = 11-region GDP donut; State B (select R3+R4=Adjara+Guria) composition re-purposes to apex-BAR x-axis=SECTORS(დარგები) grouped by the 2 selected regions (legend=აჭარის ა.რ.+გურია), comparison bar=only the 2 regions, KPI GVA=9,081 (selection sum, not national 80,883), map highlights 2. NO [object Object], no console/page errors. Cosmetic follow-ups (NOT the fix, pre-existing): the composition panel TITLE stays "GDP — by region" in State B (content re-purposed but heading not relabeled to "Sectoral structure"); "Tbilisi's share" KPI shows >100% under selection. (Earlier 2026-07-02 ~17:44 UTC state, now superseded: `/tmp/statdash-build` on `main` at **`428783e`** (694255a full FULL rebuild — AR-36 runtime pivot + KPI root-fix + theme-contrast, touching packages/core+react+charts+plugins+styles + apps/api/provisioning, PLUS a same-session hotfix 428783e; NO migration/DATA delta vs prior). Prod DB unchanged (no ingest, no migration — flyway re-run was a no-op re-apply of already-applied V1-V37). All 3 images rebuilt from scratch: api=sha256:303c28f0e2d8, geostat=sha256:e77341218f15, panel=sha256:4aa61abd7e07. `:rollback` tags = TRUE prior-live (0a16509): api=75532ea884ff / geostat=e86814f8f2f5 / panel=2ed3320f3955. Git rollback tag pushed: `pre-pivot-deploy`=**0a16509**. Backup `/tmp/statdash-prod-backup-20260702-214009.dump` (791,753 bytes, 866 TOC entries, pg_restore -l verified in-container). RESOLVED same session: `ops/scripts/ingest-canonical.sh` `EXPECTED_OBS[GDP_ANNUAL]` was stale at 288 (should've been bumped to 384 back when GFCF/NOE landed) — every idempotent re-run of the `ingest` one-shot (409 ALREADY_PUBLISHED on all 3 datasets, no data touched) false-failed on its own self-check, exit 1, which blocks the compose dependency gate (`geostat`/`panel` both `depends_on: ingest: service_completed_successfully` — a failed ingest means the SPAs never start even though api/postgres are healthy and data is 100% correct). Fixed by bumping the constant to 384 (commit 428783e) — root-cause, not a workaround (bypassing the gate with `--no-deps` would've left the driver broken for the next deploy). The `ingest` service's script is BIND-MOUNTED (`../scripts/ingest-canonical.sh:/ingest-canonical.sh:ro`), so a script-only fix needs NO image rebuild — just re-`git fetch`+checkout on the server clone, then `up -d` again to retry the gate. General lesson: after any additive re-ingest that changes a dataset's true obs count, the golden `EXPECTED_OBS` in this script must be bumped in the SAME PR/session, or the NEXT deploy's ingest gate breaks. (Earlier 2026-07-02 ~16:07 UTC state, now superseded: `/tmp/statdash-build` on `main` at **`0a16509`** (GDP structural+NOE charts restored, regional State-A KPI `$ne:'_T'` fix — all pure provisioning JSON + canonical DATA; NO frontend-source/migration delta vs prior-live). Prod DB **V38** (unchanged — no migration in this delta). MINIMAL deploy set proven correct: rebuilt **statdash-api ONLY** (provisioning is baked into the api image via `COPY apps/api` → `/app/apps/api/provisioning`, re-applied every boot by `runProvisioning`) + additive GDP re-ingest; geostat/panel NOT rebuilt (bundles byte-identical). New api image sha256:75532ea884ff. Recreated via `docker-compose -f docker-compose.prod.yml up -d --no-deps statdash-api` (no flyway/ingest deps triggered). GDP now **384 obs** (288+96 new: approach GFCF ×80 = 5 gfcf-* measures ×16yr, + non-observed-economy-share ×16). Rollback `:rollback` tags = TRUE prior-live: api=91ad25c4 / geostat=e86814f8 / panel=2ed3320f. Git rollback tag pushed: `pre-gdp-charts-deploy`=**0dc7d54** (prior-live; containers were up 3h on it). Backup /tmp/statdash-prod-backup-20260702-160124.dump (1,250,423 bytes, PGDMP + pg_restore -l verified, 39 stats/config table-data entries). Prior-live was 0dc7d54 (linear ancestor of 0a16509 → dropped nothing). (Earlier 2026-07-02 09:42 state: `2cdf190`, backup …094036.dump, rollback api=f4a9edd7/geostat=c8bd9d4d/panel=6adb835e, tags `pre-parity-deploy`=b5bf242 / `pre-parity-origin-main`=57a34e5.) IMPORTANT: the owner may deploy the branch to prod CONCURRENTLY with your staging probe — recon-time `docker ps` uptimes can go stale mid-session. Your isolated `statdash-stg` work never collides (separate project/net/volume/`:stg` tags), but do NOT assume prod is frozen; re-smoke `:3002/:3003` at the end. For a NON-DISRUPTIVE branch probe use `ops/compose/docker-compose.staging.yml` (project `statdash-stg`, ports 5457/3009/3008, volume `statdash-stg-pgdata` — fully isolated, shares NOTHING with `statdash-prod`); it is committed but STALE (header says "V1→V32", predates V33+). Add a `-f` override to point api/geostat at branch-built `:stg` images (never rebuild `:latest`). Reach the staging DB from a dev machine over LAN at `192.168.1.199:5457` to run DATABASE_URL-gated suites. Create a detached `git worktree` for the branch so the live `/tmp/statdash-build` checkout stays untouched. See [[fresh-provision-canonical]] for the V33 fresh-DB block.
- Build source = a **server-side git clone at `/tmp/statdash-build`** that (per memory) tracks `main` (remote `https://github.com/aleksandre17/statdash.git`). To deploy a branch: `git fetch origin <branch>` then `git checkout -B <branch> FETCH_HEAD` (the clone's refspec does NOT create `origin/<branch>` tracking refs, so checkout from `FETCH_HEAD`, not `origin/<branch>`). gitignored secrets (`ops/config/*/.env*`, `ops/compose/.env`) are untracked → preserved across checkout.
- Compose: use **standalone `docker-compose` v1** (`/usr/local/bin/docker-compose`) — the `docker compose` v2 plugin is NOT installed (`docker: unknown command: docker compose`).
- Authoritative command = **prod file ALONE**: `cd /tmp/statdash-build/ops/compose && docker-compose -f docker-compose.prod.yml up -d --build` (project `statdash-prod`, self-contained: postgres, flyway, statdash-api[internal, no ports], ingest[one-shot], geostat[:3002], panel[:3003]). Do NOT add `-f docker-compose.yml` (the dev base re-introduces `../config/api/.env` which is absent in prod → config error). Ports come from `ops/compose/.env` (GEOSTAT_PORT=3002, PANEL_PORT=3003).
- Migrations run via the `flyway` service in that same compose (V1→V37 as of this branch). Safe to run explicitly first: `docker-compose -f docker-compose.prod.yml run --rm flyway`.
- BACKUP before deploy: `docker tag statdash-{api,geostat,panel}:latest statdash-{...}:rollback` (the `--build` overwrites `:latest`). ROLLBACK: retag `:rollback`→`:latest` + `git checkout main && git reset --hard <ref>` + `up -d --no-build`. The `statdash-prod-pgdata` named volume holds the live DB — NEVER `down -v`. Migrations are forward-only but all additive/backward-compatible, so a code rollback over an advanced schema is safe (expand-contract).
- Smoke: `curl :3002 :3003`→200; `docker exec statdash-api wget -qO- 127.0.0.1:3001/health`→`{"status":"ok"}` (route is `/health`, NOT `/api/health`); end-to-end `curl :3002/api/bootstrap`→200 manifest. Data anchors: GDP_ANNUAL=**384** (was 288 pre-0a16509; +96 GFCF/NOE), ACCOUNTS_SEQUENCE=415, REGIONAL_GVA=1554.
- RE-INGEST driver gotchas (`ops/scripts/ingest-canonical.sh`, hit 2026-07-02 on the GDP additive re-ingest): (1) its `EXPECTED_OBS[GDP_ANNUAL]=288` serve-assert is STALE now (should be 384) — a re-run's final count-check false-fails. (2) Its `publish_job` does `poll(staged)` then unconditionally `curl -fsS -X POST …/publish`; on a re-ingest the **codelists** job auto-converges to `published` (identical/mostly-existing members → converged no-op, per [[project_canonical_partial_failure_retry]]) BEFORE the explicit publish, so that POST returns **409** and `set -e` ABORTS the driver — leaving the `facts` job STAGED and unpublished (data validated but not in gold). Fix pattern that worked: publish the facts job manually — `POST /api/ingest/jobs/:id/publish` (empty body, `curl -X POST`, Bearer) → `200 {published,revised}`. Do NOT use busybox `wget --post-data=` (returns 400); use curl (throwaway `alpine:3.20` + `apk add curl jq` on `statdash-net`, creds from `../config/api/.env.prod`). Additive new-member ingest is ROUTINE (no `?datasetVersion=` — DSD dims unchanged, only new codelist VALUES; SCD-2 upserts members when codelists publish, then facts validate 0-issues). Driver owner (orchestrator) should bump the anchor + make `publish_job` tolerate a converged-409 as success.
- Headless screenshots via the `geostat-chat-ai-chromium` (zenika/alpine-chrome:124) container DON'T faithfully execute this Vite SPA under `--virtual-time-budget` (blank PNG / DOM dump shows only the static shell). It is NOT a render-failure signal — verify the SPA via served-bundle 200 + `/api/bootstrap` 200 instead.
