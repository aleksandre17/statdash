---
name: live-deploy-mechanism
description: How the LIVE 192.168.1.199 demo (geostat :3002 / panel :3003 / api) is actually deployed — server git clone + prod-only compose, diverges from RUNBOOK
metadata:
  type: project
---

The ACTUAL live-demo deploy path (verified 2026-06-28 by container `com.docker.compose.project.*` labels), which DIVERGES from `ops/RUNBOOK.md` §B and is NOT the per-module geostat-kit drivers.

**Why:** the runbook B.3/B.4 (`-f docker-compose.yml -f docker-compose.prod.yml`, `geostat api deploy --prod`) is stale doc drift. The kit's node-api (rsync) / node-vite (tar) drivers ship the LOCAL tree; the live stack does NOT use them.

**How to apply (this is the real procedure):**
- SSH target: `geostat-deploy` (= administrator@192.168.1.199:22, key `ops/config/ssh/id_rsa`, via `-F ops/config/ssh/config`). Reachable on LAN; Docker 29.1.3.
- CURRENT STATE (2026-07-03 ~07:17 UTC): `/tmp/statdash-build` on `main` at **`ba95362`** (merge of c0f70d0 = regional cross-filter State-B fix: node-unique async `_promiseCache` key in `packages/react/useNodeRows.ts`; FRONTEND-ONLY, no migration/DATA/provisioning delta). Rebuilt **geostat+panel ONLY** (fix is in packages/react → bundled into both SPAs; api NOT rebuilt — no backend change). GENUINE no-cache classic-builder rebuild (`DOCKER_BUILDKIT=0 … build --no-cache geostat panel`). New images: geostat=sha256:2c7d6e4a72f3, panel=sha256:9d9c87b257a5 (api untouched, still 09cd4521771d). `:rollback` tags = TRUE prior-live (ff9d020): api=09cd4521771d / geostat=087c9aa4675f / panel=d45d042db237. Git rollback tag pushed (from LOCAL repo — the server clone has NO github push creds, `fatal: could not read Username`): `pre-statebfix-deploy`=**ff9d020**. Backup `/tmp/statdash-prod-backup-20260703-071225.dump` (809,174 bytes, 877 TOC entries, 38 stats/config data entries, pg_restore -l verified; harmless circular-FK warning on timescaledb continuous_agg for -Fc dump). Prod DB unchanged (flyway V1→V37 no-op re-apply; ingest one-shot exit 0, anchors GDP=384/ACCOUNTS=415/REGIONAL=1554 all matched — EXPECTED_OBS already correct at 384). GOTCHA HIT THIS SESSION: the fix commits (c0f70d0 + merge ba95362) were UNPUSHED local commits — origin/main was still at ff9d020; had to `git push origin HEAD:main` FIRST before the server `git fetch` could see ba95362. Also the server clone canNOT resolve a bare sha (`git reset --hard <sha>` fails "Use '--' to separate…") — MUST `git reset --hard FETCH_HEAD` after `git fetch origin main`. LIVE-DISPLAY VERIFIED (local Playwright → :3002): State A composition = 11-region GDP donut; State B (select R3+R4=Adjara+Guria) composition re-purposes to apex-BAR x-axis=SECTORS(დარგები) grouped by the 2 selected regions (legend=აჭარის ა.რ.+გურია), comparison bar=only the 2 regions, KPI GVA=9,081 (selection sum, not national 80,883), map highlights 2. NO [object Object], no console/page errors. Cosmetic follow-ups (NOT the fix, pre-existing): the composition panel TITLE stays "GDP — by region" in State B (content re-purposed but heading not relabeled to "Sectoral structure"); "Tbilisi's share" KPI shows >100% under selection. (Earlier 2026-07-02 ~17:44 UTC state, now superseded: `/tmp/statdash-build` on `main` at **`428783e`** (694255a full FULL rebuild — AR-36 runtime pivot + KPI root-fix + theme-contrast, touching packages/core+react+charts+plugins+styles + apps/api/provisioning, PLUS a same-session hotfix 428783e; NO migration/DATA delta vs prior). Prod DB unchanged (no ingest, no migration — flyway re-run was a no-op re-apply of already-applied V1-V37). All 3 images rebuilt from scratch: api=sha256:303c28f0e2d8, geostat=sha256:e77341218f15, panel=sha256:4aa61abd7e07. `:rollback` tags = TRUE prior-live (0a16509): api=75532ea884ff / geostat=e86814f8f2f5 / panel=2ed3320f3955. Git rollback tag pushed: `pre-pivot-deploy`=**0a16509**. Backup `/tmp/statdash-prod-backup-20260702-214009.dump` (791,753 bytes, 866 TOC entries, pg_restore -l verified in-container). RESOLVED same session: `ops/scripts/ingest-canonical.sh` `EXPECTED_OBS[GDP_ANNUAL]` was stale at 288 (should've been bumped to 384 back when GFCF/NOE landed) — every idempotent re-run of the `ingest` one-shot (409 ALREADY_PUBLISHED on all 3 datasets, no data touched) false-failed on its own self-check, exit 1, which blocks the compose dependency gate (`geostat`/`panel` both `depends_on: ingest: service_completed_successfully` — a failed ingest means the SPAs never start even though api/postgres are healthy and data is 100% correct). Fixed by bumping the constant to 384 (commit 428783e) — root-cause, not a workaround (bypassing the gate with `--no-deps` would've left the driver broken for the next deploy). The `ingest` service's script is BIND-MOUNTED (`../scripts/ingest-canonical.sh:/ingest-canonical.sh:ro`), so a script-only fix needs NO image rebuild — just re-`git fetch`+checkout on the server clone, then `up -d` again to retry the gate. General lesson: after any additive re-ingest that changes a dataset's true obs count, the golden `EXPECTED_OBS` in this script must be bumped in the SAME PR/session, or the NEXT deploy's ingest gate breaks. (Earlier 2026-07-02 ~16:07 UTC state, now superseded: `/tmp/statdash-build` on `main` at **`0a16509`** (GDP structural+NOE charts restored, regional State-A KPI `$ne:'_T'` fix — all pure provisioning JSON + canonical DATA; NO frontend-source/migration delta vs prior-live). Prod DB **V38** (unchanged — no migration in this delta). MINIMAL deploy set proven correct: rebuilt **statdash-api ONLY** (provisioning is baked into the api image via `COPY apps/api` → `/app/apps/api/provisioning`, re-applied every boot by `runProvisioning`) + additive GDP re-ingest; geostat/panel NOT rebuilt (bundles byte-identical). New api image sha256:75532ea884ff. Recreated via `docker-compose -f docker-compose.prod.yml up -d --no-deps statdash-api` (no flyway/ingest deps triggered). GDP now **384 obs** (288+96 new: approach GFCF ×80 = 5 gfcf-* measures ×16yr, + non-observed-economy-share ×16). Rollback `:rollback` tags = TRUE prior-live: api=91ad25c4 / geostat=e86814f8 / panel=2ed3320f. Git rollback tag pushed: `pre-gdp-charts-deploy`=**0dc7d54** (prior-live; containers were up 3h on it). Backup /tmp/statdash-prod-backup-20260702-160124.dump (1,250,423 bytes, PGDMP + pg_restore -l verified, 39 stats/config table-data entries). Prior-live was 0dc7d54 (linear ancestor of 0a16509 → dropped nothing). (Earlier 2026-07-02 09:42 state: `2cdf190`, backup …094036.dump, rollback api=f4a9edd7/geostat=c8bd9d4d/panel=6adb835e, tags `pre-parity-deploy`=b5bf242 / `pre-parity-origin-main`=57a34e5.) IMPORTANT: the owner may deploy the branch to prod CONCURRENTLY with your staging probe — recon-time `docker ps` uptimes can go stale mid-session. Your isolated `statdash-stg` work never collides (separate project/net/volume/`:stg` tags), but do NOT assume prod is frozen; re-smoke `:3002/:3003` at the end. For a NON-DISRUPTIVE branch probe use `ops/compose/docker-compose.staging.yml` (project `statdash-stg`, ports 5457/3009/3008, volume `statdash-stg-pgdata` — fully isolated, shares NOTHING with `statdash-prod`); it is committed but STALE (header says "V1→V32", predates V33+). Add a `-f` override to point api/geostat at branch-built `:stg` images (never rebuild `:latest`). Reach the staging DB from a dev machine over LAN at `192.168.1.199:5457` to run DATABASE_URL-gated suites. Create a detached `git worktree` for the branch so the live `/tmp/statdash-build` checkout stays untouched. See [[fresh-provision-canonical]] for the V33 fresh-DB block.
- Build source = a **server-side git clone at `/tmp/statdash-build`** that (per memory) tracks `main` (remote `https://github.com/aleksandre17/statdash.git`). To deploy a branch: `git fetch origin <branch>` then `git checkout -B <branch> FETCH_HEAD` (the clone's refspec does NOT create `origin/<branch>` tracking refs, so checkout from `FETCH_HEAD`, not `origin/<branch>`). gitignored secrets (`ops/config/*/.env*`, `ops/compose/.env`) are untracked → preserved across checkout.
- Compose: use **standalone `docker-compose` v1** (`/usr/local/bin/docker-compose`) — the `docker compose` v2 plugin is NOT installed (`docker: unknown command: docker compose`).
- Authoritative command = **prod file ALONE**: `cd /tmp/statdash-build/ops/compose && docker-compose -f docker-compose.prod.yml up -d --build` (project `statdash-prod`, self-contained: postgres, flyway, statdash-api[internal, no ports], ingest[one-shot], geostat[:3002], panel[:3003]). Do NOT add `-f docker-compose.yml` (the dev base re-introduces `../config/api/.env` which is absent in prod → config error). Ports come from `ops/compose/.env` (GEOSTAT_PORT=3002, PANEL_PORT=3003).
- Migrations run via the `flyway` service in that same compose (V1→V37 as of this branch). Safe to run explicitly first: `docker-compose -f docker-compose.prod.yml run --rm flyway`.
- BACKUP before deploy: `docker tag statdash-{api,geostat,panel}:latest statdash-{...}:rollback` (the `--build` overwrites `:latest`). ROLLBACK: retag `:rollback`→`:latest` + `git checkout main && git reset --hard <ref>` + `up -d --no-build`. The `statdash-prod-pgdata` named volume holds the live DB — NEVER `down -v`. Migrations are forward-only but all additive/backward-compatible, so a code rollback over an advanced schema is safe (expand-contract).
- Smoke: `curl :3002 :3003`→200; `docker exec statdash-api wget -qO- 127.0.0.1:3001/health`→`{"status":"ok"}` (route is `/health`, NOT `/api/health`); end-to-end `curl :3002/api/bootstrap`→200 manifest. Data anchors: GDP_ANNUAL=**384** (was 288 pre-0a16509; +96 GFCF/NOE), ACCOUNTS_SEQUENCE=415, REGIONAL_GVA=1554.
- RE-INGEST driver gotchas (`ops/scripts/ingest-canonical.sh`, hit 2026-07-02 on the GDP additive re-ingest): (1) its `EXPECTED_OBS[GDP_ANNUAL]=288` serve-assert is STALE now (should be 384) — a re-run's final count-check false-fails. (2) Its `publish_job` does `poll(staged)` then unconditionally `curl -fsS -X POST …/publish`; on a re-ingest the **codelists** job auto-converges to `published` (identical/mostly-existing members → converged no-op, per [[project_canonical_partial_failure_retry]]) BEFORE the explicit publish, so that POST returns **409** and `set -e` ABORTS the driver — leaving the `facts` job STAGED and unpublished (data validated but not in gold). Fix pattern that worked: publish the facts job manually — `POST /api/ingest/jobs/:id/publish` (empty body, `curl -X POST`, Bearer) → `200 {published,revised}`. Do NOT use busybox `wget --post-data=` (returns 400); use curl (throwaway `alpine:3.20` + `apk add curl jq` on `statdash-net`, creds from `../config/api/.env.prod`). Additive new-member ingest is ROUTINE (no `?datasetVersion=` — DSD dims unchanged, only new codelist VALUES; SCD-2 upserts members when codelists publish, then facts validate 0-issues). Driver owner (orchestrator) should bump the anchor + make `publish_job` tolerate a converged-409 as success.
- Headless screenshots via the `geostat-chat-ai-chromium` (zenika/alpine-chrome:124) container DON'T faithfully execute this Vite SPA under `--virtual-time-budget` (blank PNG / DOM dump shows only the static shell). It is NOT a render-failure signal — verify the SPA via served-bundle 200 + `/api/bootstrap` 200 instead.
