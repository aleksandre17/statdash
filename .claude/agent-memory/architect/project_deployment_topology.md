---
name: deployment-topology
description: Pointer to ADR-008-deployment-topology.md — per-app single-origin reverse proxy (nginx serves SPA + proxies /api/); vite-only build stage; SPA fallback ''; no wildcard CORS. Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-008-deployment-topology.md`** (Phase-2 SSOT reorg).

Deployment Topology — per-app single-origin reverse proxy (nginx serves SPA + proxies /api/); vite-only build stage; SPA fallback ''; no wildcard CORS. Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
