# Architect Memory Index

## Project
- [Multi-tenancy decision fork (P0 one-way door)](project_multitenancy_decision_fork.md) — branch is tenant-AGNOSTIC (rebrandable single-tenant) NOT multi-tenant-isolated (no tenant_id in 35 migrations); decide SaaS-vs-per-deploy ADR before stacking features. Also: planned roadmap essentially COMPLETE; sync-data+no-auth findings SUPERSEDED in code. Board: work/board/06-architecture-vision.md
- [apps/ monorepo migration](project_apps_monorepo.md) — workspaces restructure; why a @plugins alias seam was added when moving src → apps/geostat
- [@geostat/* alias resolution](project_geostat_alias_resolution.md) — packages resolve via path aliases only, NOT npm workspaces; workspace:* is a latent install-breaker
- [Panel as external product](project_panel_external_product.md) — panel ships externally, engine packages = the published contract (SemVer); reframes publication strategy
- [catalog.ts React purity](project_catalog_react_purity.md) — plugins/catalog.ts pulls Shell modules; blocks apps/panel palette; fix = per-slice *.meta.ts extraction
- [Charts split (Phase 8.1)](project_charts_split_8_1.md) — @geostat/charts out of engine; cycle resolved by TWO registries split by package, not one shared
- [Semantic Layer (Phase 10.1 N26)](project_semantic_layer_n26.md) — MetricRegistry as 2nd registry axis; by→encoding.series; provenance via MetadataPort decorator
- [Unit attachment decision](project_unit_attachment_level.md) — UNIT_MEASURE attaches at the MEASURE-classifier level, not dataset, not series
- [Seed units codelist mismatch](project_seed_units_codelist_mismatch.md) — seed-units.ts emits PCT but V16 seeds PERCENT; silent today, breaks once unit_code becomes an FK
- [Data-binding SHIPPED](project_data_binding_shipped.md) — VERIFIED 2026-06-26: the 4 data-source ADRs are now CODE. old 3 variants (LOCAL/HREF/storeId) = 3 store KINDS (static/href/stats) behind one DataStore port; href D-HREF OPENED (format+auth registries+SSRF allowlist); semantic layer metric→store (MetricDef.dataSource/specDataSource) + declarative blend (resolveBlends→joinByField) both wired. Prior ADRs' "PROPOSED/DEFER href" status now STALE. Answer to "is the storeId arch better now?" = current one better AND more complete, nothing lost; open work = ADOPTION not architecture (use metric refs, author 2-store blend page); still defer D3-PLANNER. Doc: work/RESEARCH-data-binding-architecture.md
- [Time-Range Readiness Seam](adr_time_range_readiness_seam.md) — last data-render blocker: year-select {from:options,pick:last} returns [] on live ApiStore because getOptions→resolveYears→store.classifiers['time'] is NEVER populated (nonTimeDims-only build). getOptions is FULLY SYNCHRONOUS (no async-warm like useNodeRows), so any classifier-gate hangs forever. VERDICT = A-variant: store-builder folds server time coverage into store.classifiers['time'] at build time → {$cl:'time'} resolves sync, pick:last picks max, ZERO config migration, time stays a generic classifier (Law 1). Source = cube profile gains timeCoverage{min,max,periods} from existing V26 cube_actual_region (first/last_time_period already there). Readiness signal = store-construction promise (already awaited by buildStoreManifest), .catch-degrades. REJECT B (distinct query-type): exists in StoreQuery but forces filter-gate async rework + config migration → door D-DISTINCT (live cardinality trigger). PLUS 2 guards: toObsParams omit from/to on 0/unset (=all years), autoParse year-select no-value sentinel not 0 (the Number(raw)||...||0 bug). Guards alone = correct all-years non-hang; +profile = latest-year precision. FF-YEAR-DEFAULT-LATEST/TIME-RANGE-LOADS/NO-ZERO-TIME/TIME-COVERAGE-SSOT/NO-HANG. T0 api→T1/T2 plugins (primary) ∥ T3/T4 core guards + T5 react settle (safety net, parallel)

## ADR (proposed)
- [Mode → generic `perspective` axis (VISION #3 FINAL + FULL-STACK + SYNTHESIS)](adr_mode_as_view_axis.md) — FINAL one-way-door, BUILD-IT (capstone-of-capstones doc=.v3-SYNTHESIS.md: verdict "this is the plan; build it"; 4 P0 refinements = CUT snapshot-from-config→render-option + page.perspectives:Record<param,Axis> one-container + scope=Record-of-registry-keys-NOT-closed-interface + facet-on-axis-NOT-scope; +1 new door D-GUARD available?:VisibilityExpr; 7-door roadmap each=optional-field|Record-key|scope-key-registration fitness-locked-shut; power=2^N composable from 1 primitive; FLAG=half-built ScopeOverride.compare is liability-to-contain; NO further paper, next=P0 code). READY TO IMPLEMENT (docs .v3.md + .v3-PLAN.md engine; .v3-FULLSTACK.md + .v3-FULLSTACK-BENCHMARK.md = Constructor+API+e2e — see body FULL-STACK EXTENSION section: ONE contract home=packages/contracts not core, 2 missed api surfaces SiteManifestContract.modes+node_mode-bar, site/page registry↔instance split, Perspectives PANE + VisibilityBuilder/MetricDef registration, coverage gate 5th axis, config-cube perspective-contract, residual RESOLVED scope.metric-canonical; v2 `view`-named docs DELETED). Name=`perspective` CONFIRMED (collision-free w/ live node.view, OLAP-correct; lineage=OLAP perspectives/named-cube-query NOT PowerBI bookmarks). PerspectiveAxis{param,perspectives[]} (perspectives[0]=default, NO default? — one SSOT) + PerspectiveDef{id,label,when?,scope:{timeBinding,metric?}}; active id in NEW generic ctx.perspectiveState:Record<string,string> (Harel orthogonal-regions container, retires Law-1-violating ctx.timeMode, multi-axis free later). by-mode=DELETE everywhere NO shim (8 engine sites: data-spec/resolvers/schema/validation/spec-catalog/discriminant-manifest/metric-store-union/mode-types-dataKey + Constructor ByModeEditor+.test+index+DataSpecEditor+coverage-fitness+setupCanvasRegistry + roundtrip-fitness case). EXPAND-CONTRACT ORDER (HIGH-2, load-bearing): ContextMapping.timeMode is MANDATORY (filter-params.ts:293) → relax to optional?+Postel-derive perspectiveState[param] FIRST (P1) → migrate 3 pages (P5) → delete (P6); same for BarDef.timeToggle/timeModes/TimeModeItem + SiteRenderer timeModeKey; no phase un-typecheckable. perspectiveState↔evalVisibility=SINGLE SSOT (HIGH-3): migrate evalVisibility(…,mode?) positional→ctx.perspectiveState[param]; rewire ALL 6 mode-reading sites: renderNode.ts:229 (was ctx.mode.current), BOTH SSR walkers warm.ts+api.ts (NO visibility eval today, thread active id arrow-safe=engine-pure), navUtils.getNavMode:52 (6th site, hand-parses {op:eq,param:timeModeKey}). modeOrder→perspectives[]-order SSOT (3 schema page-defs :169/253/337 + SiteRenderer:103,145 + navUtils:124-132 nav-SORT). scope.metric vs node value.type (LOW-2): measurement-diff authored in EXACTLY ONE place, never both (FF guards dual-encode). INNOVATION: permalink GENERATED from PerspectiveAxis registry (param names+default-elision+snapshot policy) = Law-9 url-permalink as architecture property not convention (FF-PERMALINK-FROM-REGISTRY). effects DELETED=restoring Harel orthogonality NOT cleanup. PATH FIX (MEDIUM-1, capstone's own paths wrong): renderNode/navUtils/warm/api/SiteRenderer all in packages/react/src/engine/** NOT plugins; modeRegistry=core/src/mode/registry.ts (engine singleton, alias→perspectiveRegistry arrow-safe). +2 sites capstone missed: scopeOverride.ts:31 ScopeOverride.timeMode (per-panel, also carries compare= deferred peer), kpi.ts:62 KpiSpec.mode CLOSED union (Law-1 violation, T1). Reframe ELIMINATES not relocates: bar-visibility-gate+alwaysResolve+effects+dup-span/shared-params+by-mode+privileged-dim. one-perspective=zero-machinery STRUCTURAL. Deferred (YAGNI, shape-compatible): scope.store/dims/blend/filters[]/facet/2nd-axis; compare+navMode stay deferred peers NOT live axes. FF: ONE-VIEW-NO-MACHINERY/PERSPECTIVE-IS-PURE-FUNCTION(no-cross-region-mutation)/NO-PER-VIEW-DUPLICATION/VIEW-AXIS-GENERIC/NO-BYMODE-REMNANT/SNAPSHOT-VIEW-EQUIV(P6 gate)/SSR-WALKER-VIEW-AWARE/PERMALINK-FROM-REGISTRY. START=P0 (types+registry-alias+ADR) ∥ P-opt (perspective-aware SSR walkers), both additive two-way doors. Residual user-confirm: scope.metric needs range measurements as registered MetricDefs (register-as-metric vs node-local for one-offs). P4.5 INSERTED (2026-06-27, DECISION=A): the P4 byte-identical blocker — config can't collapse 2 bars→1 + drop effects until scope.timeBinding OWNS the binding, blocked by 3 verified gaps (bar-scoped default gate useFilterState.ts:104-112=the parity mechanism; window writes timeFrom/timeTo but geostat reads {$ctx:fromYear/toYear}; no ctx-ref single-period pin). FIX=additive engine phase BEFORE collapse (Strangler: mechanism alongside live bars→proven byte-identical→P5 switches→P6 deletes System A). 3 capabilities: (a) ctx-ref PIN via resolveTimeBound, (b) configurable targetKeys (write fromYear/toYear not ${dim}From/To), (c) perspective-scope default gate (Protected Variations bar-visibility→perspective-ownership; non-active perspective's param unresolved→range time-unset→FULL SPAN). FFs BINDING-PIN-CTX-REF/TARGET-KEYS/PERSPECTIVE-DEFAULT-GATE/ADDITIVE-IDENTITY. SEQ P4→P4.5(engine-only ∥P3)→P5(collapse byte-identical)→P6(delete barShowWhen gate, perspective-gate=sole SSOT). Parity preserved structurally: full-span range dynamics, CAGR/single-period KPIs, regional map, accounts SNA, ka+en. P5.2 INSERTED (2026-06-27, DECISION=A, 2nd escalation): P6-sequencing blocker — engine spine P0-P4.5 VERIFIED BUILT + config has perspectives-blocks authored, but 3 System-A surfaces STILL LIVE w/ no perspective replacement so P6-delete would regress: (1) mode-bar node (packages/plugins/nodes/mode-bar/default/ModeBarShell.tsx, 3× config, reads ctx.mode via useModeContext→modeRegistry) NO toggle node exists; (2) KpiSpec.mode:'year'|'range'|'both' (packages/core/src/data/kpi.ts:62) filtered at 2 sites SAME predicate interpretKpis:221 render + extractKpiRequirements:309 warm (move TOGETHER or warm-set diverges) closed-union Law-1 violation; (3) modeOrder (3× config + nav-sort navUtils:137-144 + useModeContext avail). FIX=P5.2 migrate-then-delete (same Strangler as P4.5): (1) NEW perspective-bar plugin node (param? NOT modes — options DERIVE from PerspectiveAxis.perspectives[]; reads perspectiveState[param] via activeIdForAxis; label=PerspectiveDef.label; SiteRenderer feeds RenderContext.mode triad from axis not modeRegistry, field-name kept=P6 churn; set=filterSet+URL permalink seam; byte-identical UX); (2) KpiSpec.mode→per-item when?:perspective-is (NOT strip-partition-key=re-privilege), factor kpiVisible(spec,ctx) ONE fn both sites=warm===render SSOT, mode→Postel until P6, value.type untouched; config "mode":"year"→when perspective-is, "both"→drop; (3) modeOrder→perspectives[]-order, SiteRenderer:190 passes axis.perspectives.map(id) to navUtils (no signature change, source moves), delete modeOrder config+schema. FFs PERSPECTIVE-BAR-FROM-AXIS/PERSPECTIVE-BAR-EQUIV/KPI-WHEN-NOT-MODE/NO-PRIVILEGED-KPI-UNION(P6 gate)/NAV-ORDER-FROM-PERSPECTIVES. SEQ P5→P5.2(plugin+engine+config-switch)→P6 grep-clean ALL System A (mode-bar/ModeContext/useModeContext/modeRegistry/applyEffects/KpiSpec.mode/modeOrder/barShowWhen-gate/alwaysResolve/ContextMapping.timeMode/ctx.timeMode/ScopeOverride.timeMode+compare+resolveCompareRows/mode-is-in-not+{op:eq,param:mode}+legacy desugar; grep-zero accept). Parity: toggle switches year/range same buttons/order/active/URL, same KPIs per perspective render+warm, nav order, 3 pages ka+en. PATH-NOTE: mode-bar=packages/plugins/nodes/mode-bar/** (de-tenant added packages/, body §11 path was stale). Plan .v3-PLAN.md §0b+P5.2 phase+P6 re-seq
- [Deployment Topology](adr_deployment_topology.md) — per-app reverse-proxy single-origin (nginx serves SPA + proxies /api/ to INTERNAL api, empty VITE base → relative /api/); kills CORS '*'/CSP served-file patch/local-build-mount/3-origins. RC-1: image tsc -b type-checks the WHOLE ../../packages tree → peer-deps (ajv/react-router-dom/i18next/apexcharts) unresolved under shamefully-hoist=false filtered install; fix = vite-only build stage (drop tsc -b, typecheck=CI), NOT global hoist (refused) nor the 2nd-full-install band-aid. RC-2: SPA ?? 'http://localhost:3001' fallback DEFEATS relative base → must become '' (frontend src change). D3: CORS origin:false prod default; CSP header CANNOT loosen meta (intersection/most-restrictive wins) — frame-ancestors=header-only. FF-SAME-ORIGIN/NO-WILDCARD-CORS/IMAGE-BUILDS-CLEAN/CSP-META-UNPATCHED. Rejected A2 unified-subpath (panel basename+external-product), A5 hoist
- [Platform Structure Re-architecture](adr_platform_structure_rearchitecture.md) — engine/→packages/, @geostat→@statdash, NEW @statdash/contracts, laws-vs-reality drift, Strangler-Fig roadmap
- [Constructor Phase 2](adr_constructor_phase2.md) — tree+inspector+preview triad; PropSchema inspector seam (88% populated, no consumer); open store model (kill CanvasNodeKind enum); cube-profile palette; round-trip fitness; C0–C5 roadmap
- [Constructor VISION / North Star](adr_constructor_vision_north_star.md) — best-in-class builder steal-table; full renderer→authorable COVERAGE-GAP audit (FilterSchema/16 transform ops/VisibilityExpr/by-mode-pivot-transform = NOT); non-programmer UX (field-wells/Show-Me/slash/templates); 4-pane shell; packages (dnd-kit/cmdk/react-colorful adopt, RJSF/Craft.js refuse); V0–V8 roadmap (coverage MUST leads, ergonomics follow)
- [Element Config Schema Seam](adr_element_config_schema_seam.md) — kill shared-base bloat (ChromeConfig.brandTitle ISP/OCP); per-slice schema+useSlotConfig; base-minimality fitness fn; controls/KPI verdicts
- [SDMX-P1 Frontier](adr_sdmx_p1_frontier.md) — prioritized P1: ConceptScheme separation (V27) + dataset lifecycle FSM (V28) + CategoryScheme (V29) NOW; ref-metadata/quality/SDMX-REST deferred w/ doors; release≠dataset-lifecycle distinction
- [Constructor G3 live preview](adr_constructor_g3_live_preview.md) — inject live store into CanvasView via the SAME SiteProvider stores= seam the runner uses (buildStoreManifest); share the stats builder; keep empty-store as a toggle mode
- [Config & Render Vision](adr_config_and_render_vision.md) — config-object + SDUI renderer vs Vega-Lite/Grafana/Builder.io/RSC; 4 must-do P1 (engine validateConfig, full-page round-trip, generated whole-config JSON Schema, color-SSOT migration); gold-plating deferred; one-body fitness fns F1–F6
- [Semantic-Token / Theming Spine](adr_semantic_token_theming_spine.md) — 3-tier tokens; brand-neutral default + [data-tenant] override; byte-identical geostat (#0080BE not #005a9c); reconciliation role→value table; Strangler-Fig P0–Pfinal over ~34 files; FF-TOKEN-ONLY/THEME-COMPLETE/TENANT-OVERRIDE; de-tenant resolution (accent→tenant, neutrals→default)
- [No Privileged Element / Capability Nav](adr_no_privileged_element_capability_nav.md) — only navUtils hardcodes node types (section/georgraph/row); nav-contributor + nav-transparent caps + registry-driven visitor; DefaultSectionShell=misnamed fallback, SectionNavContext=generic anchor-nav; FF-NO-PRIVILEGED-NODE; P0–P5
- [Shell Variant / Style Spine](adr_shell_variant_style_spine.md) — kill hand-coded BEM modifier strings + inline variant→class logic in ~35 shells; meta-declared VariantDef → resolveVariants→data-attrs (mirrors PresentationProjector + resolveViewState); hero+compact→one emphasis enum; CSS-attr scoping NOT CSS-Modules (byte-identity); defineShell wires variantAttrs; FF-NO-VARIANT-CLASS; P0–P6
- [Multi-store / storeId re-introduction](adr_multistore_storeid_reintroduction.md) — VERDICT: spine ALREADY LIVE (buildStoreManifest+resolveStore+node storeKey, 3 real cubes seeded gdp/accounts/regional), not truly sidelined; what got dropped = the AUTHORING+ENVELOPE half (ApiResponse Tier-2/3, structureUrl, auth union, getMetadata/testConnection, blending); RE-ADOPT a contained slice (M0 prove routing + M1 metric→store via R1 + M2 Constructor authoring seam), DEFER envelope/auth/blending behind named doors D1-D3; per-platform steal-table (Grafana Mixed/Cube dataSource-on-measure/Tableau blend/Superset dataset/Looker model); FF-MULTISTORE-ROUTES/METRIC-NAMES-STORE/SOURCE-AUTHORABLE
- [Data-Source-Reference Spectrum](adr_data_source_reference_spectrum.md) — STATIC/HREF/STOREID = Vega-Lite data:{values|url|name}; recovered 3-mode model (datasource.ts kinds external|api|stats; source.ts StaticSource|ApiSource|InlineSource; 25-datasource-system.md sdmx-api/rest-json/static plugins + ApiResponse Tier1/2/3); delta: storeId LIVE (only registered kind), static SURVIVES FRAGMENTED (transform.source live + staticStore Null-Object + InlineSource selectors + unregistered external kind), href GHOST (ApiSource resolver returns null pointing at deleted FilterSchema.tsx); why-dropped=de-tenant collapsed to ONE postgres backend; UNIFY VERDICT = N source-kinds behind ONE DataStore port via buildStoreManifest kind-dispatch (choose A: kind-at-store-tier, REJECT B: node-level DataSpec.data union = F-A/F-B fragmentation), transform.source stays as bounded node-local inline-static sugar; per-platform steal (Grafana Infinity=href-kind/Superset upload→dataset/Retool resources/Tableau extract); register static NOW (real consumer=round-trip+tests+emptyManifest), DEFER href behind D-HREF (real format/auth/Suspense tail-cost), CLEAN the ghost (delete dead api branch); doors D-HREF=multistore-D1/D-STATIC-ASSET/D-EXTRACT; FF-STATIC-KIND/SOURCE-KIND-CLOSED/NO-FETCH-IN-CONFIG; S0-S3 path
- [Data-Reference / Render Vision](adr_data_reference_render_vision.md) — the 3rd axis: how nodes REFERENCE data. Renderer/data-model steal-table (Vega-Lite/Grafana/Tableau/Cube/Malloy/Looker/PowerBI/deck.gl); 12-ref-type inventory + verdict STRONG-GRAMMAR-FRAGMENTED-REFS (F-A too-many-DataSpec-branches, F-B five-$-ref-vocabs-four-evaluators, F-C MetricRegistry-orphan-not-in-binding-path); R1 semantic-layer-as-binding-spine + R2 enrich-channels + R3 desugar-convenience-specs + R4 unify-ref-taxonomy + R5 first-class-timeDimension; FF-METRIC-FLOWS/REF-RESOLVES/DESUGAR-EQUIV; LEAD extractRequirements+long-format, LAG semantic-layer-unwired
- [Data-Blending Decision (D3)](adr_data_blending_decision.md) — VERDICT = SHIP THE SEAM, DEFER THE PLANNER (the last data-source door). Gap named precisely: interpretSpec(spec,ctx,ONE-store) is the whole resolver registry's signature → no node reaches a 2nd store; manifest (ctx.stores) lives in REACT RenderContext, never threaded to core (Law-3-correct). joinByField ALREADY a built/tested hash-join (inner/left/outer, A-wins, non-mutating) but deliberately schema-less (source=pre-resolved EngineRow[], COVERAGE_TODO) — it is the right ENGINE, missing half = declarative front-door + 2nd-store resolution. Canonical shape (Vega-Lite lookup/Tableau primary-secondary/Grafana Mixed+join-by-field/Cube view/Malloy join_one) = ONE-sided shared-dim LEFT lookup, NOT a symmetric SQL planner; Law-1 generic dims make the link FREE (by:'time'). SHIP B0-B2: declarative `blend` transform step {from:{storeKey,query,encoding}, by, mode, fields, rename} resolved in REACT resolveBlends (resolveStoreByKey+interpretSpec secondary) → desugars to joinByField; NOT a new DataSpec discriminant (F-A guard), NOT a core resolver (arrow). DEFER behind D3-PLANNER: symmetric N-store/pushdown/server-side-join/blend-aware-extractRequirements/many-to-many/metric-level-blended-view (R1 destination, node-blend=bridge); trigger=3+stores OR too-big-to-client-join OR reused-blend. joinByField = interim custom-escape bridge (its inline-source bakes data into config = round-trip smell `blend` fixes by referencing a store). FF-BLEND-ROUTES-SECOND-STORE/DESUGARS-TO-JOIN/DECLARATIVE/ROUNDTRIP/KEY-GENERIC. Closes multistore-D3; secondary store = any source-kind (spectrum ADR)


---

> Entries below merged from platform (current @statdash content) during .claude SSOT reorg Phase 1.


## [platform] Project
- [Authoring-Schema SSOT epic](project_authoring_schema_ssot.md) — Constructor must CONSUME engine manifest/PropSchema not fork it; P1 SHIPPED (commit c1a635e: config-semantics SSOT prop-path.ts/prop-visibility.ts + evalShowWhen, 4 forks retired, FF-NO-FORKED-ISVISIBLE); P2 saveGuard capability-check / P3 chart colorMode+thresholds / P4 dataLinks remain, PARALLEL on shared helpers. Doc: work/DESIGN-authoring-schema-ssot.md
- [API Demo Parity](project_api_demo_parity.md) — root causes blocking NEW :3002 demo from OLD parity: pipe engine survived de-tenanting; gaps are ApiStore missing `display` channel + stats-api classifier/obs wire drift (label:{en,ka}, obs_value string, seq_pos in obs_attribute) + empty `aggregates` classifier + regional `_T` pollution; OLD spec at git 7a47e5d^
- [Live Provisioning](project_live_provisioning.md) — live statdash demo seeded by Flyway V1-V32 + R__ repeatable GOLD seed (NOT V5); fresh DB always re-seeds 3-dim placeholders unless R__ neutralized; GDP_ANNUAL canonical adds `approach` (DSD conflict), ACCOUNTS/REGIONAL match dim-set
- [GDP Page Dim Underspecification](project_gdp_page_dim_underspecification.md) — GDP page panels pin only measure+time; obs route does NO aggregation (dim_key @> raw rows); a 4-dim real GDP needs approach+geo pinned in every panel query or render garbles
- [Platform Layout](project_platform_layout.md) — real engine/* + apps/* paths; chart/table/kpi are *panels* not nodes; where framework seams live
- [Deferred Framework Seams](project_deferred_framework_seams.md) — published-but-unconsumed seams (getByCapability, node:status, PropSchemaForm, SwitchNode) + their activation triggers
- [Typecheck Baseline](project_typecheck_baseline.md) — root `tsc --noEmit`=0 is a FALSE GREEN; real gate is `cd apps/geostat && tsc -b` = ~293 pre-existing errors; tests 571/571 green
- [DB Layer](project_db_layer.md) — Phase-2 DB (PG+Timescale+LTREE+JSONB) now backed by Flyway V1-V10 at ops/postgres/migrations/; seed.ts implements the 3-bundle ETL
- [Ingestion Architecture](project_ingestion_architecture.md) — proposed Staged Submission Pipeline (Medallion bronze/silver/gold + 202-job + PUBLISH gate) to replace manual seed.ts
- [engine/core build gap](project_engine_core_build_gap.md) — core is a dist-pointing pkg w/ NO tsconfig + empty dist + undeclared @geostat/expr dep; only bundler apps consume it from source; Node consumers (apps/api) break
- [I18N DB](project_i18n_db.md) — two co-existing locale patterns (JSONB bags vs classifier_display.locale rows); canonical img timescaledb-ha:pg16 HAS ICU; recommend keep ka/en short subtags + config.locale registry
- [Classifier code-path ADR](project_classifier_code_path_adr.md) — ADR-0023: classifier hierarchy moves from surrogate-id-chain to code-chain LTREE over (dim_code,code); deletes upsert.ts Step 3/3b; V23/V24/V25 Strangler-Fig
- [Vintage-as-Release ADR](project_vintage_release_adr.md) — ADR-0025 SDMX-P0-2: release = publication-event aggregate stamped on observation + observation_revision (GUC app.release_id); as-of via pre-image overlay; V25 additive
- [ContentConstraint ADR](project_content_constraint_adr.md) — ADR-0027 SDMX-P0-1: cube region = predicate-row model (allowed-set per dim + conditional B9-only-on-U); validated in SILVER (ILLEGAL_COMBINATION); actual = view; V26 additive
- [Bootstrap Runner ADR](project_bootstrap_runner_adr.md) — ADR-0026: gut apps/geostat into a generic SDUI runner booting any site from GET /api/bootstrap; 3 entanglement points; compiled plugins + content-from-DB; store half already live
- [Bootstrap Phase B](project_bootstrap_phase_b.md) — ADR-0026 Phase B: extract geostat content to config.* via export-script→provisioning JSON; 3 structural gaps (publish-state, nav shape, label encoding); nav→site_config.nav blob; slug===config.id enforced
- [De-tenant Phase C ADR](project_detenant_phase_c_adr.md) — ADR-0028: gut geostat to a pure SDUI runner; extract facts/classifiers/displays to ops/seed-data/geostat/*.bundle.json (existing format:'bundle'); 2-stage preserve (R__ gold SQL + pipeline); buildManifest→emptyManifest; 4-way parity gate before deletion
- [Statistical Platform North-Star](adr_statistical_platform_north_star.md) — maturity-gap vision vs Eurostat/.Stat/IMF + VTL/dbt/PROV/FAIR/RDF-cube; 12-cap maturity matrix; 3 ports absorb the whole roadmap (Serializer `?format=`, silver RuleSpec, QuerySpec) + 2 cheap cols (submission.provenance, release.pid); TIER1=SDMX-serve·VTL·PROV; moat=lossless Constructor + Law-1 generic dims; all trigger-gated YAGNI
- [Excel Ingestion ADR](adr_excel_ingestion.md) — ADR-0030: declarative TemplateMapping spec (Frictionless+Tidy+SDMX-CSV) → upload-time adapter feeding createSubmission; code-from-label; DQAF; SUPERSEDED-AS-PRIMARY by adr-ingestion-build-ready (now the SECONDARY legacy→canonical path)
- [Ingestion BUILD-READY ADR](adr_ingestion_build_ready.md) — ADR-0031: THE consolidated build-ready design. Generic self-describing parser over canonical STRUCTURE+CL_*+DATA workbooks (verified in DATA/canonical/*.xlsx) → existing bronze contract → createSubmission → existing pipeline. 6 improvements as seams (OCP self-describe, codelist/DSD declare-OR-reference+SCD-2 versioning, validation-as-data VTL-ready RuleSpec, W3C PROV 2-col, compat-check codelist-open/DSD-gated, SIMS slot + reserved Serializer/QuerySpec ports). Parallelizable 6-wave roadmap + BAKE-NOW vs YAGNI-SEAM ledger + nothing-missed edge-case checklist. work/legacy-to-canonical/* converter exists; xlsx undeclared dep; migration SQL absent this checkout

- [Responsive CSS Model](project_responsive_css_model.md) — binding spine = work/DESIGN-css-responsive-standard.md (300-line ceiling); 2 root-cause decisions both fitness-guarded: panel sizing = honest height band `--size-panel-height` clamp(380,56vh,560) NOT aspect-ratio (deprecated alias; --ar-* cascade + ≤1280 reset deleted); KPI strip = count-aware @container ladder over data-kpi-count NOT auto-fit (kills 3+1 orphan); Playwright harness-verified; staged: ratio→size token migration, cqi container-fluid, count-clean layout-node mode, retire 1100/700 off-scale bps
- [Chrome/Panel/Section unification](project_chrome_panel_section_unification.md) — SHIPPED 2026-07-01 (ade152b): body-only height model — token on .panel__body/.section__body via bodyProps, NOT the .panel-col wrapper; map-collapse blocker RESOLVED, real-browser layout-verified (chart-wrap definite height 380→560 cqi every width, no Leaflet collapse). GeographShell = only <PanelLayout> consumer (bodyProps thread = complete). counter-rule DELETED; --ar-* inert vars retired (a286833). STILL aspirational: SectionShell-as-PanelLayout-adapter + isOpen/disclosure/rootProps (only bodyProps built); "panel" size-token migration deferred (needs live-render verify)
- [RSP defects R1/R2/R3 status](project_responsive_css_model.md#rsp) — R1/R2/R3 were ALREADY FIXED in code by 2026-06-30 (CLOSE-BOARD snapshot stale); this session added the missing fitness LOCKS: FF-HEADER-NO-OVERFLOW (622410f) + FF-PAGE-MEASURE-SSOT (bc865af, also added the missing plugins-vitest pages/** include). Remaining subsystem work needs the docker api+db stack (unavailable in-shell): live-tile/legend/padding screenshot pass, aspectRatio→"panel" token Strangler, RSP-R5/F5/F9, mobile-first authoring migration, L1 Constructor-canvas audit

## [platform] Feedback — corrections & validated approaches
- [engine/react locale-agnostic](feedback_engine_react_locale_agnostic.md) — hook BLOCKS Georgian codepoints + 'ka' literals in engine/react; use 'en'/'fr' in tests
