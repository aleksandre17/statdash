---
name: semantic-token-theming-spine
description: Pointer to ADR-006-semantic-token-theming-spine.md — 3-tier tokens, brand-neutral default + [data-tenant] override, byte-identical geostat, CSS attribute scoping (not CSS-Modules). Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-006-semantic-token-theming-spine.md`** (Phase-2 SSOT reorg).

Semantic-Token / Theming Spine — 3-tier tokens, brand-neutral default + [data-tenant] override, byte-identical geostat, CSS attribute scoping (not CSS-Modules). Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
