---
name: ingestion-pipeline
description: Pointer to ADR-004-ingestion-pipeline.md — generic self-describing canonical-workbook parser (PRIMARY); per-template declarative mapping demoted to SECONDARY legacy->canonical path. Status: Proposed (build-ready).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-004-ingestion-pipeline.md`** (Phase-2 SSOT reorg).

Ingestion Pipeline — generic self-describing canonical-workbook parser (PRIMARY); per-template declarative mapping demoted to SECONDARY legacy->canonical path. Status: Proposed (build-ready).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
