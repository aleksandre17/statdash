---
name: shell-variant-style-spine
description: Pointer to ADR-013-shell-variant-style-spine.md — meta-declared VariantDef -> resolveVariants -> data-attrs (mirrors PresentationProjector); hero+compact -> one emphasis enum; CSS attribute scoping. Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-013-shell-variant-style-spine.md`** (Phase-2 SSOT reorg).

Shell Variant / Style Spine — meta-declared VariantDef -> resolveVariants -> data-attrs (mirrors PresentationProjector); hero+compact -> one emphasis enum; CSS attribute scoping. Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
