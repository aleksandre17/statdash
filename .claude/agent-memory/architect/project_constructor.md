---
name: constructor
description: Pointer to ADR-003-constructor.md — open store model, PropSchema inspector, lossless round-trip; G3 live preview via the runner's buildStoreManifest seam. Status: Accepted (substantially built).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-003-constructor.md`** (Phase-2 SSOT reorg).

Constructor (authoring core + live preview) — open store model, PropSchema inspector, lossless round-trip; G3 live preview via the runner's buildStoreManifest seam. Status: Accepted (substantially built).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
