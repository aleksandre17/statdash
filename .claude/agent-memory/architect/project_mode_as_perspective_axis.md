---
name: mode-as-perspective-axis
description: Pointer to ADR-005-mode-as-perspective-axis.md — generalize privileged mode into a generic perspective axis (ctx.perspectiveState); delete by-mode with no shim; permalink generated from the registry. Status: Proposed (final, ready).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-005-mode-as-perspective-axis.md`** (Phase-2 SSOT reorg).

Mode as a Generic perspective View Axis — generalize privileged mode into a generic perspective axis (ctx.perspectiveState); delete by-mode with no shim; permalink generated from the registry. Status: Proposed (final, ready).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
