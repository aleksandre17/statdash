---
name: multistore-storeid
description: Pointer to ADR-010-multistore-storeid-reintroduction.md — routing spine LIVE (buildStoreManifest+resolveStore+storeKey, 3 cubes); re-adopt metric->store + authoring slice, defer envelope/auth/blending behind D1-D3. Status: Accepted (implemented).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-010-multistore-storeid-reintroduction.md`** (Phase-2 SSOT reorg).

Multi-store / storeId Re-introduction — routing spine LIVE (buildStoreManifest+resolveStore+storeKey, 3 cubes); re-adopt metric->store + authoring slice, defer envelope/auth/blending behind D1-D3. Status: Accepted (implemented).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
