---
name: platform-constructor-vision
description: Pointer to ADR-002-platform-and-constructor-vision.md — config-object SSOT + pure SDUI renderer; enforce contract server-side + publish JSON Schema; Constructor coverage LEADS, ergonomics follow. Status: Proposed (vision).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-002-platform-and-constructor-vision.md`** (Phase-2 SSOT reorg).

Platform & Constructor Vision — config-object SSOT + pure SDUI renderer; enforce contract server-side + publish JSON Schema; Constructor coverage LEADS, ergonomics follow. Status: Proposed (vision).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
