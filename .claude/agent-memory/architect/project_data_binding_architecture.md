---
name: data-binding-architecture
description: Pointer to ADR-001-data-binding-architecture.md — one DataStore port + N source kinds; semantic-layer binding spine (R1); ship the blend SEAM, defer the planner (D3-PLANNER). Status: Accepted (partially implemented).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-001-data-binding-architecture.md`** (Phase-2 SSOT reorg).

Data-Binding & Data-Reference Architecture — one DataStore port + N source kinds; semantic-layer binding spine (R1); ship the blend SEAM, defer the planner (D3-PLANNER). Status: Accepted (partially implemented).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
