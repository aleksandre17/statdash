---
name: time-range-readiness-seam
description: Pointer to ADR-011-time-range-readiness-seam.md — store-builder folds server time coverage into store.classifiers['time'] at build (A-variant) so pick:last resolves sync; +2 guards. Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-011-time-range-readiness-seam.md`** (Phase-2 SSOT reorg).

Time-Range Readiness Seam — store-builder folds server time coverage into store.classifiers['time'] at build (A-variant) so pick:last resolves sync; +2 guards. Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
