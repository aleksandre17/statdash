---
name: sdmx-p1-frontier
description: Pointer to ADR-007-sdmx-p1-frontier.md — P1 NOW = ConceptScheme (V27) + dataset-lifecycle FSM (V28) + CategoryScheme (V29); defer ref-metadata/quality/SDMX-REST behind doors. Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-007-sdmx-p1-frontier.md`** (Phase-2 SSOT reorg).

SDMX P1 Frontier — P1 NOW = ConceptScheme (V27) + dataset-lifecycle FSM (V28) + CategoryScheme (V29); defer ref-metadata/quality/SDMX-REST behind doors. Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
