---
name: element-config-schema-seam
description: Pointer to ADR-009-element-config-schema-seam.md — kill shared-base bloat; per-slice schema via useSlotConfig; base-minimality fitness function (ISP/OCP). Status: Proposed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-009-element-config-schema-seam.md`** (Phase-2 SSOT reorg).

Element Config Schema Seam — kill shared-base bloat; per-slice schema via useSlotConfig; base-minimality fitness function (ISP/OCP). Status: Proposed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer; the ADR is the SSOT.
