---
name: bootstrap-phase-b
description: Pointer to ADR-018-bootstrap-phase-b.md — Bootstrap Phase B (orig. ADR-0026 Phase B): geostat SiteManifest from Postgres; nav as site_config.nav JSONB blob; publish as a property of the provisioning upsert. Status: Designed.
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-018-bootstrap-phase-b.md`** (Phase-2 SSOT reorg).

Bootstrap Phase B (orig. ADR-0026 Phase B): geostat SiteManifest from Postgres; nav as site_config.nav JSONB blob; publish as a property of the provisioning upsert. Status: Designed.

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer.
