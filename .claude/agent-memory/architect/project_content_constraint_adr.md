---
name: content-constraint-adr
description: Pointer to ADR-016-content-constraint.md — ContentConstraint cube region (orig. ADR-0027, SDMX-P0-1): predicate-row allowed-set validated in SILVER (ILLEGAL_COMBINATION), actual region as a view. Status: Accepted (implemented).
metadata:
  type: project
---

**Decision record migrated to `docs/architecture/decisions/ADR-016-content-constraint.md`** (Phase-2 SSOT reorg).

ContentConstraint cube region (orig. ADR-0027, SDMX-P0-1): predicate-row allowed-set validated in SILVER (ILLEGAL_COMBINATION), actual region as a view. Status: Accepted (implemented).

Full context, decision, rejected alternatives, consequences, and the verbatim design record now live in the ADR. This memory is only the pointer.
