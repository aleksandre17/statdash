# Memory Index

## Boot hydrate / probe-selector diagnosis
- [Hydrate append-vs-replace + selector scoping](project_hydrate_append_vs_replace_and_selector_scoping.md) — duplicate-key UUIDs falsified a plausible "metric collision" hypothesis (was a StrictMode double-hydrate append race, sibling of an already-fixed `setPages` defect); + an insertion-palette tile and the live canvas overlay sharing one data-attribute silently no-op'd a probe click — scope selectors to the real container, confirm via a positive emitted signal
- [kpi-strip data-bindable misdiagnosis](project_kpistrip_data_bindable_misdiagnosis.md) — a "boot-path ctx divergence" was falsified by a no-click control; real cause was a false `data-bindable` cap letting a stray Inspector bind hijack `effectiveStoreKey`'s generic store routing for a whole kpi-strip. A union CSS selector + `.first()` (chart-or-kpi-strip) manufactured the false boot-path correlation. + the panel dev-container's `packages/*` bake-in gap (no live sync — `docker cp` + restart required)

## Canvas / authoring geometry
- [Async-path double node-anchor stamp](project_async_path_double_middleware.md) — 0109 whole-node selection collapse (16/16 frames → 0×0 at origin): renderNode's ASYNC store path applied `middleware.after` TWICE (renderWithRows inner + outer Suspense wrap) → the ONE canvas node-anchor middleware stamped `data-part-node-id` twice ONLY in live/async mode; sync/structural single-stamped so it "worked" there. Dossier's "middleware vs slot-part path" origin guess FALSIFIED (no PartAnchor-nodeId stamp exists). Fix: `renderWithRows(applyAfterMiddleware)` + `resolveAnchorBox` descends all display:contents + FF-NODE-FRAME-NONDEGENERATE. "Works structural, breaks live" ⇒ sync-vs-async engine divergence
- [Drop-port geometry falsified](project_drop_port_geometry_falsified.md) — "container/page drop-ports collapse to 4×4 at (0,0)" was FALSE: the `display:contents` anchor is {0,0,0,0} but `measure()` reads `firstElementChild` = the real box (page main 603×640, empty grid/section 539×72); a real HTML5 drop LANDS a child. Actual drop-into-container blocker = the atomic-CDP mount race (ports gated on `dragging`), not geometry — not human-facing. jsdom can't pin geometry (0×0 layout); the pin is a Playwright e2e that reveals ports via real dragstart then measures/drops (`e2e/dropTargetGeometry.e2e.ts`)

## Config store / restore
- [DataSpec homes: inline-page vs orphan workbench](project_datospecs_orphan_workbench_vs_inline_page.md) — published pages EMBED their DataSpec inline in `page.config` (diff vs provisioning); `/api/config/data-specs` is orphan authoring "(შემოთავაზებული)" scratch with NO provisioning/git baseline & backs no surface. A GDP-dynamics "spec corruption" report was falsified: published gdp-dynamics section was pristine (`gdp.current`, resolves 22bn→104bn); corruption confined to unbound workbench specs flipped to GDP_AGRI (0 rows). No fabricated restore target
- [Config source versioning gap](project_config_source_versioning_gap.md) — `dataSource:"regional"` routes to a cube via `config.data_source.config.datasetCode` (the SSOT); a raw-browse spec's ~72-measure blob is identical across cubes and NOT a corruption signal. Incident: auto-save flipped regional datasetCode REGIONAL_GVA→GDP_ANNUAL (kept sector dims GDP lacks) → every regional query served national GDP, 0 rows for regional geos. config.data_source/data_spec have NO version history (destructive PUT; versions are pages-only) → git provisioning JSON is the only clean baseline. Diagnose regional-data-wrong at the SOURCE datasetCode first, not the specs

## Data / query windowing
- [Year-window default vs data extent](project_year_window_default_data_extent.md) — range fromYear/toYear defaults resolve against the time CODELIST/classifier, not the measure's realised periods; overshoots when a measure ends before the codelist (regional GVA 2010-2024 vs codelist 2010-2025) → empty final year / honest em-dash. Plus the regional `_T`-default KPI gap + stale token yardstick
- [Metric-id-in-filter self-mismatch](project_metric_id_filter_selfmatch.md) — a migration text-substitution leaked a governed metric-id into a raw `filter.measure` dimension pin the engine never resolves → 0 rows on gdp/regional queries, live AND in tests; presented as a fake "golden drift" (closest-number pool coincidence) and a KPI honest-no-data state — one cause, two symptoms; the codelist-existence guard stayed green because its own validation generously resolves what the runtime never does
- [Natural-axis vs concrete total](project_natural_axis_concrete_total.md) — a metric-natural browse kept a foreign regional geo pin because the national total is carried at the CONCRETE country code `geo='GE'` (not SDMX `'_T'`), so `isNatural` ("any concrete member") mis-classified geo → empty slice → null/-100 every year; NOT store routing (dataSource routed fine). Fix: neutralize by MEMBERSHIP (`!pinPresent`), not axis-existence. The durable trap: a `'_T'`-modeled fixture can never exercise a total encoded as a concrete code
- [Preview-path store-routing SSOT](project_preview_path_store_routing_ssot.md) — the COMPLEMENT of the above: the workbench preview hook grabbed `stores[Object.keys(stores)[0]]` (first/page store) instead of the renderer's `specDataSource→resolveStore` SSOT, so a `gdp`-sourced metric on a regional page read REGIONAL_GVA → 0 rows LIVE while every engine fixture (resolves against the metric's own store) stayed green. Any new read surface that selects a store must reuse that SSOT; guard with two stores answering one code with different values. RESIDUAL (0122): a raw wildcard section spec (`measure:'*'`, no metric) has no `specDataSource` to fall back on either — the missing 3rd cascade tier is `page.storeKey`, absent from both the source-read AND Tier-3-default stores; +`PageMeta` type gap (`storeKey` lived only on `NodeBase`, not `PageConfigBase`)

## Auto-relocated (memory-home-guard — reconcile into a topic section)
- [Async-store live-render patterns](project_async_store_live_render_patterns.md) — the two-store contract (sync ExternalStore vs async ApiStore/CachedStore) + a 12-trap catalog for "renders in tests, broken live" bugs (dropped pins, cache-key node-uniqueness, warm≡read key, $ne/comma client-side-only, tree-shaken register*(), 304-to-empty, loading-vs-empty, span-derived hidden params) + render/CSS traps (`??`-vs-`||`, value-shading ramps, height-chain, Leaflet hidden-remount)
- [LocaleString resolve boundary](project_localestring_boundary.md) — the positional i18n boundary: resolve {ka,en}→scalar ONLY at/after React; combine per-locale + re-tag (composeLocale) or resolve in shells; discriminate objects by field-identity not shape; honest types + full-page fitness matrices
- [Probe methodology: hard vs soft nav](project_probe_methodology_hard_vs_soft.md) — goto-only probes hard-load and can't see soft-nav bugs (scroll restoration, theme-at-first-paint, leaked route state); replicate the SOFT path; apply data-theme synchronously pre-render; theme-reading useMemo needs a theme-epoch dep
- [Wire type-lie → LocaleString render crash](project_wire_type_lie_localestring_render.md) — panel hand-mirrored wire type says `label:string` but live returns `{en,ka}`; raw `{d.label}` render throws React #31 and unmounts the editor; e2e mock echoes the lie so CI stays green. Repro via Playwright→:3008 + `page.on('pageerror')`
- [Vitest 4 workspace removed](project_vitest4_workspace_removed.md) — vitest.workspace.ts is silently ignored in Vitest 4; use root vitest.config.ts test.projects; "Failed to resolve entry across many suites" → suspect the project-config layer, not aliases
- [Typecheck peer-dep resolution](project_typecheck_peer_dep_resolution.md) — real gate is `tsc -b apps/geostat/tsconfig.app.json`; engine peer deps need tsconfig `paths` to apps/geostat/node_modules (+@types redirect for type-less deps); never add the dep to an engine package
- [Barrel export gaps](project_barrel_export_gaps.md) — a symbol defined-but-not-re-exported from the engine barrel surfaces as "X is not a function", often swallowed by try/catch and mis-reported as a data 'error' status; grep `export (function|const)` and add the re-export
- [Escalated type decisions](project_escalated_type_decisions.md) — two tsc clusters that are architectural decisions, not mechanical fixes: control `category` vs SliceCategory taxonomy, and the `custom` DataSpec union gap (deliberately unresolved — needs team intent)
