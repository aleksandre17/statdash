---
name: project-benchmark-corpus
description: The benchmark corpus (statdash vs reference-platform class) — platform-architect owns it; consulted at every Leader's Scan
metadata:
  type: project
---

`docs/architecture/BENCHMARK-REFERENCE-PLATFORMS.md` is the capability-comparison SSOT (statdash vs Form.io/Builder.io/Grafana/Superset·Metabase/Retool·Appsmith). I (platform-architect) OWN it.

**Why:** owner mandated the lead operate as a principal innovator — continuously benchmarking against the reference class, spotting functional LAGs, completing half-formed concepts. That needs a maintained corpus; I created it 2026-07-08 (first Leader's Scan).

**How to apply:** at every Leader's Scan — (1) re-ground each "We-today" cell against the cited file (code is authoritative, not the matrix); (2) re-rank Lag/Missing rows by (Constructor leverage × distance); (3) new Missing row or a leader shipping a new model = refresh trigger → add row + propose initiative into `ARCHITECTURE-REGISTRY.md` (never into the benchmark file — it stays one-body). Keep the "None" rows visible (versioning/expr/validation/tokens/i18n/a11y are genuinely reference-grade — honest calibration).

**Grounded standing (first scan):** reference-grade = schema versioning/migration (`CURRENT_SCHEMA_VERSION=5` chain + per-node version+migrate + forward-compat guard), node registry+palette (`NodeSliceMeta`+`describeApp()`), expr sandbox, two-tier validation, DTCG tokens, i18n LocaleString, WCAG AA. Lag/Missing = interaction WRITE adapter (AR-36/42 registered), plugin SDK (internal-only), authoring governance (version rows only), export/embed (stub), RBAC (missing).

**First scan's 3 NEW registered initiatives:** AR-46 (Extension/Plugin SDK), AR-47 (config authoring governance draft→review→publish + audit + diff/rollback), AR-48 (headless export/embed/snapshot port). Do NOT re-propose the already-registered visions: cross-filter/interaction (AR-36/38/42), semantic layer (AR-40), data lineage (AR-43), multi-tenancy (AR-30, owner-DEFERRED). See [[maximal-orthogonality]] for the defer-until-real-consumer discipline.

**2026-07-10 — Part II added (deep CONSTRUCTOR study).** The file now carries TWO instruments: Part I = the capability matrix (Leader's-Scan, unchanged); **Part II = durable design reference** — per-group constructor distillation (page/site builders · app builders · Figma · docs/structured-content · BI/semantic · form-schema), each with mental model + contextual-simplicity mechanisms + our-quadrant gap; then canonical-principles synthesis (14, attributed+seam+wave), the sharpened our-better thesis (8 points), and NEW candidates N1–N9. Honesty: expert distillation (no live web), grounded in cited seams. Our-quadrant = **governed × simple × no-query** (Looker governance + Tableau directness + Figma contextual editing + Notion non-programmer surface — no single leader holds all).
**Grounded our-better anchors (real seams found this study):** `apps/panel/src/features/data-layer/showme/ShowMe.tsx` = Tableau "Show Me" projected from SDMX *semantic role* (richer than datatype heuristic); `.../fieldwells/binding.ts` = Tableau/PowerBI field-wells with a **byte-identical** drag-path===typed-editor invariant Tableau lacks; `packages/core/src/config/prop-schema.ts` `PropertyGroup` already cites Retool/Appsmith + `PropFieldType` carries `DataSpec`/`ChartDef` (statistical field types no generic form lib has).
**2026-07-17 — D6 cycle refresh (Authoring-Canon layer scan).** Appended a dated section to `BENCHMARK-REFERENCE-PLATFORMS.md` (Part I) + created `docs/architecture/proposals/CAPABILITY-INJECTION-BACKLOG.md` (I own it; companion action-list to the benchmark). Four seed hypotheses tested: (1) derived metrics = SHARPENED — ratio/share/derived already ship (AR-40/49-M3.0/50-M5); only **time-relative** (YoY/growth/cumulative) is the gap = AR-49 M3.2. (2) versioning/rollback = CONFIRMED thin (AR-47, W5). (3) lineage+impact = PARTLY REFUTED — steward `computeMetricImpact` ships; reader lineage = AR-43 horizon. (4) declared ingest DQ = CONFIRMED ABSENT — the headline our-better (Great Expectations × SDMX × honest-state, rides W2 front door). Top-3 formed recs: DQ-on-ingest→W2 · time-relative metrics→W2 (engine seam, after corpus migration) · config diff/rollback→W5. Refused (out of stats domain): A/B testing, alerting, RLS. Named the executing-CI gate (Stage-0/AR-53) as the highest-leverage non-feature precondition. Delivery ledger (shipped): dynamic binding/thresholds/per-breakpoint/schema-aware-editor (orchestrator `capability-injection-pipeline`).
**NEW candidates to fold into roadmap (net-new, NOT in M4/registered):** register as ARs → **N1 reusable node/section symbols w/ per-instance overrides** (Figma/Webflow; instances rebind a different governed metric — L, packages-crossing), **N2 collection/repeat over a governed dimension** (Webflow CMS list; one governed number per member — L; N1+N2 = self-building regional profiles), **N3 metric certification lifecycle** draft→certified→deprecated badge (Tableau Certified/PowerBI endorsement; generalizes Law-9 preliminary; noun-level, complements AR-47 — M). Fold into M4 → N4 slash-insert (→W1), N5 breakpoint preview (consumes responsive model — W7-adjacent), N7 faceted catalog (§2.2 follow-on, YAGNI-gated). Extend M3 → N6 visual derived-metric builder (no formula text; keeps FF-AUTHOR-NO-QUERY). Route into existing → N8 consumer provenance popover→AR-43/44, N9 review-comments→AR-47.
