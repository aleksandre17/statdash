---
name: project-canonical-panel-ia
description: 0102 apex study I own — STUDY-canonical-panel-ia.md; spine EXISTS in engine, chaos = unprojected middle (skeleton LOST as projection); skeleton = page-kind × PresetDecl; remedy R1-R6
metadata:
  type: project
---

`docs/architecture/proposals/STUDY-canonical-panel-ia.md` (I am sole author, 2026-07-19, card 0102) — the owner's whole-panel "fallen out of the canon" circle-break, the canonical site-builder object-model/IA study.

**Verdict (the load-bearing correction of the lead hypothesis):** the engine object model ALREADY carries the full site-builder spine — the chaos is the AUTHORING PROJECTION. The skeleton is literally lost as projection, not absent as model: page kinds registered (`plugins/pages/meta.ts` — container/landing/inner/tab, `sliceType:'page'`) but `PageBrowser.tsx:89` hardcodes `DEFAULT_PAGE_TYPE`, no chooser/retype; `starterTemplates.ts` = panel fixtures outside every registry (a second "curated whole" home vs ADR-049 presetRegistry); chrome region+order chain reference-grade in engine (`resolveChrome.ts:6-10`) but projected as a raw NumberControl (`chromeFacetModel.ts:56`) — a MODALITY defect.

**Corrected hierarchy (owner's, with skeleton lifted between Site and Page):** DATA → SITE → SKELETON → PAGE → SECTION/ELEMENT(+preset) → BINDING → PUBLISH. Reference-class convergence: skeleton = site-level asset instantiated by pages (WP Template, Wix masterPage, Puck root, Builder page-model); chrome belongs to the skeleton and is ARRANGED not configured; IA = the containment projection.

**THE move (D-0102-1, near one-way door, rec Option A):** skeleton = *registered page-kind × page-level `PresetDecl`* (`NodeSeed.type` seeds a page root — ONE generalization/confirmation of ADR-049 P2b), NEVER a separate template entity/document (Option B rejected = fifth grammar). New guards: FF-SKELETON-CHOOSABLE · FF-STARTERS-ARE-DECLARATIONS.

**Remedy sequence:** R1 = 0101 floor (empty-container affordance + page containment + URL) · R2 = 0100-P2b presetRegistry · R3 = skeleton restoration (kind gallery + retype + fixtures→declarations, STANDS ON R2) · R4 = chrome visual arrangement (ADR-042 modality) · R5 = ONE Site workspace + rail-as-spine (absorbs [[project-panel-canonical-relay]] Step 1 + resolves SPEC-studio-ia-canonical S5 sign-off) · R6 = data-floor polish (structurally already canon per 0091/0099).

**How to apply:** once owner blesses, 0101 and 0100-P2b re-sequence as R1/R2 of THIS program; refuse any new template document species; R3 must not start before R2's registry exists (would mint a second fixture home).
