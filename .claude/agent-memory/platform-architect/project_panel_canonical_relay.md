---
name: project-panel-canonical-relay
description: The whole-panel re-lay blueprint I own — four-moment IA + three laws + Strangler sequence; the coherent whole-ordering of already-routed work
metadata:
  type: project
---

I OWN `docs/architecture/proposals/BLUEPRINT-panel-canonical-relay.md` (2026-07-15) — the owner's WHOLE-panel canonical re-lay of `apps/panel` (the Constructor), demanded holistically ("go through EVERYTHING as a whole, not a narrow slit").

**The IA:** the panel already has the reference-class six regions (top/rail/left-panel/canvas/inspector/bottom). Re-cast as the **four moments** — DATA promoted to rail-mode #1 (the front door, replacing the top-bar Compose⇄Data screen-swap), COMPOSE = Add-mode+canvas, REFINE = author-plane Inspector, PUBLISH = a top-right terminal (PageWorkflowBar re-seated). One ordered rail: `Data · Add · Layers · Site · Style`.

**Three laws, file-anchored:** PLANE (`PropField.plane`/`FacetDescriptor.plane`; hide 6 leak classes — `vars`@pageSchemaSource.ts:67, `presentation.crumbs`, coordinate `dim→value`, raw `field`, VISIBILITY facet@builtinFacets.ts:105) · CONTAINMENT (layout-node boxing; retire bespoke page-div + `row`) · ONE-PLACE (kill duplicate doors: data-model has 3, page-nav 2, site/brand 2 — `StudioTopBar.tsx`).

**Anti-circle framing (the load-bearing point):** NOT a new vision. ADR-041/042 untouched. ~90% re-homing of existing surfaces + already-routed work (PLANE=W3/PM-B, honest canvas=W1, metric-first=W2, composeConstructor=audit G1). It IS ROADMAP H1 made real.

**Why:** owner exhausted by slice-by-slice fixing; needed one coherent blueprint the lead drives to a visibly re-laid panel on :3013.
**How to apply:** THE first motion to drive = Step 1, the Four-Moment Shell (one rail Data-first + top-right Publish terminal + strip the top bar's scattered doors) — most whole-visible, lowest-risk. Builds on [[project-panel-quality-ledger]] (the six-dimension audit that anchors the leak lists).
