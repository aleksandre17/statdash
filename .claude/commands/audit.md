---
description: Audit a scope against ISO 25010 + anti-patterns; write findings as cards
argument-hint: [scope/target]
---
Run the **/audit** playbook. The canonical, full procedure lives at `.claude/kit/commands/audit.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
