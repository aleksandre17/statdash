---
description: Sequence work into layers with deployment strategy + DORA framing
argument-hint: [scope/target]
---
Run the **/roadmap** playbook. The canonical, full procedure lives at `.claude/kit/commands/roadmap.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
