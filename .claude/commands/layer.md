---
description: Build the next layer: spec → plan approval → build → continuous audit
argument-hint: [scope/target]
---
Run the **/layer** playbook. The canonical, full procedure lives at `.claude/kit/commands/layer.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
