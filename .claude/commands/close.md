---
description: Settle the board, update memory, write ADR + learning note
argument-hint: [scope/target]
---
Run the **/close** playbook. The canonical, full procedure lives at `.claude/kit/commands/close.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
