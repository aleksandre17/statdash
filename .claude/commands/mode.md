---
description: Switch the session operating mode (build/plan/review/strict/fast)
argument-hint: [build|plan|review|strict|fast]
---
Run the **/mode** playbook. Canonical procedure: `.claude/kit/commands/mode.md` — read it and follow it exactly (persist via `echo <name> > .claude/session/mode`). Args: $ARGUMENTS.
