---
description: Refactor a target to standard (Strangler-Fig, incremental)
argument-hint: [scope/target]
---
Run the **/refactor** playbook. The canonical, full procedure lives at `.claude/kit/commands/refactor.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
