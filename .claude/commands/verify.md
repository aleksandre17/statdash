---
description: Health check — live-fire the hooks against this project's config
argument-hint: [scope/target]
---
Run the **/verify** playbook. The canonical, full procedure lives at `.claude/kit/commands/verify.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
