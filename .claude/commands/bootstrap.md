---
description: Detect architecture, scaffold missing slots/agents/skills/board, validate
argument-hint: [scope/target]
---
Run the **/bootstrap** playbook. The canonical, full procedure lives at `.claude/kit/commands/bootstrap.md` — read that file now and follow it exactly for this project, under the orchestrator's doctrine (`.claude/kit/INDEX.md`). Any text after the command is the scope/target: $ARGUMENTS.
