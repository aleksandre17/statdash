# Geostat app environment — copy to .env.dev / .env.prod (gitignored)

# API endpoint (leave empty to use MSW mock store in dev)
VITE_API_URL=http://YOUR_SERVER_IP:PORT

# Store mode: "mock" (MSW) | "api" (live backend)
# VITE_STORE_MODE=mock
