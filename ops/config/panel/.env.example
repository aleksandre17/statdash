# Panel app environment — copy to .env.dev (gitignored)
# Phase 2 Constructor; minimal config for now.

# API endpoint for the Constructor backend (Phase 2)
# VITE_PANEL_API_URL=http://localhost:3001
