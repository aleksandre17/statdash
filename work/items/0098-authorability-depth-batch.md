---
id: "0098"
title: "AUTHORABILITY DEPTH BATCH — the five opaque sub-objects become offered forms (parity audit top-list A)"
status: QUEUED (2026-07-18 — dossier `docs/architecture/audit/AUTHORABILITY-PARITY-2026-07-18.md`; one cohesive projection wave, the role/schema-projection family)
class: M
priority: P0
owner: lead → build agent (Opus)
implements: the audit's Direction-A top-5 — all one shape (rich sub-objects falling back to raw JSON), all cured by the SAME schema-projection discipline that 0087 proved
---
1. **Structured TREND builder** (~33 occurrences, the highest-frequency opaque bag): the `trend` union (yoy/cagr/share num-denom) becomes a real offered form (`TrendField`, the ThresholdField template); `KpiValueItemSchema` gains share's num/denom. *Grafana value-options completeness.*
2. **`$prev` in CalcBuilder:** the MetricInput `at.time.$prev` lag surfaced visually — the growth-by-lag class composable without JSON. *dbt/Looker period-over-period.*
3. **Wire the EXISTING VisibilityBuilder onto item-level `when`** (42 occ) + `view.visibleWhen` — a projection, not a build; lowest cost, high visibility.
4. **Filter options-source + default-from-options + spanRole pairing** — kill the `type:'object'` escape hatch in `param-schemas.ts`. *Builder.io schema-driven editors.*
5. **Perspective onEnter/onExit resets (6 occ) + perspective-keyed page-header badge** (keyed-map field, registry OCP).

**Direction-B moves (already homed — pointers, no new cards):** the `query`→`pipeline` emission flip = the ⛔ W-P5 door (gated as-is) · governed-growth-instead-of-hand-yoy = after #1+0090 (the builder emits either) · redundant `value.filter` coordinate → absorbed by 0084 promotion + 0090 passport · CHECK-4 verified CLEAN (the b544819 guard holds).

**DoD.** Each of the five authored live through its offered form on :3013 (author a trend, a $prev growth metric, an item-when, an options-source, a perspective reset — zero hand-JSON); gates green (full ritual if core schemas touched); screenshots; the corpus's construct classes now ALL verdict «authorable».
