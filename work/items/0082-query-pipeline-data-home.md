---
id: "0082"
title: "QUERY AS A VISIBLE PIPELINE + THE RAW-DATA HOME — simple at full power, once and for all"
status: GO (owner-blessed 2026-07-17 verbatim «ნდობას გიცხადებ, გააკეთე»; ADR-046 ACCEPTED; SPEC final incl. lead's §9 elevation; waves W-P0…W-P6 fire serially per WIP=1 — first build wave starts when the integrity cluster (data-truth) lands)
class: M
priority: P0
owner: lead → platform-architect (design) → build agents (waves)
implements: owner 2026-07-17 (verbatim, condensed): «query და pipeline მაქსიმალურად მარტივად და სრული ძალით, დატა-ელემენტებზე · ნედლი დატა ერთხელ და სამუდამოდ გამიჯნე, თავის კანონიკურ ადგილას · დღევანდელი query-აწყობა გაუგებარია (ტეგები, ერთად გამოტანილი) · აწყობისას ნედლი დატა ჩანდეს · და ჩანდეს რა query გამოდის» — Canon C1 continuation
depends_on: ["0072"]
links:
  - docs/architecture/proposals/CAPABILITY-INJECTION-BACKLOG.md   # rec #1 (DQ-on-ingest) folds INTO this data-home concept
  - platform/apps/panel/src/features/data-layer/                  # today's query editors (the confusion to be replaced)
---
**Intent.** The author must build a query as a VISIBLE, STEPPED PIPELINE — seeing the raw data flow through every step and the resulting declarative query alongside — while raw data itself gets ONE canonical home, separated from the semantic model and specs forever. Full power, simple perception, whole standards.

**Reference anchors (Law 4 — adopt whole):** Power Query's step-pipeline with a live grid per step · Grafana's builder↔code duality (the generated query always visible) · Vega-Lite / Tidy-Data transform grammar (declarative verbs: filter/aggregate/derive/pivot) · SDMX (our ObsQuery stays the wire truth) · the W2 governed-noun spine (metrics/dims as vocabulary, never raw codes in the author plane).

**Design deliverables (this card's DoD):** a decided SPEC (not a menu): the pipeline grammar (declarative steps over the ONE evaluator/lowering path) · the data-home IA (raw → governed model → specs → elements as the visible floor plan; DQ expectations declared at the raw floor — backlog rec #1 folds here) · the authoring surface concept (step rail + live grid + generated-query pane; where it lives in the studio IA) · Strangler route from today's tag-based editors · wave decomposition sized for WIP=1 · what is refused and why. Owner sees the concept BEFORE build waves fire.

**Hard boundaries.** Laws 1–3, 10–11 verbatim · one evaluator (@statdash/expr), one lowering path (resolveMeasureRef), ObsQuery stays the only wire query · author plane speaks governed nouns only (FF-AUTHOR-NO-QUERY class holds; the pipeline is the author's power WITHOUT raw-code exposure) · no object-model change · additive/Strangler, the old editors demote only when the new path is journey-proven.

---

## Log

### W-P0 — gates registered + baseline captured (engine-specialist, 2026-07-18)

S-sized wave: register the program's fitness gates + capture the equivalence baseline. **No `pipeline` discriminant, no `source` op, no UI, no engine behaviour change** (only a pure additive type-level `category?` seam). All bounds held.

- **`FF-PIPELINE-EQUIV` (BITING) — `platform/apps/api/src/provisioning/pipeline-equiv.fitness.test.ts` + committed baseline `platform/apps/api/provisioning/pipeline-equiv.baseline.json`.** For every DataSpec in the corpus SSOT (18 `query` specs, collected at the `data` residence), under two fixed canonical contexts (year `time:2020` + range `time:0`), records the store-read contract `extractRequirements()` derives — the {code, dims} warm/prefetch set, the pure store-free invariant a `pipeline` desugar must preserve (SPEC §1.3). Store-agnostic: metric/dim catalogs primed from the artifact's own `siteConfig` via `registerManifestMetrics`/`registerManifestDimensions` (the real boot seam), so governed metric-ids expand to DSD codes faithfully. Deterministic (canonical key/array ordering; regeneration produces an empty diff). Bites via deep-equal vs the committed baseline; regen with `UPDATE_BASELINE=1`. **This is the artifact W-P4's shadow byte-compares against; green here + `FF-JOURNEY-PIPE` gate the ⛔ W-P5 default-emission flip.**
- **`FF-VERB-COVERAGE` (BITING) — `platform/packages/core/src/data/transform/verb-coverage.fitness.test.ts`.** Pins the current 19-op registry inventory: a new/removed op fails loudly with a pointer to SPEC §1.2 (make the category decision). Proves the additive `category` seam is present + inert (every `getTransformStepCategory` is `undefined`; runtime dispatch byte-identical). The W-P3 total-coverage obligation is named as `it.todo`. The pure type-level seam (`StepCategory` union + `category?` param + `getTransformStepCategory`/`listUncategorizedOps`) lives in `packages/core/src/data/transform/step-registry.ts`, exported from the core barrel.
- **Pending gates registered (suite-visible `it.todo`, never silent):** `FF-JOURNEY-PIPE` (`platform/apps/panel/src/features/data-layer/pipeline-journey.fitness.test.ts`) · `FF-DQ-DECLARED` (`platform/apps/api/src/provisioning/dq-declared.fitness.test.ts`) · `FF-PROMOTE-ROUNDTRIP` (`platform/packages/core/src/data/promote-roundtrip.fitness.test.ts`) — each carries its SPEC reference + the wave it lands in.
- **Folded fix (0083 cluster, b544819 class) — `platform/apps/api/src/provisioning/config-cube-contract.fitness.test.ts`.** CHECK-2 previously resolved EVERY `filter.measure` through the metric catalog ("mirrors resolveMeasureRef") — but the runtime (`matchesFilter`/store-filter.ts) treats filter values as LITERALS and never resolves them. Tightened to engine-real semantics: resolution now applies ONLY to the folded top-level `measure` field (tracked via a new `literalDims` provenance set); a new **CHECK-4** fails any explicit `filter.<dim>` literal that is a governed metric-id (the exact hole that let b544819 through green). Falsified: clean on the current corpus, 18 violations when a metric-id is injected as a filter literal. Engine filter-resolution is NOT extended here — that decision is deferred to W-P4 (noted in the test header).

**Gate (parsed):** `tsc -b` EXIT 0 (full workspace) · full vitest from platform root **3694 passed | 81 skipped | 15 todo | 0 failed** (466 files; baseline was 3685+ green) · `pnpm eslint` clean on all 8 changed source files (the 20 pre-existing `ApexRenderer.tsx` lint errors untouched). **`packages/core` touched → engine dist rebuilt** (`pnpm --filter @statdash/engine run build`), though apps/api consumes none of the new exports (no functional dependency on the rebuild for this wave).

**W-P4 pre-notes (ledgered by the lead from W-P0 findings — read BEFORE W-P4 starts):**
1. **Discriminant-string collision:** DataSpec `type:'metric'` shares its literal with the KpiValueSpec `metric` variant (resolves via `resolveMetricValue`, not `desugar`). W-P0 disambiguated by RESIDENCE (DataSpecs collected at the `data` field) — W-P4's desugar/collectors must apply the same residence discipline, and an architect note should decide whether the collision gets a structural cure (rename vs residence-law) before real `metric` DataSpecs appear under `data`.
2. **Corpus coverage gap:** all 18 corpus DataSpecs are the `query` discriminant — the baseline proves `query`→`pipeline` equivalence richly but NOTHING for the other seven. W-P4 must add synthetic per-discriminant fixtures to FF-PIPELINE-EQUIV before claiming total equivalence; a green corpus replay alone is NOT sufficient for the ⛔ W-P5 flip.
3. **Engine filter-resolution decision (deferred here from the 0083 cluster):** whether `filter.<dim>` values learn governed-ref resolution uniformly (one lowering path) or stay literal-with-gate (CHECK-4) — decide INSIDE W-P4's `source` design, where the governed read is being shaped anyway.

### W-P1 — the live per-step data grid (senior-frontend, 2026-07-18)

M-sized, apps-only, reversible. The owner's "see the raw data while building" want, made real over TODAY'S `query.pipe` model — **no new discriminant, no `source` op** (that stays W-P4). New feature `platform/apps/panel/src/features/data-layer/pipeline-preview/`; mounted in `QuerySpecEditor` beside `PipelineBuilder` (NOT the three-pane shell — W-P2 re-homes the grid verbatim).

- **The projection = the ONE engine seam (E5, FF-ONE-DERIVATION-PATH).** `pipelinePreview.ts` `deriveStepRows(sourceRows, pipe, asOfStep)` = `applyPipeline(source, pipe.slice(0, asOfStep+1))` — the EXACT composition the `QueryResolver` uses (`storeObs` → post-fetch `effectiveBounds` clamp → `applyPipeline`). A full-pipe prefix is byte-identical to the bound element's rows. **NO module-level preview cache** — the source read is resolved ONCE (`usePipelineSourceRows`) off the live store; selecting a step is a PURE in-render re-slice, no re-fetch. The source read is warmed via `store.queryAsync(queryReadObs(query))` debounced (300ms) + cancel-on-supersede (closure flag), keyed on the resolved-obs-query ⊕ store instance.
- **E1 browse-first:** the grid shows the source read's rows from the first moment (`AS_OF_SOURCE = -1`); the empty pipeline IS the browse grid. A metric bind (`bindMeasureToSpec` → `query` spec) puts data on screen before any step.
- **E3 capped honest:** `capRows` → first 200 rows + an honest count note ("ნაჩვენებია 200 / 1650 სტრიქონი" — proven live), never a silent truncation.
- **Honest states (Law 11 / FF-CANVAS-NEVER-LIES):** `toGridCell` — a genuine 0 renders "0" (`ok`), a null/undefined/'' renders a declared "—" (`no-data`), never a fabricated 0. Loading is a DISTINCT state from empty (async trap #10). Grid-level states: `unbound`/`loading`/`error`/`unavailable`/`ok`, each a declared affordance (no silent blank). **Fail-soft:** a half-authored step that throws in an engine op (e.g. default `derive` → "missing 'as'") is caught → honest `error` state, never a crashed editor (the same throw NodeErrorBoundary catches on the live path).
- **Governed headers (Law 4 / FF-AUTHOR-NO-QUERY):** `buildColumnLabels` maps field → governed metric/dimension label off the `describeApp` catalog (`useMetricCatalog`), never raw codes. Proven live: headers rendered "გეოგრაფია · სექტორი · შრომის ანაზღაურება · … · პერიოდი".
- **WCAG (Law 9):** a real `<table>` — `<caption>` naming the current step, `<th scope="col">` governed headers, honest `data-cell-state` cells with aria labels. Bilingual ka/en throughout. Step rail: source/step chips carry `aria-pressed`.
- **Gate (parsed):** `tsc -b apps/panel` EXIT 0 · `pnpm eslint` clean on all changed files · panel vitest **1004 passed | 0 failed | 4 todo** (137 files); the new suites: `pipelinePreview.test.ts` (prefix-run correctness, cap+count honesty, column derivation, honest-cell 0-vs-null) + `PipelineDataGrid.test.tsx` (loading-vs-empty distinction, governed headers scope=col, honest "—", capped count note) — 26 tests.
- **Live proof (DoD):** synced (`dev-watch-panel.sh --once`, md5 verified) + probe `platform/e2e/probes/probe-wp1-pipeline-grid.mjs` on :3013 → bound `accounts.compensation` → grid shows 200/1650 REAL rows + governed headers; selecting the filter step changed the caption Get→"ნაბიჯი 1: filter"; back-to-source restored it. **Zero console errors.** Shots → `work/authoring-truth/wp1/`.
- **Seam for W-P2/W-P3:** `PipelineDataGrid` is a PURE view-model component (store-free, hook-free) — W-P2 re-homes it into the three-pane shell; the `pipeline-preview` barrel exports the projection + grid + source hook. Step selection state currently lives in `QuerySpecEditor` (lifts trivially to the shell).

**W-P2/W-P3/W-P4 pre-notes (ledgered by the lead from W-P1 findings + own craft review):**
1. **CRAFT (W-P2, binding):** the W-P1 grid lives in the ~320px inspector dock — columns truncate; the three-pane shell must give the grid REAL WIDTH (the Power Query perception fails in a squeezed dock). Re-home `PipelineDataGrid` verbatim into a wide surface.
2. **Entry gesture (W-P2):** `bindMeasureToSpec` (DATA-facet MetricPalette) reliably converts any element's `data` to a `query` spec — the shell's natural opening move; the grid currently mounts for the `query` discriminant only.
3. **Valid-by-default steps (W-P3):** an unauthored `derive` step throws (`missing 'as'`) — the verb palette must insert steps as VALID minimal shapes; grid fails soft (fixed in W-P1), canvas relies on NodeErrorBoundary.
4. **`MEASURE_DIM` barrel export (W-P4):** not exported from `@statdash/engine` (only `TIME_DIM`); W-P1 inlined the `'measure'` key — add the one-line barrel export in the W-P4 engine wave and de-inline.

### W-P2 — the three-pane authoring shell (senior-frontend, 2026-07-18)

M-sized, apps-only, reversible. The Power-Query-class surface: ONE screen, THREE panes always visible together (SPEC §3). New feature `platform/apps/panel/src/features/data-layer/workbench/`. **No new discriminant / no `source` op** (W-P4); built over today's `query.pipe`.

- **IA placement — the workbench IS a focus-view escalation (SL-4 node-field), NOT a dock section.** The pre-note #1 CRAFT fix (the W-P1 grid suffocates in the ~320px dock) is answered by escalating the whole surface OUT into the studio's established separate-screen for workspace-weight subjects (`FocusView` / `useFocusEscalation`): the DATA facet (`props.data`) hands the subject out via `escalation.escalate({ source:'node-field', fieldPath:'data', render:(bind)=><DataWorkbench …/> })`. The grid gets the full-viewport center column (`minmax(0,1fr)`) — the Power-Query perception restored — WITHOUT touching the StudioShell grid (canvas layout untouched). Back returns loss-free (shell state persists). Proven live: 200 real rows at real width, breadcrumb `კომპოზიცია / მონაცემთა ვორქბენჩი`.
- **Entry gesture (pre-note #2).** `DataFacetField` gains a primary **"Open data workbench"** door (`data-testid="open-data-workbench"`), shown when the spec is a `query` OR unbound (seeded fresh browse-first — E1). Binding a governed metric (MetricPalette → `bindMeasureToSpec`) makes `data` a `query` spec → the door opens the workbench. A non-query spec enters via the existing conversion (the advanced accordion — coexists, W-P5 gated). Fail-soft: no escalation host (unit tests) → the door hides, the in-place editors still serve.
- **Grid re-homed VERBATIM.** `PipelineStepGrid` mounts unchanged in the center pane (mission mandate). Rail = `PipelineBuilder` reused verbatim (dnd-kit keyboard-reorderable) with the Get head chip; step selection state lifted into `DataWorkbench` (`asOfStep`, default `AS_OF_SOURCE` — browse-first).
- **E4 — the generated-query pane IS the EXPLAIN seam.** New `GeneratedQueryPane` + pure `generatedQuery.ts` (`describeAuthorSteps`/`describeStewardDetail`). **Author plane:** a read-only, GOVERNED, bilingual declarative rendering — each step a friendly verb + the governed nouns it consumes (resolved through the SAME `buildColumnLabels` catalog the grid speaks; it never touches `queryReadObs`, so it *structurally cannot* leak a raw code). **Steward lens (`useRole`):** additionally the raw DataSpec JSON + the lowered ObsQuery (`queryReadObs` — fail-soft on an unresolved ref). No separate lineage viewer, ever. The 8-type discriminant `Select` is ABSENT from the workbench (author starts from Get — SPEC §3.4).
- **WCAG (Law 9):** three labelled `region` landmarks; the rail keyboard-reorderable; the grid a real `<table>`; the query pane a labelled region with a real ordered list. Bilingual ka/en throughout.
- **Gate (parsed):** `tsc -b apps/panel` EXIT 0 · `pnpm eslint` clean on all changed files · panel vitest **1023 passed | 0 failed | 4 todo** (139 files; baseline 1004 → +19). New suites: `generatedQuery.test.ts` (the governed-vs-raw fixture — author steps carry `Region` never `REGION`/`GE`; steward JSON carries the raw), `GeneratedQueryPane.test.tsx` (plane split: author governed + no wire truth; steward adds JSON/ObsQuery), `DataWorkbench.test.tsx` (three panes mount together, query pane mirrors the pipe, no discriminant Select, non-query honest note).
- **Live proof (DoD):** synced (`dev-watch-panel.sh --once`, md5 verified in-container `/app/apps/panel/src/…`) + probe `platform/e2e/probes/probe-wp2-workbench.mjs` on :3013 → bound `accounts.compensation` → **Open data workbench** → ALL THREE PANES in one screenshot with REAL data (rail + wide 200/1650 grid `წყარო: შრომის ანაზღაურება` + generated-query `წყარო: შრომის ანაზღაურება`) → added a Filter step → grid caption `წყარო…`→`ნაბიჯი 1: filter` AND the query pane 1→2 steps (`+ ფილტრი`), BOTH updated. **Zero console errors.** Shots → `work/authoring-truth/wp2/`.
- **OBSERVATION (W-P1 grid, surfaced not fixed — verbatim re-home mandate):** the re-homed grid still exposes author-plane raw in its CELLS + some HEADERS — member codes as values (`adjara`/`AGRI`/`GVA`/`A`), the metric label duplicated (both `value` + `measure` columns → `შრომის ანაზღაურება`), and `obsStatus` untranslated. These are `columnLabels.ts` / cell-localization concerns from W-P1 (grid re-homed verbatim per mission), borderline for FF-AUTHOR-NO-QUERY. The GENERATED-QUERY pane (W-P2's own surface) is clean — governed nouns only. Recommend a W-P3 grid follow-up: governed MEMBER labels + dedupe value/measure header + govern/hide `obsStatus`.

**W-P3 pre-notes (from W-P2 findings):**
1. **Grid author-plane rawness (above):** member-code cell values + duplicate metric header + `obsStatus` — a `columnLabels`/cell-localization pass belongs with the 7-verb palette wave (both are grid-perception polish).
2. **Verb labels are W-P2-LOCAL:** `generatedQuery.ts` carries a small op→verb display map (`VERB_LABELS`) for the pane's readability — NOT the registry `category` projection. W-P3 must make the pane's verbs a projection of the op registry's `category` field (the SSOT), and the field-ref noun extraction (`FIELD_VALUE_KEYS`/`FIELD_RECORD_KEYS`) should likewise derive from op metadata rather than the hand-listed key sets.

### W-P3 — the 7-verb palette + author-plane perception polish (senior-frontend, 2026-07-18)

S–M, crosses `packages/core` lightly (data-only additive `category` assignment) + panel UI. Built over today's `query.pipe` — no `pipeline` discriminant, no `source` op (W-P4).

- **Registry categorized (SSOT) — `FF-VERB-COVERAGE` now BITES.** Every one of the 19 registry ops declares its SPEC §1.2 `category` at its registration site (`packages/core/src/data/transform/index.ts`, the 4th `registerTransformStep` arg) — a data-only additive change through the W-P0 seam, ZERO runtime-dispatch change (`applyStep` never consults `category`). The mapping IS SPEC §1.2's table: filter→`filter`; aggregate/group/reduce/rollup→`aggregate`; derive/addField/template/concat/cast/window→`derive`; melt/select/rename→`reshape`; lookup/join/blend/**joinByField**→`combine` (joinByField = the resolved-rows underside of blend, SPEC §1.1); sort→`sort`. New projection helpers on the step-registry: `STEP_CATEGORIES` (the 7 verbs, canonical order, Get first), `getOpsInCategory(verb)`, `listOpsByCategory()` — all barrel-exported. `verb-coverage.fitness.test.ts` flipped from the W-P0 inventory-pin/`it.todo` to the biting total-coverage assertion: `listUncategorizedOps() === []`, every category ∈ the 7, the ⋃-of-projections round-trips to `listTransformOps()`, and a `CATEGORY_PIN` map pins the exact taxonomy (a new/removed/re-categorized op fails loudly with a SPEC §1.2 pointer). `get` maps to `[]` (no `source` op until W-P4) — an honest empty verb.
- **The 7-verb palette (panel, workbench-only).** `verbProjection.ts` (pure model) = `buildVerbPalette(locale)` PROJECTS the registry categories into 7 verb entries; the only panel-owned data is display chrome (bilingual verb labels = the SPEC §1.2 verb names, intent hints = the "simple words", concrete-op leaf names) — the GROUPING is the registry's, never a hand-list. `VerbPalette.tsx` = the "+add step" popover: 7 intent cards (icon + verb + hint), click a verb inserts its SPEC default op (Filter→filter, Aggregate→aggregate, …), an expander discloses the concrete ops (progressive disclosure). Wired into the workbench rail ONLY via a new `PipelineBuilder` `renderAddStep?` slot — the legacy editors keep their flat dropdown (Strangler; W-P5's door). The old W-P2 `VERB_LABELS` hand-map is DELETED: `generatedQuery.ts` now derives each step's verb from `verbLabelForOp(op)` → `getTransformStepCategory(op)` → the verb label (the SSOT; the pane and the palette speak the same 7 verbs by construction).
- **Valid-by-default steps (pre-note #3, root cause).** `defaultStep.ts`: the field-PRODUCING ops (derive/template/addField/concat, +rollup) now carry a non-empty placeholder produced-field name (`newField`) + a trivial expr — an inserted `derive` no longer throws `missing 'as'`. Fixed at the shared `defaultStep` (it honors that file's own stated "valid, minimal starting step" contract — strictly better for the legacy editors too, no behaviour regression). Proven live: inserting Derive added a `newField` column of `0`, grid stayed green.
- **Author-plane perception (the FF-AUTHOR-NO-QUERY edge).** New `memberLabels.ts` = `buildMemberLabels(profile, locale)` resolves a CELL's member CODE → its governed member label off the cube profile's codelists (`useActiveProfile`, Law 5), the peer of the header resolver. `_T` renders as the SDMX standard governed total ("სულ"/"Total") — a standard code adopted whole (Law 4), the ONE exception to the honest raw-code fallback. `PipelineStepGrid` is now plane-aware (`useRole`): the AUTHOR plane hides the SDMX plumbing echoes (`measure` dedup of the value column + `obsStatus`) and resolves members; the STEWARD plane keeps every column raw. Proven live: cells read `აჭარის ავტონომიური რესპუბლიკა` (adjara), `სოფლის მეურნეობა` (AGRI) — **zero adjara/AGRI/GVA/_T raw leak** in the author plane.
- **Get-step detail + no vestigial one-liner.** `describeAuthorSteps` head names the metric + the governed grain nouns (year, pinned dims — already generic via `grainDims`); a truly-empty spec (no metric + no steps) now returns `[]` so `GeneratedQueryPane` paints an honest "bind a metric to begin" hint, never a lonely `Get: (pick a metric)` one-liner. An unbound Get WITH steps still shows the prompt.
- **Gate (parsed):** `tsc -b` (full workspace) EXIT 0 · full vitest from platform root **3762 passed | 81 skipped | 14 todo | 0 failed** (474 files; baseline 3694 → +68: the FF-VERB-COVERAGE todo→3 biting tests + new suites) · panel vitest **1044 passed | 0 failed | 4 todo** (baseline 1023 → +21: `verbProjection.test.ts`, `memberLabels.test.ts`, `VerbPalette.test.tsx`, net generatedQuery). `pnpm eslint` clean on all 20 changed source files. **`packages/core` touched → engine dist rebuilt** (`pnpm --filter @statdash/engine run build`).
- **Live proof (DoD):** panel src synced (`dev-watch-panel.sh --once`, md5/grep verified in-container). **INFRA GAP (flag):** the `statdash-dev-panel-full` container serves `packages/core` from **baked SRC** (vite dev resolves `@statdash/engine` → `packages/core/src`, no dist), and that baked snapshot was STALE (Jul-16, pre-`relative-coord.ts`) — a surgical `docker cp` of only the changed core files 500'd on the barrel's dangling `./data/relative-coord` import. Recovered by tar-syncing the WHOLE local `packages/core/src` into the container (self-consistent) + restart. The mission's "docker cp the rebuilt dist" recipe does not match this container's src-based reality — flagged for the platform team. Probe `platform/e2e/probes/probe-wp3-palette.mjs` on :3013 → bound `accounts.compensation` → workbench → "+add step" shows **7 verb cards** → inserted **Filter** then **Derive** via the palette → both valid immediately (grid 200 rows green throughout), caption Get→`ნაბიჯი 1: filter`→`ნაბიჯი 2: derive`, query pane 1→2→3 steps → grid cells GOVERNED (rawLeaks: `[]`). **Zero console errors.** Shots → `work/authoring-truth/wp3/`.

**W-P4 pre-notes (from W-P3 findings — read BEFORE W-P4 starts):**
1. **`source` op auto-joins the palette by projection.** When W-P4 registers `source` with `category:'get'`, `getOpsInCategory('get')` becomes non-empty → the palette's Get card flips from honestly-disabled to insertable with ZERO panel change (the projection ideal held). Register `source` with `'get'` and add it to `CATEGORY_PIN` in `verb-coverage.fitness.test.ts` (the pin will fail loudly otherwise). Also de-inline `MEASURE_DIM` (W-P1 pre-note #4) — still `'measure'`-inlined in `columnLabels.ts` + the grid's `AUTHOR_HIDDEN_FIELDS`.
2. **Field-noun extraction still hand-listed (softer pre-note #2, deferred).** `generatedQuery.ts` `FIELD_VALUE_KEYS`/`FIELD_RECORD_KEYS` are still hand key-sets, NOT derived from op `PropSchema` metadata. Deriving them cleanly needs the schema to MARK which fields are field-name refs vs member values — a deeper correctness change than W-P3's scope. Fold into W-P4's `source`/schema work where the governed read is being shaped anyway.
3. **Grid caption still names the raw op (`ნაბიჯი 1: filter`).** `PipelineStepGrid`'s caption uses `pipe[k].op` (the English op code), not the governed verb — a minor author-plane roughness left because `verbLabelForOp` lives in `workbench/` and `PipelineStepGrid` is the lower-level `pipeline-preview/` (importing up would invert the layer). Not a raw-CODE leak (it's the op tag). Cure options for W-P4/W-P5: thread a caption-verb resolver from `DataWorkbench` down, or lift `verbProjection` to a shared leaf.
4. **Dev-container core is src-based + was stale (infra, above).** Any wave touching `packages/*` and wanting live proof must tar-sync the whole changed package's `src` into `statdash-dev-panel-full` (not a surgical file cp — the barrel will dangle) + restart. The container has NO dist; the "cp the dist" recipe is wrong for this line.
