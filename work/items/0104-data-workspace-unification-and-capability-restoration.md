# 0104 — Data-Workspace Unification & Capability Restoration

_Session handoff, 2026-07-20 (owner-directed: "restore the lost parts, unify the two pipelines FULLY with beautiful UI, then write everything down for next session"). Status truth: this card + ADR-051 + ADR-046 Add.4/5 + the DESIGN/STUDY docs. This card is the entry point for the next session._

## Origin
Owner circle-break (2026-07-20): "one concept, two independent sources" — the query **workbench** and the **advanced raw data editor** are two tools for one thing; "not ready in any aspect; we move too slowly." → platform-architect study proved a data-workspace archipelago; program **ADR-051 "One Data Workspace — the source is step 0"** (Strangler, waves DU1–DU6). Owner blessed full autonomy + "build the BEST UI/platform, research the reference class (Power Query/Retool/Grafana/Airtable/Looker-dbt), go faster." Standing mandate: **the ONE pipeline must do EVERYTHING the arsenal can AND be simple enough for a non-programmer — simple AND powerful** (owner authorized adopting reference-class libraries; verdict so far: adopt TanStack Table only, build the rest).

## Shipped today (committed on main, each live-J-walked on :3013)
- **DU1** `1e62e140` — Sources+Model folded into one `/studio/data` workspace (floors), old routes redirect.
- **DU2** `dbe5e142` — courier/teleport `sourcesHandoff.ts` deleted; cube-browse seeds the workbench via a URL one-shot (the likely freeze cause, gone).
- **DU3** `e1c99afd` — one editing surface: `SpecBody` → workbench fallback lane; both hosts route to the ONE `DataWorkbench`; removed the duplicate accordions + kind `<Select>`.
- **persistence** `39a32e99` — DataSpec edits now PUT to the API (were store-only/lost-on-reload since AR-49); optimistic + debounced coalesced PUT + honest Saving/Saved chip.
- **DU4a** `cc090228` — engine value-cell `source` variant (ADR-046 Add.4); **timeseries folds** onto the spine byte-identical.
- **DU4b** `952b230b` — **single-code growth folds** byte-identical (honest first-period drop).
- **DU4c/d** `9722e58c` — ratio-list + row-list assessed: **deferred to ADR-046 Add.5** (need the explicit-cells variant; kept on fallback with guards).
- **Step A** `fec72cab` — timeseries + single-code growth OPEN as the three-pane pipeline (reversible view; convert-on-first-edit = query behavior).
- **F2** `8243f5b7` — value-cell fold routes cross-store (governed metric-id) + honest '—' not fake 0 (Law 11); closed the FF-PIPELINE-EQUIV cross-store corpus gap.
- **authoring-hold** `5506cc59` — **INTERIM: DataSpec persistence PAUSED by default (`DEFAULT_AUTHORING_HOLD=true`)** — owner was auto-saving experiments and corrupting specs. Amber "Draft — not saving" + "Enable saving" toggle. Flip OFF (or ship the draft→publish model) to restore saving.
- **Docs:** ADR-051, ADR-046 Add.4 `d6ca4379` + Add.5 `bccd1798`; `docs/architecture/proposals/DESIGN-data-workspace-canonical-redistribution.md`; STUDY-canonical-panel-ia.
- **DU6-IA-1** `20739ac7` (night 07-22→23) — ✅ **CLOSED, J-LADDER 6/6 live PASS, 0 console errors** (evidence `work/authoring-truth/0104/30-jladder-*.png`). The declared floor ladder (`DATA_FLOORS` SSOT + `parseDataFloor`, FF-FLOOR-IS-DECLARED) — Sources · Model · **Specs** (new floor = the retired `DataModelingPanel`'s spec half, extracted verbatim: list + ShowMe + workbench takeover + URL cube-seed consumer retargeted model→specs) + source-CRUD re-homed to Sources. Capability-parity pre-gate held (survivor ⊇ removed, enumerated in the commit). Spec rows chip-only per owner §3d; `lifecyclePlacement.fitness` re-encodes the new contract (dense row band unrepresentable). In-flight agent's tree work protected + reconciled by the lead (pathspec-commit discipline held).

### DU6-IA-1 walk findings (chief-engineer, triaged — queued, not fixed)
- **F1 (URL-truth, Law 9 permalink):** after browse-in-workbench the URL reads `?dataFloor=specs&page=regional` — the `cube=` seed is consumed/absent, so the seeded workbench is NOT reproducible from the URL (refresh → bare list). Spec-vs-behavior divergence vs `studioDataWorkbenchPath` (which DOES set `cube=`). Root-cause candidate: the one-shot seed consumer strips params after read (DU2) — decide the canonical permalink (the created spec's selection should be URL-addressable).
- **F2 (URL-truth):** invalid `?dataFloor=bogusfloor` falls back to Sources visually but the URL keeps lying — normalize/rewrite to the canonical value.
- **F3 (URL-truth):** default landing carries no `dataFloor` while selector clicks write it — two URL spellings for Sources; one canonical URL per floor wanted. → F1–F3 = ONE small URL-canonicalization slice at the `useStudioRoute` seam.
- **F4 (design smell, feeds DU6-IA-2/DW-A):** every browse-in-workbench PERSISTS a fresh `… — ნედლი დათვალიერება` stored spec → near-duplicate rows with no disambiguator (echoes the 8-orphan-scratch-specs cleanup of 07-20). Raw-browse should reuse/be-ephemeral, not accrete stored specs; rows need a quiet disambiguator (source/created) within FF-METRIC-ROW-QUIET's ≤2-chip bound.

## Data restores (dev :3013 / portal :3012)
- **Regional** — `config.data_source[regional].datasetCode` had been flipped REGIONAL_GVA→GDP_ANNUAL (every regional pipeline silently read the national cube). Restored from provisioning. Verified (samtskhe/OTHER/GVA: 0 → 538.67).
- **GDP portal `/ka/gdp?mode=range`** — charts 1 (nominal level ₾) & 2 (per-capita $) render the WRONG series (both show the real-growth % series); 3 & 4 correct. **NOT a config corruption — the page config is byte-identical to provisioning.** Live-render + network trace root-caused it to a **real ENGINE bug** → see the priority item below. (Two of my premises were WRONG here: first "published page is fine" (config-read, not render), then "config is mis-bound, restore it" (it wasn't). Live render + network trace = truth; my premises are hypotheses. No PUT applied; backup `work/gdp-restore-backup/gdp-page-live-20260720-173540.json`.)
- **Orphan scratch** — broken GDP_AGRI/probe "(შემოთავაზებული)" specs (`d954e1ba` + 7 siblings, unbound/0-rows/regenerable) — **DELETED 8/8** (backup `work/data-spec-backups/orphan-suggested-specs-backup-2026-07-20-173919.json`; 32→24 specs, 0 collateral, verified against full page configs). Note: `/api/config/pages` list omits `config` bodies — a "is this referenced?" check MUST fetch each `/api/config/pages/:id`.

## ⚠️ THE REGRESSION INCIDENT (owner lost trust — must not recur)
DU3 + Step A **silently dropped capability**. Root: Step A's `toWorkbenchModel` fold gate conflated "engine CAN lower to a pipeline" with "the author SHOULD edit it as a pipeline." Confirmed losses (parity sweep):
- **R2 (HIGH):** timeseries/single-code-growth head READ-ONLY — can't edit `code`/`years`.
- **R3 (HIGH):** pivot diverted from `PivotEditor` (rows/keyField/values/colors) — violates its own POLA lossless-round-trip contract.
- **R4:** transform inline `source` rows + `encoding` not editable.
- **R5:** single→multi growth one-way trap.
- **R1 (DU3):** spec-type picker / kind `<Select>` (create-from-scratch + inter-kind convert) unmounted.
- **R6 (DU3):** read-only JSON disclosure gone for editor kinds.
- **Advanced-editor-in-pipeline:** for a query/pipeline in the three-pane, `encoding` editing, `FieldWells`, steward `FilterBuilder`, `MeasureSelector`, writable raw-JSON all unreachable.

**Why it happened (owned):** I verified PROXIES (FF-ONE-SPEC-EDITOR "no duplicate", one-gesture live walk) not the PURPOSE ("survivor ⊇ removed"). I HELD `feedback_verification_doctrine.md` ("verify purpose not proxy" · pre-action gate pt.2 "state the WHOLE target" · "old version = the spec") and did NOT execute it as a pre-gate. DU1/DU2/persistence/authoring-hold were clean supersets; DU3 + Step A were not.

**BINDING FIX (now a hard pre-gate):** before ANY retire/merge/fold — enumerate the removed surface's FULL capability set FIRST, prove the survivor is a superset, guard it with a fitness (`FF-EDITOR-CAPABILITY-PARITY`). DoD = "no capability lost", never "no duplicate". Executed + shown, not held passively. (memory: `feedback_capability_parity_gate`.)

## ▶ PRIMARY NEXT-SESSION OBJECTIVE — full unification + beautiful UI
Owner: "restore the lost parts, and **unify those two pipelines FULLY, with everything, including a beautiful UI**."
1. ✅ **Restore capability — DONE, LIVE-VERIFIED 2026-07-22 (chief-engineer QC walk, 7/7 PASS, real gestures).** R1 picker+convert (all 8 kinds driven live) · R2 timeseries/growth code+years edited · R3 PivotEditor keyField accepted · R4 transform inline rows+encoding · R5 single↔multi both directions · R6+writable raw JSON accepted an edit · query/pipeline still fold to the three-pane (no over-revert). Evidence: `work/authoring-truth/0104/01..08*.png`. Walk surfaced 5 findings — triaged into the elevation inputs below. Original spec (kept for the record): BUILT + GATED `8abb6b8d`. Narrowed the fold gate → timeseries/growth/pivot/transform back to their full dedicated editors (R2/R3/R4/R5); `WorkbenchAdvanced.tsx` = SpecTypePicker (R1, owned Radix) + AdvancedRawPanel (encoding/measure/filter/wells + writable raw JSON = the owner's complaint) + ReadOnlyJson (R6); new `FF-EDITOR-CAPABILITY-PARITY` gate reds the build on any future silent regression. 1275 panel tests green, tsc clean. NOT deployed — walk the 5 capabilities live on :3013 (see the commit body) to close it. This partially reverts Step A's three-pane VIEW for those 4 kinds (correct — it was read-only/lossy).
2. **THEN unify properly (next session):** ONE workbench that authors EVERY kind with FULL power (dedicated-editor capabilities available IN the three-pane — editable value-cell heads: measure/years, encoding, pivot fields, inline rows), re-admitting kinds to the three-pane ONLY when capability-parity is proven — with a **beautiful, reference-class UI** (Power Query applied-steps + Retool/Grafana single-flow + Airtable core-ops). This is the true end of "one concept, not two sources": simple forward, full power one click behind, beautiful.

## ✅ PRIORITY #1 — engine measure-drop: FIXED + LIVE-VERIFIED 2026-07-22
`a91cd74c` — `measurePin()` SSOT pins MEASURE_DIM in BOTH val/obs branches ('*'→skip for $ctx; explicit filter overrides). Gates: FF-OBS-MEASURE-PIN + FF-QUERY-RENDER-TRUTH (the missing truth gate). Class-sweep: no other measure-less key path. Lead independently re-gated (fitness 18/18 + whole-graph tsc), deployed BOTH tiers (:3013 image rebuild; :3012 portal batch — carried the parked 0092/0093 chrome+i18n), live-verified 6/6 on /ka/gdp (evidence `work/authoring-truth/0104/10-*.png`): chart1 nominal 22,148.7→104,598.1 ₾ · chart2 per-capita $3,281→$10,296 DISTINCT, full to 2025 (the 2017 truncation concern did NOT manifest) · 4 separate measure-scoped obs requests · console clean. Surfaced cosmetic: x-axis tick labels truncate to "2..." on all charts → folded into the visual-refresh item below. Historical spec follows:

### (was) 🔴 NEXT-SESSION PRIORITY #1 — engine measure-drop bug (live Law-11 violation)
`buildObsFilterParam` (`platform/packages/core/src/data/store-filter.ts:65`) folds `q.filter` for an `obs` query but **never pins the top-level `q.measure`** (the `val` branch DOES pin it). A `query` DataSpec carries its measure via `query.measure` (metric-ref) → `resolveQueryMeasures` (`registry/resolvers.ts:64`) returns `{type:'obs', measure, filter}` with measure top-level → the obs fetch/cache key is **measure-less** → reads the covering `{geo,approach}` slice (all measures) and collapses last-wins. So ANY two sibling `query` charts sharing a dim-slice but differing only by measure **both render the last-wins measure** (GDP charts 1 & 2 are the live victims; chart 3 = the winning measure so looks fine; chart 4 puts measure in `filter` so is correct; KPI uses the `val` path so is correct — the query-vs-KPI AR-38 asymmetry). **This is a live Law-11 "canvas lies" violation and is NOT GDP-specific.**
**Fix (Tier-2, engine — a config PUT cannot reach it):** in `buildObsFilterParam`'s obs branch, pin `MEASURE_DIM` from `q.measure` (single | array), mirroring the val branch. **Close the false-green:** warm-key ≡ read-key (both measure-less) is why gates stayed green — add a render-truth fitness: sibling `query` charts, same dims / different measure → **distinct** series. Root cause + evidence: chief-engineer memory `project_obs_measure_drop.md`. Deploy needs `docker cp core/src` (engine, per the topology note). This is a sensitive shared seam — full gate + live-render verify + capability-parity discipline; do NOT rush.

## Elevation inputs (QC walk 2026-07-22, surfaced beyond the checklist — feed the reference-class design pass)
1. **Law-11 plumbing-token leak (lead item):** pivot/transform editors show raw TS signatures to the author (`Record<string, DimVal>[]`, `Record<series, color>`) — an author must never see these.
2. **Metric-facet lies "data not bound"** for a chart with a working pipeline spec (facet blind to non-metric-ref specs) — a false honest-state.
3. **Duplicate metric options** in the perspective-scope dropdown (catalog dedup smell) — pairs with DW-C MetricCatalogView.
4. **Pipeline conversion is one-way** (picker omits `pipeline`) — capability asymmetry to close.
5. **`code` fields are unguarded free-text** (no offer/autocomplete vs raw-cube codes) — P-OFFER violation: the author never types an identifier.
6. **Owner 2026-07-22 — the dataTable right-dock is "mushed + doesn't fit":** root = the dock acts as an editing warehouse; canon (Figma/Webflow/Builder class + one-model-two-zooms) = summary + doors + progressive disclosure. Lands in E3 (Binding Summary) + DW-B (core-ops band) + 0102's concern-group inspector remedy — this owner pain RAISES those waves' priority.
7. **Owner 2026-07-22 — metric scoping (site vs page)** → card 0107, folded into DW-A: scope-declared MetricDefs, scoped offers (page-first ranking), promote gesture. Treats "metrics pushed forward" dislike at the root (relevance by scope).

## ✅ WAVE E0 — CLOSED 2026-07-22 (the elevation program's first wave, end-to-end in one day)
Design (ADR-052) → backend `db51e189` (V39 config.revision + validated PUT, fitness 10/10) → panel `2fed78e7` (lifecycle band both zooms, authoring-hold DELETED, main's lazy-seam red root-fixed) → deployed dev (V39 applied, api+panel images) → **J-LIFECYCLE walked live 8/8** (draft→publish→reload-persists→restore-appends→discard; evidence `20-*.png`) → walk surfaced the code-resolution gap → hardened `30ee70e7` (`code-resolves` vs live SDMX codelist + contracts adoption + role-gate SSOT, fitness +4) → **live re-proof: the exact bogus publish now 422 `code-resolves /spec/pipe/0/code`**. Combined suite 4139/1 (only 0106). Owner door #2 (revision contract) exercised as designed.
Open echoes ledgered: fake-0 render on unresolvable code = engine honest-null (ADR-047 Wave B) · POST/create unvalidated (drafts-are-free stance; revisit at draft/published split) · dim-MEMBER `member-resolves` check (feasible via stats.classifier; YAGNI until a walk proves the class) · row-band density = owner's open UI call.

## E0 residue (small, resolve in the next backend/design touch — not blockers)
- **api adopts `ConfigViolation` from `@statdash/contracts`** (panel promoted the 422 wire shape to the contracts layer; `apps/api/src/lib/problem.ts` keeps a byte-identical local copy — Strangler swap, backend-owned).
- **publish-role gate SSOT:** `lib/publish-roles.ts` is the SSOT; `pages.ts` still carries its own copy — harmonize.
- **Design note (accepted divergence, correctly argued):** DESIGN-0104 §C3 named the DATA facet as the band's compact zoom, but the facet edits INLINE `element.data` specs (no `config.data_spec` docId — they publish with their PAGE). The faithful compact zoom = the Specs-floor browser row; inline-spec lifecycle = the page's lifecycle (a later, separate declaration). Design doc stands corrected by the build — carry this into E1+ briefs.
- Lint truth: see card 0108 (20 pre-existing packages/react engine errors — "lint green" is NOT currently true repo-wide).

## Owner verdict on the Model floor (2026-07-22 — DU6 pulled FORWARD by owner order)
Verbatim: «საერთოდ არ მომწონს ეს გვერდი (/studio/data?page=gdp&dataFloor=model). გადააწყვე საერთაშორისო ui და ფუნქციონალებიც. ასე ერთად შეტენილი, რა ზევით რა ქვევით. არა საერთაშორისო გაიდლაინების სტანდარტის, არც პლატფორმის დონის და არც ფრეიმვორკის ხარისხის». DU6+DW-A design starts NOW (reference class: Looker model view · dbt Explorer · Power BI model · Airtable schema); folds 0107 (metric scoping), 0110 ("what data & how built"), verdict #4 (DataFlowMap dedup, browse/edit lens dissolution), Placement-Law routing. Build slicing decided at design review (floor-IA parts may precede E2a; workbench-embedded parts stay after E2 per the accepted rationale).

## Queued program (fold into the above / DU6)
- **5 UX items** (owner live review) + `DESIGN-data-workspace-canonical-redistribution.md` verdicts: (1) metric IA scatter → one `MetricCatalogView` (Looker/dbt "one definition, many views"); (2) core table ops forward on the element (a "workbench"=the powerful DataWorkbench; basics as a lightweight band/popover); (3) Sources classifiers 3→1 disclosure (renderer is world-class, tune defaults); (4) Model-floor canonical+beautiful redistribution = **DU6** (de-dup DataFlowMap, dissolve browse/edit lens exclusion); (5) popups on a principled interruption axis (add the missing modal/confirm-dialog). Root: route data-workspace surfaces through the existing Placement Law (`resolveSurface.ts`) instead of hand-placement. Waves DW-C→DU6→DW-A→DW-B→DW-D.
- **VISUAL refresh** of chart rendering (owner: GDP page "structure good, visual outdated" — flat gray bars/heavy dark). Reference-class theme/typography/color. Structure = canonical Section⊃Elements⊃metric-binding reference.
- **Per-capita chart stops at 2017** while siblings reach 2025 — real data/binding gap to investigate.
- **DU4 remainder:** ADR-046 Add.5 build (explicit-cells `cells` variant → fold ratio-list + row-list) · multi-code growth (calc-metric-browse, DU4e — flagged not-byte-identical, needs its own equivalence proof) · then the ⛔ one-way emission flip (DU5) gated on FF-ALL-KINDS-SHAPED + FF-PIPELINE-EQUIV full-corpus + J-walk.
- **Data-integrity guards (from the corruption incident):** the real fix for auto-save is a **draft/dirty → explicit Save/Publish (+ discard/undo)** model (like pages); add **version history** to `config.data_source`/`config.data_spec` (destructive UPDATE today — only git provisioning saved regional); add **PUT validation** (a source's dims ⊆ the datasetCode's DSD dims; datasetCode exists).

## Environment / ops facts (load-bearing)
- **Deploy topology:** dev container `statdash-dev-panel-full` bind-mounts only `panel/src`; `packages/*` are baked → an ENGINE change needs `docker cp core/src` (or image rebuild), NOT just `dev-watch-panel.sh`. "Committed" ≠ "live on dev" for packages/*.
- **Live-check host:** playwright against the REAL container — studio `http://192.168.1.199:3013`, portal `http://192.168.1.199:3012`; API `http://192.168.1.199:3011/api` (auth `admin`/`dev_admin_pw_123`). Do NOT boot a local panel (502s on the Docker api hostname).
- **authoring-hold is ON** → :3013 is NOT saving DataSpec edits until flipped (or draft→publish ships).
- Backups: `work/gdp-restore-backup/`, session scratchpad `BACKUP-live-data-specs-*.json`.

## Discipline carried forward
WIP=1 (finish end-to-end: built→gated→**live-J-walked**→shown). **Capability-parity pre-gate before any retire/merge.** Verify the PURPOSE on the live render, never a proxy. Owner feedback QUEUES, never abandons. Decide-and-drive reversible work; don't ask what I can decide.

## 🏛️ ONE-PIPE — the two-dialect unification EXECUTED (2026-07-23 night, owner-ordered)
Design `DESIGN-one-pipeline-grammar.md` (lead pen; D1–D8, U0–U4) + `RECON-two-dialect-capability-map.md`. Executed: **U0** dialect honesty `ee436398`+`3af53567` · **U1** live-switch widened `f503a057` (one desugar scope) · **U3 ⛔ FIRED `930bebe9`** — pages gdp(8)/regional(6)/accounts(4) + 12 stored specs → pipeline, PUT 200 all, backups `work/data-spec-backups/one-pipe-u3-*`, parity 0-fail, idempotent re-run; acceptance walk 4/5→5/5 after the structural-coincidence fix (evidence 60-u3-*, 61-one-block). **At rest the grammar is ONE.** Queue: normalize-on-write (api PUT seam) · U2 `cells` head (1 blocked scratch spec + ratio/row-list class) · U4 contract (bespoke resolvers retire w/ parity) · provisioning seed JSON re-emission.

## 🎨 AUTHORING SURFACE FINISH — the ACTIVE arc (owner-ordered 2026-07-23 morning: «საუკეთესო UI, მარტივი და უძლიერესი» — finish this FIRST; lead's personal pen + specialists' knowledge)
Design: `DESIGN-authoring-surface-three-zooms.md` (Z1–Z9, waves W0–W5) — PROPOSED, awaiting owner pick. Grounding: authoring-surface recon (parity checklist = the retire gate; D8 green-field; sugar-leak audit) + reference-class UX brief (PQ/Metabase/Retool/Grafana). Thesis: three zooms over ONE artifact (element popover → step-builder → canonical JSON); escalation = lens change, never convert. Subsumes: the U4 authoring half · E2a (step-editor registry made real) · UX-backlog #2/#5 (#6 = E0, shipped). **W0 = the one-dialect hygiene (normalize-on-write · `buildSuggestedSpec.ts:44` still emits `type:'query'` — live corpus regained 1 query post-U3 · Advanced/fallback lane emission flip · provisioning re-emission) — precedes all view-building.** Rename lands in W3: «გაფართოებული / დაუმუშავებელი» → «გაფართოებული / კანონიკური ჩანაწერი».

**✅ W0 CLOSED 2026-07-23 (`0ffc46b0`+`4c06a99e`, live-proven):** api normalize-on-write on ALL three write doors (POST/validated-PUT-merged-snapshot/restore; lower-then-validate) · FF-ONE-DIALECT-AT-REST red-green demonstrated · provisioning 18×query→pipeline (requirements byte-identical, backups w0-*) · ShowMe emits spine · lane flip capability-scoped (`lowerLaneEmission` — blanket lowering would recreate the Step-A regression, deliberately avoided) · live gesture: sugar POST → stored pipeline; corpus 27 pipeline + 1 row-list (U2-blocked, honest). Allowlist (byte-frozen, empties at U2): ratio-list · row-list · growth(multi-code) · **metric** (fold undesigned — open D1-tail work, brief gap caught by the builder).
**W0 residual holes (queued, not blockers):** pages PUT does NOT normalize node-resident (inline `element.data`) specs — a fallback-lane sugar edit saved with its page still rests as sugar in `config.page_version` · POST `/config/data-specs` lacks the E0 referential gate (dialect seam only). Fold into the next api touch.
**NEXT: W1** — Z3 shape-detection registry (packages/core: detect/project/absorb, first shape one-jump; FF-SIMPLE-VIEW-ROUNDTRIP) → Zoom-0 element popover.
