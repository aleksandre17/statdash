---
id: "0086"
title: "ONE MODEL, TWO ZOOMS — the DATA facet becomes summary+door; the workbench is THE editor (owner-caught duality)"
status: DONE (2026-07-18, build agent Opus) — the DATA facet is now SUMMARY + one prominent «გახსენი ვორქბენჩი» door; the inline full editor left the author plane (retained steward-lens only, plane law). Live-verified: bound element → summary (წყარო·ნაბიჯები·სტრიქონები, 415 rows read honestly) + door, `inlinePipeAuthor=0`; unbound → honest affordance + quick-bind; zero console errors. Shots: work/authoring-truth/0086/01-02. Gates: panel vitest 1085/0 · tsc -b apps/panel clean · eslint clean.
was: QUEUED-HOT (2026-07-18, owner verbatim: «ორი ვარიანტია — workbench ან ააგეთ პაიპლაინი, ვეღარ ვხვდები რა განსხვავებაა… გაერთიანდეს?» — lead decided: unify per the Power BI / Figma canon)
class: S-M
priority: P0
owner: lead → build agent (Opus); pairs with 0085 in one wave (same facet/pane area)
implements: Power BI (report view = compact wells; Power Query Editor = THE editor) · Figma/Builder.io (inspector = projection, advanced = escalation) · progressive disclosure with ONE editing model — never two parallel editors
links:
  - platform/apps/panel/src/inspector/controls/DataFacetField.tsx    # today: workbench door + the full inline QuerySpecEditor — the duality
  - platform/apps/panel/src/features/data-layer/editors/QuerySpecEditor.tsx
---
**The rule (decided):** the inspector DATA facet presents ONE thing — a compact read-only SUMMARY of the element's data (governed source name · step count · row count/state, honest states incl. unbound) with ONE primary action «გახსენი ვორქბენჩი» (prominent — this also closes the owner's discoverability complaint). ALL pipeline editing lives in the workbench. The inline QuerySpecEditor/PipelineBuilder mount leaves the author-plane facet (its components already live re-homed in the workbench; the steward-advanced lens may retain the raw editors per the plane law until the ⛔ W-P5 demotion formalizes it).

**Boundaries.** The summary derives from the SAME workbench model (`toWorkbenchModel` — one derivation, no second interpretation) · unbound = the honest affordance leading INTO the workbench (the J4 door) · bilingual, WCAG · no engine change · the MetricPalette quick-bind for an UNBOUND element may stay in the facet (one-gesture bind is a Power BI fields-well equivalent — quick action, not an editor).

**DoD.** Live: select a bound element → the facet shows the summary + the one door → open → workbench edits → back → the summary reflects the change · select an unbound element → honest affordance → bind lands · no «ააგეთ პაიპლაინი» inline editor in the author facet · gates: panel vitest + tsc + lint · screenshots · zero console errors.
