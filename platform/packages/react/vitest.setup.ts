// Global vitest setup for engine/react — runs before every test file.
// @testing-library/jest-dom extends expect() with DOM matchers
// (toBeInTheDocument, toHaveTextContent, etc.).
import '@testing-library/jest-dom'
