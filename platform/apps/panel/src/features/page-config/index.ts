// ── page-config — page-root (PageConfigBase) authoring surface [V3] ───────────
//
//  The Page Inspector: authors the page root's presentation / frame / perspectives /
//  vars through the EXISTING generic Inspector (pageSchemaSource), round-tripping
//  losslessly through page.meta. Mirrors features/filters (page-scoped).
//
export { PageInspectorPanel } from './PageInspectorPanel'
export { pageSchemaSource, pageSchema, pageGroups } from './pageSchemaSource'
