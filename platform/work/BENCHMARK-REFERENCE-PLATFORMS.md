# Benchmark — statdash-platform vs the reference-platform class

> **SSOT for capability comparison.** The one-body instrument the lead consults at every Leader's Scan to spot where we functionally LAG the config-driven / visual-builder standard (Form.io · Builder.io · Grafana · Superset/Metabase · Retool/Appsmith) and where a half-formed concept needs completing to its canonical form.
>
> **Owner:** platform-architect. **Consulted by:** the lead at every Leader's Scan (orchestrator §Second Mandate). **Refresh trigger:** a platform capability change (new node/seam/registry) OR reference-class movement (a leader ships a new capability model).
>
> **Reading rules:** *Reference-leaders* = market claims from domain knowledge of those products. *We-today* = grounded in files cited (path). *Severity* = **None** (at/above standard) · **Lag** (have the seam, under-built/under-populated) · **Missing** (no seam). *Opportunity* = the canonical adoption in one line. Registered visions are cross-linked to `ARCHITECTURE-REGISTRY.md` (AR-*) and NOT re-litigated here.
> _First scan: 2026-07-08 (platform-architect)._

---

## Capability matrix

| # | Capability area | Reference leaders ship (concrete) | We today (cited) | Severity | Opportunity (canonical form) |
|---|-----------------|-----------------------------------|------------------|----------|------------------------------|
| 1 | **Config schema versioning / migration** | Grafana dashboard `schemaVersion` + forward migration chain; Builder.io content-model versioning; Form.io form revisions | `CURRENT_SCHEMA_VERSION=5` ordered migration chain (`core/config/migration.ts`), per-node `version`+`migrate` hook, pre-load `migratePageConfig` runner (`react/.../migratePageConfig.ts`), forward-compat guard throws on future config | **None** | Hold the standard; publish the migration authoring guide as the Constructor gains node authors |
| 2 | **Node registry + palette metadata** | Grafana panel/plugin catalog; Retool component palette; Builder.io registered components w/ inputs; Backstage software templates | `NodeSliceMeta` carries `label`(i18n)·`icon`·`category`·`schema`(PropSchema)·`defaults`·`slots`·`groups`·`caps`·`version` (`plugins/**/meta.ts`); `describeApp()`→generated `page-config.schema.json` | **None** | Hold; surface `caps`/`category` as the Constructor's palette taxonomy filter (AR-10 consumes this) |
| 3 | **Declarative logic / expressions** | Vega-Lite predicates/calc; Form.io JSONLogic; Retool `{{ }}` sandboxed JS; Superset Jinja allowlist | `@statdash/expr` AST — comparison/logic/math/string/collection/lookup ops, `registerExprOp` extension, no `eval`, JSON-serializable (`expr/src/ops/*`, `types.ts`) | **None** | Hold; the safe-eval canon is done right — keep expressions off `MetricDef`/config-as-code |
| 4 | **Validation model** | JSON Forms / RJSF Ajv-at-boundary; Form.io server+client schema; Grafana provisioning validation | Two-tier: core structural floor shared by api-save + render (`core/validation/config.ts`), PropSchema field validation (react/plugins), generated JSON Schema, RFC-9457 Problem Details at api (`contracts/problem.ts`) | **None** | Hold; add cross-field/semantic rules (`crossValidate`) to the same floor as the Constructor authors richer configs |
| 5 | **Conditional visibility / logic** | Form.io conditional + advanced-logic (`effect: show/hide/disable/require/setValue`); JSON Forms `rule`; Retool per-component show/disable | `VisibilityExpr` boolean tree `eq/neq/in/isset/and/or/not/perspective-*`, registry-extensible (`visibility-schema-registry.ts`), Constructor-authorable — **effect = show/hide only**; predicates are param-vs-literal (no field↔field / numeric compare in the gate op set, though `expr` has them) | **Lag** | Widen the effect vocabulary (enable/disable/require/setValue) + let the gate reuse `expr` predicates — one rule→effect grammar (adjacent to AR-41/42, not a new axis) |
| 6 | **Data-source abstraction + caching** | Grafana datasource plugins; Cube.dev `dataSource`; Retool resources; Superset DB connectors | `DataStore` port + `datasources[]` manifest (`kind: external/api/stats`), `CachedStore` + warm, `fromSDMX` sole adapter boundary (Law 5); metric-`dataSource` routing (`manifest.ts` `ManifestMetric.dataSource`) | **None** | Hold; semantic layer completion is AR-40 (registered) |
| 7 | **Interactions (cross-filter / drill / actions / events)** | Grafana data links + panel events; Vega-Lite selections/params; Superset cross-filter; Retool event handlers | Cross-filter READ side + store `=ANY` + KPI reactivity live; `on[]`→CommandBus one-write-point designed; general WRITE adapter for the dead `on[]` port still one-off (geograph) — see `project_cross_filter_capability` | **Lag** | Registered: AR-36/AR-38 (pivot/directional) + AR-42 (full grammar). Close the general Action WRITE adapter through the single CommandBus point |
| 8 | **Theming / design tokens** | Grafana theme + `$var` tokens; Builder.io/Plasmic design tokens; Retool themes | DTCG token layer, `[data-theme]` dark/light switcher node (AR-13), `NodeStyles` `sx` responsive prop + `@layer` cascade + named PARTS (AR-1/4/7), chart chrome reads tokens (AR-14) | **None** | Above standard; continue token-constrained `StyleField` authoring (AR-11) |
| 9 | **i18n model** | Contentful/Sanity locale fields; Grafana i18n (chrome only); Builder.io localized fields | `LocaleString` on every content field, resolve-at-boundary seam, `activeLocales`/`defaultLocale` from `config.locale` registry (`site.ts`), per-slice `i18n` bags (`meta.ts`), leak fitness gate (AR-26/37) | **None** | Above standard for the class (most builders localize chrome, not data+config+chart) |
| 10 | **RBAC / permissions per node / page / datasource** | Grafana folders + RBAC + datasource perms; Superset roles + RLS; Retool app/resource permissions | Only publish-state (`status: published`, `page_version` FSM) + auth at api; **no per-page/node/datasource authorization, no authoring roles** | **Missing** | Within-tenant authz model (author/reviewer/publisher roles + node/datasource scope) — complements the deferred MT (AR-30), does NOT require it → **NEW proposal** |
| 11 | **Authoring UX (undo · preview · draft/publish)** | Builder.io visual editor + live preview + revision history; Grafana edit/save/version; Retool undo stack | Draft/publish exists (`page_version` publish FSM, `is_published`); Constructor panel (`apps/panel`); **undo/redo, live preview, draft-diff not evidenced** | **Lag** | Fold preview + version-diff into the authoring governance workflow → **NEW proposal** |
| 12 | **Plugin / extension SDK** | Grafana signed plugin SDK + `plugin.json`; Backstage plugin API; Retool custom components; Superset viz plugins | Rich INTERNAL registry (`registerSlice`/`registerSpec`/`registerMigration`/`registerMetrics`) but monorepo-internal — no external versioned SDK, capability manifest, scaffold, or engine-version compat contract | **Lag** | Externalize the seams as a contract-first, versioned `@statdash/plugin-sdk` ("ship capabilities, not one-offs") → **NEW proposal** |
| 13 | **Governance (audit trail · config provenance)** | Grafana version history + provisioning-as-code; Builder.io revision log; Contentful/Sanity editorial workflow | DATA provenance strong (SCD-2 `reference-metadata.ts`, `agency_scheme` AR-20, lineage vision AR-43); **config-change audit (who/when/why) + draft→review→publish workflow + rollback thin** — only `page_version` rows | **Lag** | Editorial-workflow FSM + config-change audit + version diff/rollback (distinct from AR-43 data lineage) → **NEW proposal** |
| 14 | **Export / embed / permalink** | Grafana snapshot + iframe embed + CSV/image export; Superset export; Datawrapper/OWID embed codes | Permalink strong (URL=SSOT, `useSearchParams`); **export = STUB** (`plugins/CLAUDE.md`: "Excel+CSV per section — now: stub"); **no embed/snapshot** | **Lag** | Headless export/embed/snapshot delivery port (per-section CSV/Excel/PNG + embeddable card + shareable snapshot) → **NEW proposal** |
| 15 | **Accessibility** | Most builders: chrome-a11y only; OWID/Observable: explorable + SR parity | WCAG 2.1 AA baseline (Law 9), sr-only data-table parity, roving tabindex, keyboard nav, no color-only, methodology/preliminary badges | **None** | Above standard; explorable/narrative dissemination is AR-44 (registered) |

---

## How to use at a Leader's Scan
1. Re-ground each **We-today** cell against the cited file (memories/snapshots go stale; the code is authoritative).
2. Re-rank the **Lag/Missing** rows by (Constructor-phase leverage × distance from standard) — that ranking is the scan's Top-5.
3. New **Missing** rows or a leader shipping a new model = a refresh trigger; add the row, then propose an initiative into `ARCHITECTURE-REGISTRY.md` (never here — this file stays one-body).
4. Honesty rule: keep the **None** rows visible. A benchmark that only lists gaps mis-calibrates the platform's real standing (versioning, expr, validation, tokens, i18n, a11y are genuinely reference-grade).
