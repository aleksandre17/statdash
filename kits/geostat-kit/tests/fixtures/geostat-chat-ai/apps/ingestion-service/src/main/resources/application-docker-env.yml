# generated by geostat config-gen — do not edit
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/geostat?currentSchema=ingestion}
    username: ${POSTGRES_USER:geostat}
    password: ${POSTGRES_PASSWORD:geostat-dev-change-me}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:geostat}
    password: ${RABBITMQ_PASSWORD:geostat-dev-change-me}
