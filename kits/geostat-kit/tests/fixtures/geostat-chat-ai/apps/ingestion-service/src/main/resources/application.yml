# generated by geostat config-gen — do not edit
server:
  port: ${INGESTION_PORT:8091}

spring:
  application:
    name: ingestion-service
  config:
    import: optional:classpath:application-custom.yml
  profiles:
    default: nodb
    group:
      hybrid:
        - db
        - hybrid-env
      docker:
        - db
        - docker-env

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
