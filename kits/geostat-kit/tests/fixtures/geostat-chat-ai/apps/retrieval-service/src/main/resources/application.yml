# generated by geostat config-gen — do not edit
server:
  port: ${RETRIEVAL_PORT:8090}

spring:
  application:
    name: retrieval-service
  config:
    import: optional:classpath:application-custom.yml

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
