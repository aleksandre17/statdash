# Full-stack compose (API + UI)

Generated by `geostat compose-gen` into this folder.

Env loaded from `ops/config/backend`, `ops/config/frontend`, `ops/config/deploy.env`.

```powershell
.\tools\geostat.ps1 stack up -d --build
.\tools\geostat.ps1 stack -Prod up -d --build
```
